# 更新日志

## 6.2.3

### NAI 生图插件直连后端

- 生图后端新增 `nai` 选项：选择后生图请求直连 NAI 生图插件 `astrbot_plugin_nai_image`（通过其扩展接口），无需在线图片 API、ComfyUI/SDGen 或本地代理。
- 沿用现有扩展检测逻辑：总览接口新增 NAI 插件安装状态，面板的“主动/用户生图”开关、生图领域筛选和生图排障入口在 NAI 插件安装后即可见，与“我会画给你看”的显示规则一致。
- NAI 直连路径复用同一请求契约（需求正文 + 结构化背景分节），提示词模式、尺寸和风格由 NAI 生图插件本地适配；启动维护与负载检查对 NAI 直连跳过本地负载门禁。

## 6.2.2

### 生图与群聊唤醒修复

- 收紧群聊弱唤醒词判定，单独提及“机器人”等上下文词不再轻易打断群聊，明确面向 Bot 的问题仍可触发。
- 配套独立生图扩展修复传统提示词元数据泄漏到本地图像模型，以及 ComfyUI 参考图路径未转换为 Base64 的兼容问题。

### QQ 官方 Bot 绑定与配置引导

- 修复 QQ 官方 Bot 使用 `/陪伴 绑定主动消息` 时可能被误判为帮助命令的问题，兼容完整命令、带斜杠命令和适配器剥离前缀后的动作命令。
- 配置页刷新绑定状态时会重新读取概览和用户列表，清理旧的空引导草稿，自动填入已绑定用户与 `qq_official` 平台。
- 用户摘要会从已绑定的主动投递会话推断平台，避免官方 Bot 已绑定但页面仍显示 `aiocqhttp` 或“目标用户未配置”。

## 6.2.2a

### 扩展拆分后的配置与文档

- 明确独立创作扩展 `astrbot_plugin_content_companion` 的职责边界；创作项目、续写、审校、创作记忆和作品封面由扩展负责，主插件保留兼容回退和上下文协同。
- 配置引导会根据扩展检测状态隐藏本体旧版创作参数与创作模型入口，避免同一能力在两个页面重复配置。
- QQ 空间明确标记为本体保留的 OneBot/Cookie 兼容能力，未检测到平台能力时不展示可用入口。
- 更新 README、元数据、配置 Schema、答疑说明和两套面板构建，补充扩展安装顺序、迁移来源和工具注册边界。

## 6.2.1a

### 边界反馈与临时情绪内置化

- 边界与情感反馈、临时情绪状态及情绪余波继续由陪伴主插件统一承载，不再作为需要单独安装的扩展插件展示。
- 保留既有配置开关、用户状态、情绪事件记录和消息主链；升级或移除旧扩展目录不会要求重新配置，也不会丢失兼容数据。
- 总览接口现在区分“核心内置能力”和“可选外部扩展”，诊断页不会再把内置能力误报为未安装。

### 创作能力拆分

- 新增可选扩展 `astrbot_plugin_content_companion`，承接创作项目生成、续写、审校、记忆整理和作品封面流程。
- 陪伴主插件通过兼容桥接优先委托外部扩展；扩展未安装或关闭时自动回退到原有 `CreativeMixin`，不改变聊天命令、主动推进和数据格式。
- 旧版 `creative_projects`、创作配置和封面索引继续读穿，后续迁移无需重新建立作品。

### TTS 外语正文

- 修复快速标签模式下，日语或英语全量语音回复偶发漏写语音标签后，将朗读稿与中文可见正文一同作为普通文本发送的问题。
- 非中文语音规则现在明确要求：不使用语音标签时只能输出可见聊天语言；响应兜底仅在高置信的“外语朗读稿 + 中文译文”裸输出结构下保留中文正文，不影响用户明确要求显示外语文字的回复。

## 6.2.0

### 本地生图提示词

- ComfyUI 与 SDGen 现在只接收可直接渲染的正向画面描述，不再把分段标题、参考图裁决、负面词块和编排说明一并送入 CLIP/T5；NAI 与自然语言格式保持原有语义。
- 生图审计同时保留完整编排提示词与本地后端实际提交提示词的哈希和长度，便于排查而不改变远程生图后端行为。

### 现实触及拆分

- 现实触及已拆分为独立联动插件 `astrbot_plugin_reality_companion`（我会来到你身边）。
- 主插件不再直接加载 OpenCV、sounddevice、soundfile 等设备依赖；摄像头单帧、本机音频、用户授权、现实提醒和设备主动语音策略由联动插件独立管理。
- 安装联动插件后，主插件会通过受限扩展 API 继续提供人格、TTS、主要用户权限、会话投递和官方 Cron；旧命令“陪伴 现实触及 ...”保持兼容。
- 联动插件首次启动时会从主插件复制已有现实触及授权、策略、闹钟和提醒记录，迁移采用独立数据文件，不共享可变状态。

## 6.1.3

### 生图负面提示词

- 生图设置新增“安全默认、合并自定义、完全替换”三档负面提示词策略，并支持全局、文生图、自拍/人像和改图四级词库；传统、自然语言和 NAI 提示词格式统一生效。
- 完全替换只移除系统基础负面词，用户明确排除、衣着裁决和参考图一致性约束继续保留；图片供应商自身的内容安全策略不受插件配置影响。

### 配置页分类

- 配置页在原有功能域导航上新增“发送前、发送时、发送后、后台运行”作用阶段筛选，并同步优化桌面端和移动端排版；筛选只改变浏览视图，不改变配置归属和保存格式。

### 参考图审核与关系角色

- 完善参考图审核配置、路径安全校验和群聊关系角色选图；模型明确点名已绑定且可用的关系角色时，可由工具选择受管理参考图，同时继续拒绝猜测路径和未授权人物图片。

### 摄像头与媒体投递

- 摄像头单帧能力补充依赖诊断、私聊授权承接、群聊请求校验和失败回执约束，工具未取得画面时不再允许模型编造“画面黑”或“镜头被挡”。
- 当前媒体投递支持在明确请求下发送给请求者本人，并继续限制来源目录、文件时效、真实图片格式和目标身份。

### 代码与测试维护

- 清理已被关系意图新链路停用的大段死代码，修正重复配置元数据，并更新对应架构与行为测试。

## 6.1.2

### 关系网长期记忆

- 修复关系节点的“长期记忆”仍固定读取 LivingMemory 数据库的问题；当前使用“我会牢牢记住你”时，页面会按绑定的可信私聊身份读取其脱敏分类摘要并显示实际来源，只有未启用该桥接时才保留 LivingMemory 兼容查询。

### 配置导航

- 生图与 TTS 功能的关联参数卡片新增模型配置快捷入口，可直接跳转到“生图模型”或“语音合成”子页；离开存在未保存内容的页面时仍会先请求确认。

### 通用嵌入模型

- 模型页新增独立的“通用语义嵌入模型”，只列出 AstrBot 已加载的 Embedding Provider，并在快速、精准两种配置模式下保持可见。
- 表情语义召回默认继承通用嵌入模型，旧版表情专用 Provider 配置继续作为兼容覆盖，不会迁移丢失。
- 群内黑话可复用通用向量能力识别未知短语与已确认词义的语义近似；结果只作为当前回复的软参考，低相似度、模型不可用或调用失败时仍使用原有精确匹配和本地规则。

### 表情素材识别

- 修复表情素材自动识别沿用普通图片候选顺序、可能先调用 AstrBot 图片转文字或转述模型的问题；现在优先使用模型页配置的“插件识图模型”，失败后再依次尝试其备用模型和现有视觉候选。
- 修复精准配置把“工具结果转述”文本模型误接到插件识图链路的问题；普通图片、表情包和引用图片现在始终使用独立的“插件识图模型”，未配置时只回退 AstrBot 本体图片转文字，不再把 DeepSeek 等文本模型当作视觉模型调用。

### 表达规则分享

- 表达学习页支持把已审核的抽象表达规则导出为分享包；分享内容不包含用户、群聊、原始消息、证据、反馈或使用记录。
- 支持预览导入、重复项检查和目标范围选择；默认进入审核队列，也可在信任来源时直接启用。

### 现实触及摄像头

- 现实触及新增独立的摄像头单帧能力：全局开关、用户策略和知情授权互相独立，音频授权不会自动授予摄像头权限。
- 摄像头只在明确任务下读取一帧并立即释放，默认不保存原图；视觉模型仅整理在场、活动、可打扰性和光线等有限状态，不做人脸识别或持续监控。
- 管理页面支持摄像头索引、读取间隔、超时、用户授权状态和手动单帧测试；设备不可用时安全返回错误。
- 新增可选的“现实视觉好奇”主动链路：由主动 Agent 根据完整语境判断是否值得看一眼，不按吃饭、穿搭等关键词触发。
- 用户可分别选择关闭、先询问或授权后允许主动单帧；低主动强度会自动降级为询问，直接读取另受每日额度、行为冷却和未回复保护约束，且默认关闭。

### 生图额度

- 修复生图有效额度为 `-1` 时可能被当作不可用的判定；负数哨兵值现在统一按不限量处理。
- 排障页不再把生图后端、Token 暂缓、关系资格和范围额度混成“后端或额度不可用”；范围额度为 `-1` 时会明确显示不限量，并指向真正的其他阻塞来源。

### 主动问候

- 修复用户已在上午自然发起并持续进行对话后，Bot 仍绕过近期会话保护另行发送早安的问题；早安现在与午安、晚安一致，会由当前问候时段内的自然互动满足并取消。
- 日常仪式生成与终审会优先服从最近对话，不再用“还没说早安/午安/晚安”等理由打断已有话题。

### 跨插件成图投递

- 新增仅限当前会话的 `pc_send_current_media` 工具，用于承接其他生图插件本轮已经生成、但尚未投递的本地图片；`pc_generate_photo` 自有发送链保持不变。
- 当前媒体投递仅接受 AstrBot 临时目录或本插件成图目录内、30 分钟内生成且不超过 32 MB 的真实图片，并阻止伪造扩展名、同轮重复投递与模型猜测历史路径。

### 分段与标点兼容

- 分段词和内容清理词可自动匹配常见全角/半角标点变体，避免配置逗号、问号或感叹号后只对其中一种宽度生效；实际发送正文不会被改写。

### 回复去重

- 修复部分 Agent 在当前会话生成正文的同时附带空 `send_message_to_user` 调用时，分段中间正文先发送、工具报错后正式正文再次发送的问题。
- 当前会话的空文本工具调用会安全回退到一次最终回复；正常文本、媒体、跨会话主动发送和定时任务保持原有行为。
- 加强 DeepSeek 工具调用提示，普通当前会话文字优先直接回复，避免产生空工具调用和附带中间正文。

## 6.0.10

### 外部主动能力配置控件化

- 陪伴面板「外部主动能力」卡片不再要求手写 JSON：`config_schema` 支持控件类型元数据（`select` / `text` / `bool` / `number` 及 `options`），注册方声明类型后，卡片自动渲染为下拉菜单、开关、输入框和数字框，保存时按控件收集配置。
- `platform` 类枚举字段渲染为下拉选择（如 auto/twitch/bili），布尔字段为开关，数字字段为数字输入，文本字段为输入框；字段说明仍随控件展示。
- 旧式 JSON 文本输入保留为回退路径：`config_schema` 没有控件类型信息时，卡片仍显示「自定义配置」JSON 编辑框，不影响存量第三方插件注册的能力。
- 控件化保存会保留未展示的扩展配置，`select` 保持字符串、数字和布尔值类型，数字控件支持注册方声明的范围和步长；仅显式声明支持类型的字段会替代 JSON 编辑器。
- 两套陪伴面板（`陪伴面板` / `companion-panel`）同步支持控件化渲染并更新静态资源缓存版本。

### ChatUI 会话兼容

- AstrBot ChatUI（webchat）会话随普通私聊统一放行，不再需要用户名或昵称特判，也不会触发旧的未授权拒绝。

## 6.1.0

### TTS、分段与闹钟

- 强化 TTS 覆盖范围，统一处理主动消息和被动回复的分段、语音转换与降级路径，减少长文本整段失败和主动消息漏 TTS。
- 修复分段配置在新版本链路中失效的问题，补充官方 TTS 分段兼容处理。
- 优化 QQ 空间/主动场景的语音生成上下文，保持无可用 Provider 时的文本回退。
- 修复现实触摸/起床闹钟的配置保存、触发和试听链路，避免当天闹钟状态与页面显示不一致。

### 私聊权限与关系边界

- 新私聊用户默认开启私聊权限，但默认关闭私聊主动陪伴；普通私聊不再因历史授权字段误拦截。
- 增加高置信关系底线余波：明确强迫、威胁或严重针对性伤害才会扣分，玩笑、引用和用户自己的边界保持中性。
- 越界后关系分与主动性会短期收敛，分数随时间自然恢复；真诚道歉可有限恢复，重复越界会回收此前道歉恢复并升级余波。
- 通过提示词引导克制、简短和礼貌的表达，避免把角色边界实现为过硬的固定封禁。

### QQ 空间与陪伴面板

- 修复 QQ 空间面板在 AstrBot 跨域插件页面中动态 `import()` 解析到 `about:blank` 导致的模块加载失败。
- QQ 空间和 Provider 面板改为继承当前页面 `asset_token` 的经典脚本懒加载，并提升入口缓存版本。
- 优化陪伴面板响应式布局、配置控件化、图片引用和页面资源加载回退。

### 用户请求生图范围与额度

- 旧“生图使用范围”复选框改为主要用户私聊、其他陪伴用户私聊、群聊和 Bot 主动生图四项数字额度，并与显式指令和主链 `pc_generate_photo` 工具额度一起放在“用户请求生图”配置组维护。
- 四项范围额度默认均为 `-1`：`-1` 表示不限量，`0` 表示不允许，正数表示每位请求者或目标用户的每日限额。
- 用户请求生图/改图每日上限改为默认 `-1`：`-1` 表示不限量，`0` 表示不允许，正数表示每位陪伴用户的每日限额。
- 升级时会把旧范围中已勾选的项迁移为 `-1`、未勾选的项迁移为 `0`；未保存过旧范围配置时四项保持默认 `-1`，旧版明确全不选时四项均迁移为 `0`。
- 升级时还会一次性把旧用户请求额度的 `0` 迁移为 `-1`，保留原有不限量行为；迁移完成后维护者再明确设置 `0` 会保持禁用，不会被后续启动覆盖。

### 私聊权限移除

- 普通私聊不再要求用户档案具备私聊授权，旧档案中的关闭状态也不会阻断回复、指令或 LLM 请求。
- 主动联系权限保持独立并默认关闭；面板只保留主动权限开关，历史私聊权限配置与按钮已移除。

### 分类型固定生图提示词

- 新增文生图、自拍/人像和改图三项固定提示词；QQ 空间配图、主动消息、每日穿搭、命令和工具生图统一按最终 `workflow_kind` 选择，不再只能共用一条全局固定词。
- 分类型固定词会清理控制字符与内部提示词结构标记、拆分正负面语义，并继续执行服装冲突保护；旧全局固定词保留为兼容叠加项。
- 生图 trace、完整调试 JSON、近期生图记录和排障页新增分类型固定词的配置来源、应用状态、清洗状态及移除规则。

### 关系上限与成人档边界

- 优化合并 PR #114：普通用户正向亲密度阶段上限不再错误套用到主要用户；主要用户在普通关系模式下的自动加分、配置下调迁移和面板手动调分均可继续达到完整分数范围，专属联结仍保持分数冻结。
- 普通用户在面板手动调分超过阶段上限时会明确报错，不再静默截断。
- 成人私密档可分别关闭“专属关系”和“爱意互动”门槛，默认均保持开启；主要用户、私聊、后台成年确认、当轮明确同意和指定 Provider 精确匹配继续作为不可放宽的硬边界。

## 6.0.9

### 外部接口排障

- 合并并优化 PR #113：天气、余额和网页搜索配置区新增即时请求测试，直接使用尚未保存的当前表单值，并以隔离副本执行，不改写正式运行状态、缓存或密钥配置。
- 两套陪伴面板统一提供忙碌态、结果提示和诊断详情，快速重复点击不会重复提交；诊断仅保留必要的来源、路径、余额和搜索摘要，接口地址参数及凭据会脱敏。
- 补强外层调度异常的兜底脱敏，临时表单 Key 即使在测试初始化阶段报错也不会进入页面响应或警告日志。

### 命令帮助显示

- 修复插件命令帮助自动表格把尖括号占位符当成 HTML、把竖线当成列分隔的问题；参数说明改用全角括号和斜线展示，命令实际用法不变。

### 人格与创作预设

- 创作提示、日程状态模板和内置创作兜底不再默认把陪伴对象写成女性；未明确配置性别或代词时，会使用角色名、Bot、TA 或省略代词，温柔、细腻、理性等气质不再绑定性别。
- 世界知识工作台新增中性、男性、女性、非人/拟人四类角色资料预设，均可填入后继续手动修改，帮助快速建立更多元的人格与创作方向。

### 版本信息

- 插件元数据、README、运行时公开身份和更新日志统一更新为 `6.0.9`。

### 主动动作回访

- 动作回访现在独立归入对话延续路线，不再被普通定时提醒误判为明确事务。
- 根据五档主动配额调整动作回访的创建条件、强度上限和预计完成后的缓冲时间；用户提前回来可取消，未回复不会形成二次催促或忽略压力。
- “我在忙/没空/晚点聊”等边界不再被擅自估算为可查岗动作。

### 识图诊断

- 插件识图 Provider 测试现在会真正携带一张最小 PNG 图片，不再以纯文本测试冒充视觉链路测试。
- 表情包批量识别日志和错误信息细分超时、图片输入不支持、空响应和 JSON 不可解析等情况，便于定位模型或接口问题。

## 6.0.8

### 次要用户旧图片会话兼容

- 修复部分次要用户的旧会话历史包含 `image_url` 多模态段时，文本聊天模型返回 `messages[n]: unknown variant image_url, expected text` 的 400 错误；发送请求前会把历史图片段降级为文本占位，保留原有聊天记录与本轮图片识别能力，无需删除会话或重新添加用户。

### QQ 状态稳定性

- 修复自定义 QQ 状态提交失败或回执格式不兼容时，插件紧接着回退“在线”而造成两个状态快速跳转的问题；自定义状态现在至多提交一次，失败时保持账号原状态。
- 关闭自定义短状态同步后，日程细化会优先输出“保持不变”，旧细化即使仍含自定义状态也不会再强制覆盖为在线，从而保留账号手动设置的状态。

### 互动状态手动修正

- 修复管理员把“回避”手动改为其他互动状态后，旧的自动 `no_contact` 接触边界仍在下一条消息中把状态改回“回避”的问题；手动修正现在会同步解除冲突的旧自动边界，并在后续普通聊天中保持生效。
- 用户之后若再次明确表达“不想聊、别联系”等新边界，仍会按本轮真实意图重新进入回避，不会被历史手动状态永久锁定。

### 表情包语义检索

- 表情素材库新增可选 Embedding Provider，可按素材名称、描述、可见文字、标签、情绪和沟通用途建立向量；模型页和表情实验参数只列出已加载的嵌入 Provider，并可直接测试向量生成。旧素材无需重新识别，会在后台按批次增量补齐，Provider 不可用、超时或索引缺失时自动回退本地检索。
- 未启用嵌入模型时也会使用轻量、可解释的本地语义近似，例如“高兴/开心”“安慰/抱抱”“无语/无奈”；语义仅作为软加分，仍保留关键词、范围、近期使用降权和反馈偏好排序，并识别“不开心”等否定表达，避免反向误选。
- 修复“表情向量扫描上限”只保存配置却未参与运行时检索的问题；现在按配置限制本轮扫描的已索引素材数量，不影响完整关键词检索。

### 版本信息

- 插件元数据、README、运行时公开身份和更新日志统一更新为 `6.0.8`。

### 面板发布缓存

- 模型页的单次 Token 上限输入按 256 为步进，减少无效的小数值配置；中英文陪伴面板的静态资源版本戳同步升级，更新后不会继续命中旧浏览器缓存。

### 表情包与 GIF 兼容

- 修复表情包概率已命中但 GIF 视觉输入被 Gemini 兼容接口拒绝，导致本轮不发表情或直接返回 500 的问题；单帧和动态 GIF 现在统一转换为 PNG 预览供模型识别，平台发送时仍保留原始 GIF 动画。
- GIF 解码失败时只跳过该视觉输入，不再把不兼容的 `image/gif` 原样交给 Provider，也不会因此中断正常文字回复。

### 当前人格重置

- 新增“陪伴 重置当前人格”和陪伴面板重置入口；操作前以 UTF-8 JSON 归档旧资料，只重建当前人格的用户、关系、日程、状态、日记、世界知识、创作与运行记录，保留插件基础配置、多人格列表和窗口绑定。
- 重置后递增本插件人格资料代次，并清理本插件相关提示词与窗口缓存；AstrBot 会话历史和外部长期记忆不在本次操作范围内。

### 私聊权限迁移与回流保护

- 修复统一人物迁移把已有私聊授权覆盖成默认关闭，启动和首次私聊会保留可追溯的旧权限证据；配置目标、旧主人档案和适配器身份轮换可安全归一，群聊观察与自动建档不会凭空获得私聊权限，管理员明确关闭始终优先。
- 修复 OneBot/QQ 适配器把 Bot 的拒绝回复回送成普通私聊事件的问题；现在识别出站方向、Bot 自身事件和同作用域拒绝回流，避免固定拒绝文本触发无限回复。图片、表情、卡片等真实非文本消息仍会正常进入聊天链路。
- 拒绝消息发送失败、撤回或平台无返回时会释放消息去重与回流占位，允许同一条消息重试。

### 生图结果与主动工具回退

- 生图服务已生成图片但临时 URL 下载失败时，会按可重试错误短暂重试，并准确区分“上游已生成”和“本地素材可发送”，避免误报成功或重复消耗备用生图额度。
- 主动框架允许受控的 `pc_generate_photo` 工具和纯文本生图恢复，其他私聊工具继续保持边界；请求级错误仍可进入备用端点。

### 首次配置测试反馈

- “测试主动消息”会在保存配置期间显示忙碌状态，防止重复点击；保存或测试失败时会在面板给出明确诊断。

### 记忆插件探测兼容

- 修复“我会牢牢记住你”已经安装并启用，但因 AstrBot 模块别名、热重载实例入口或旧版公开 getter 不同而显示“未检测到记忆插件”的问题；现在优先从 AstrBot 已注册插件实例读取桥接，并兼容两种公开入口。
- 启动顺序导致的桥接缺失只会短暂缓存，页面会自动重新探测；已安装但未加载、Bridge 关闭或桥接协议版本不兼容会分别说明，不再统称为“未检测到”。

### 被动天气查询

- 配置天气来源后，已启用的私聊或群聊中明确询问当前天气，会由插件按需读取实况并注入本轮回答，不再让模型先调用搜索、浏览器、地图和记忆等多个无关工具；常规状态注入关闭时也能独立工作。
- 修复和风实时天气请求使用无效的 `lang=zh-Hans` 而被接口以 HTTP 400 拒绝的问题，改用和风当前支持的 `lang=zh`，避免静默回退到其他天气来源。
- 和风天气实况会同时保留体感温度、风向、风力和湿度；未来预报、天气插件配置讨论及明确询问用户所在地的问题不会被当前实况误答。
- 配置地点仅在主人私聊中显示，次要用户与群聊只获得天气实况；天气源暂时失败时会要求如实说明，并限制重复查找。

### 生图消息收束

- 修复模型未填写或只填写“生成好了”等空泛生图 `caption` 时显示固定状态句的问题；现在按自拍、改图和普通画面使用对应的短回执。
- 修复生图工具已经发送图片后，模型尾随正文仍被分段发送并触发表情附件的问题；确认图片发送成功后，本轮只保留图片及其 caption，尾随文本和反应表情都会被丢弃。

## 6.0.7

### 统一人物档案与群聊关系链路

- 合并 PR #110：私聊、群聊观察、统一画像和关系账本共用同一事件级人物档案；按平台、适配器实例和 Bot 账号隔离同名/同 ID 用户，避免串人格或串窗口。
- 群聊观察只建立中性统一档案，不会凭群消息授予私聊陪伴、主动触达或主人权限；主人群聊投影仍与真实私聊关系分离。
- 兼容旧版关系开关语义，并保留显式关闭优先级；旧版完整 UMO 用户键、历史权限和配置迁移均可回滚。
- 修复官方 QQ 绑定、图片/视觉缓存、表情反馈、AtRelay、TTS 和输入状态链路在跨平台身份下的资料键污染；输入状态继续使用 UMO 中的数字运输 ID。

### 模型卡 Token 限额与备用路由

- 在每张模型卡的“请求超时”旁新增可选的“单次 Token 上限（预估）”，按当前请求的提示词、视觉输入和最大输出预算估算；留空表示不限制。
- 当主模型的单次估算超过该卡上限时，会跳过主模型并直接尝试已配置的备用 Provider；主模型未配置备用时保持原调用行为。
- 普通文本、工具调用、私聊/群聊识图、合并消息识图、表情素材库识别、夹层阅读和 TTS 文本转换均使用同一套限额判断；主模型失败、超时、空响应或语义错误仍沿用原有备用链路。
- 两套陪伴面板同步显示和保存限额，旧配置不设置该字段时不会改变原有模型选择。
- 修复连续未回应一次就把“有效主动上限”硬压到 1 的问题；每日额度保持配置值，未回应只按既有倍率逐步拉长主动间隔，用户详情同时显示降频原因。

### 独立识图模型隔离

- 修复快速/精准配置切换或页面刷新后，插件识图模型可能被工具转述等文本模型覆盖并显示为 V4 FLASH 的问题；独立识图配置会跨模式保留，显式清空仍会生效。

## 6.0.6

### 统一人物档案与权限边界

- 新增跨私聊、群聊与 Memory 联动的统一人物投影；按 Bot 账号、适配器、平台身份和当前人格精确解析，同一用户在多人格模式下保持资料、群作用域与画像完全隔离。
- 私聊陪伴、主动私聊和智能画像改为相互独立的管理员权限，新建观察档案默认全部关闭；未授权私聊会在 Provider、Memory、工具、画像和关系结算前直接返回可配置文本，避免无效模型与 Token 消耗。
- 管理面板新增统一人物编号、私聊/主动权限、全局或单用户画像模式及 Memory 同步状态；保存权限后会立即刷新列表状态，普通资料保存不再误改主动额度。
- 修复管理面板在前后端热更新不同步时保存统一人物权限提示“统一用户权限服务不可用”的问题；页面 API 会回退到同一权限服务完成更新，仍保留原有管理员校验、能力边界与审计记录。

### 首次配置测试反馈

- 修复首次配置中的“测试主动消息”在保存当前配置期间没有可见忙碌态、按钮仍可重复点击的问题；现在会显示“保存中…”，并在保存失败时在测试面板留下明确错误，避免用户误以为点击无效。
- 群成员观察只保留群内别名和局部观察，不再建立第二套关系权威；明确查询第三方画像会在检索前拒绝，本人仅能读取已授权的公开低敏画像，普通群聊中的偏好讨论不受影响。
- 增强身份关联、解绑预览、来源事件去重和旧权限迁移：操作 ID 会绑定请求内容，解绑后可信度按剩余身份回落，损坏迁移记录可安全恢复或明确报错，回滚会先完整校验再写入。

### 生图续拍引用来源

- 自拍续拍不再把同一会话最近发送的成图自动塞入参考图候选，也不再让选图模型决定是否复用上一张；未引用图片时按普通人设图、参考图库和今日穿搭选图。
- `pc_generate_photo`、`rule_fast` 自然语言自拍和显式“陪伴 自拍/拍照”命令会直接读取本轮携带或引用的图片作为显式参考来源；引用之后仍沿用原有职责、服装与场景连续性处理，改图和合影的参考图失败关闭规则保持不变。

### 生图提示词表达方式同步到工具生图
- 修复用户指令触发 `pc_generate_photo` 工具生图时，注入的【图库表情与生图工具】说明没有包含“提示词表达方式”的格式要求，模型可能按自己的习惯书写 prompt，与主动拍照/自拍链路的格式不一致的问题；现在工具说明会按 `photo_generation_prompt_format` 配置明确要求 `prompt` 参数使用对应的书写格式，与主动生图保持同一套格式约束。
- 修复工具执行阶段仍会无条件构造自然语言提示词分段的问题；`nai` 会保留 `{}`、`[]` 和权重语法，`traditional` 会保留正向/负向结构，只有 `natural_language` 才进入自然语言分段流程，并将同一份格式快照传到实际生图后端。

### OpenRouter 参考图链路

- 在线生图平台新增 `openrouter`，也会按 `openrouter.ai` 地址自动识别；文生图继续使用已兼容的 `/images/generations`，携带参考图时改用 JSON `POST /images` 与 `input_references`，最多提交 3 张参考图，不再请求不存在的 `/images/edits`。
- OpenRouter 的 `/api/v1` 地址无论是否带尾斜杠都保持原样；原有 `/api/v1` 到 DashScope `compatible-mode/v1` 的改写现在仅作用于阿里云相关主机，避免误改其他 OpenAI 兼容地址。
- 模型页、配置保存、命令配置与两套陪伴面板均可选择 OpenRouter；排障页会把生图 HTTP 404 归类为“端点不匹配”，并提示核对实际请求 URL 和平台协议。

### 分段回复剩余组件

- 出站确认事件和 Bot 自身事件不再被当作新的入站消息，避免平台回执让尚未发送的分段被提前截断。
- 已开始发送的表情表达会继续发完排队中的正文段落；普通回复仍会在真实的新入站消息到达后停止，避免打断用户的新话题。

### QQ 官方机器人首次绑定与身份归一

- QQ 官方机器人用户可先在当前私聊发送“陪伴 绑定主动消息”登记首次投递窗口，再到配置引导明确开启私聊与主动权限；绑定指令本身不会越过默认关闭的权限边界，其他未授权指令仍由原权限门处理。
- 将适配器可能返回的完整 FriendMessage UMO 归一为稳定 openid 资料键，并迁移历史重复资料；完整 UMO 继续作为投递路由保留，避免用户页出现顶着同一名字的运输层记录。
- 配置引导与关系网支持已确认属于 QQ 官方目标的 opaque 平台身份 ID，未知来源的任意文本仍不会被当作人物身份。

### 表情选择与媒体发送

- 修复普通闲聊触发表情表达判定后，当前媒体工具虽已按本轮范围收起，但模型仍可能受人格提示和历史成功生图调用影响，返回当前工具列表中不存在的旧调用的问题；现在会同步清理本轮不可用媒体工具的历史调用组，并注入当前轮次边界，实验表情继续走内部标签，正常显式生图入口不受影响。
- 表情反馈会按用户、素材和意图保存有界偏好；后续选择在语义相关性基础上轻量偏向用户喜欢的素材，并降低不喜欢素材的排序，不会用偏好覆盖明显不符的语境。
- 偏好摘要参与低延迟检索缓存版本，避免不同用户或偏好更新后命中旧选择。
- 纯图片或纯语音消息会移除引用组件；存在正文时引用仍只绑定第一段正文，避免语音被引用回复或产生孤立引用卡片。

### TTS 请求与外语可见正文

- 用户明确要求、补发或指出漏发语音时，TTS 后处理会优先生成实际语音内容，不再只回复“这次会发”等确认话术。
- 外语语音默认保留当前聊天语言的可见正文；仅当用户明确要求显示对应外语文字时保留外语正文，并修复全量外语语音块被重复转换或漏发的问题。

### 时效内容与页面稳定性

- 天气预警与环境变化候选采用更短生命周期，过期或前一天遗留的变化不会在次日才作为主动消息发出。
- QQ 空间好友动态过滤“官方 Qzone”推广账号，并在诊断信息中记录过滤数量。
- 参考图元数据审批增加有界超时；主模型长期无响应时及时回落到本地证据结果，不再让页面请求一直等待。

## 6.0.5a

### 游戏伴侣联动

- 扩展 API 新增幂等的结构化游戏事件入口；按人格、私聊/群聊、会话和游戏隔离短期余韵，由当前人格判断胜负在意程度、共同参与温度、连胜连败上限、持续时间和再次邀请意愿。
- 游戏余韵作为普通回复、实时场景和主动消息的语气底色，不修改用户伤害、拒绝或长期关系状态；过期后自动停止注入。
- 外部主动能力新增同步可用性回调，并将能力冷却细化为按用户记录；执行前会在并发锁内再次检查冷却和可用性，便于游戏插件在用户已进入房间或房间容量不足时撤下邀请候选。
- 兼容旧版事件 ID 与状态镜像，补充乱序、重复、并发、异常数值和长期 scope 清理保护；外部上下文只作为提示词资料，不依赖简单关键词封禁。

## 6.0.5

### 关系与情感表达统一

- 被动回复、主动消息和 TTS 共用统一的人物、关系与表达决策；结合事件归属、情绪余韵、渐进恢复和有界表达维度调整语气，减少单轮情绪翻转。
- 情绪只影响表达方式，不会提升关系、隐私、P4、内容或主动权限；群成员资料保持观察用途，归属不明的负面事件按中性处理。
- 新增本地脱敏事件与表达诊断，并强化跨插件能力校验、可靠投递和生命周期边界。

### 结构化参考素材与兼容性

- 关系用户、关系角色和知识参考图统一经过作用域投影与素材安全门；未解析、越权或不安全素材会失败关闭，不会绕过正式生图边界。
- Page API、模型回退和测试诊断继续使用脱敏结果信封；多人格页面上下文、参考图目录与 Token 统计保持隔离。

### 表情包发送格式

- 表情包图片新增“普通图片 / QQ 表情格式”选项；自动表情、主动表情和显式图库工具统一遵循该设置，普通生图不受影响。
- OneBot/NapCat 选择 QQ 表情格式时会保留 QQ 图片表情子类型；QQ 官方机器人及其他平台会自动回退为普通图片，避免平台能力差异导致发送失败。

### QQ 空间 UIN 身份防护与模块化重构

- 查看说说会优先按用户原话中的“你/我”识别 Bot 与当前用户，只有归属确实含糊时才请求确认；返回结果继续用作者 UIN 二次校验，避免昵称、模型参数或选择器造成身份串位。
- 保留 `detail` 等扩展参数、发布时间提示、评论明细与登录失效的结构化结果；同一条件失败时不会诱发连续重试，“下午六点多 / 六点半”及仅给出“下午”等中文时间表达会按对应时段匹配，畸形时间不会静默退化为最新动态。
- QQ 账号与当前人格分开标注：多人格共用账号时，只有动态编号或正文命中当前人格已核验的发布记录，才会认作当前人格经历；其余 Bot 账号动态按共享账号观察处理。
- 发布反查拿到的真实动态编号会回写当前人格的已核验记录；即使发布接口最初没有返回 `tid`，后续查看也能完成可靠归属核验。
- 评论结果超过 30 条输出上限时会保留真实已加载数量并标记列表不完整，不再因截断后恰好有 30 条而误判“没有用户或 Bot 的评论”。
- 外部动态只作为带来源的当前观察，不再用整回合硬开关丢弃同一轮的其他有效记忆。
- 将原 4485 行 QQ 空间单体实现拆分为运行时、动态流、评论、发布、调度、认证、媒体和错误类型模块；保留 `QzoneMixin`、旧异常导入和自动发布补丁路径兼容。
- 新增身份、记忆边界和模块职责回归测试。详细设计、改动理由、预期效果与实际测试结果见 [QQ 空间 UIN 身份与模块化重构](docs/QQ空间-UIN身份与模块化重构.md)。

### 参考图元数据引导编辑

- 参考图库新增通俗选择题引导编辑器，仅在添加或配置参考图时打开；主模型会交叉审批多题证据并生成可预览、可追溯的 v2 结构化元数据。
- 选图试跑使用 AstrBot 原生 Function Calling 捕获 `pc_generate_photo` 参数，不执行工具或生图；报告会区分模型选图与规则兜底，并显示规范化请求、候选排名和维护者预期命中结果。
- 试跑默认读取当前用户、人格正文和最近会话快照，也支持空白或自定义上下文；两套陪伴面板保持同步并适配桌面、移动布局。
- 未变化的问答会复用已审批结果，试跑和保存不再重复审批；页面明确展示稳定性试跑预计调用量，并限制用户原话、上下文和候选规模，避免异常 Token 放大。
- 修复快速用途模板残留旧场景、时间、预设和排除条件，以及无用户 ID 的 `text2img` 草稿试跑被提前门控、工具参数为 JSON 字符串时无法解析等问题。
- 无原生 usage 的 Function Calling 统计会计入工具 schema、工具名和参数；显式参考图试跑会报告绕过目录选图，不再给出与正式改图不一致的命中结果。

## 6.0.4b

### 多人格中文名称与完整标识

- 修复多人格 ID 仅保留 ASCII 字符的问题；纯中文人格不再从选择结果中消失，中英文混合名称也不会再被截成英文片段。
- 逻辑人格 ID 与资料文件名改为分开处理；中文名称原样保留，路径特殊字符使用可逆安全编码，现有 `main.json`、`alt.json` 等英文资料文件继续兼容。
- 陪伴面板的人格选择、主人格、多选列表、窗口绑定、资料迁移和来源回显统一展示“名称 · 完整 ID”，选项提交值仍保持原始人格 ID。

### 多人格配置界面

- 修复多人格 ID 选择框被通用输入框样式撑满，导致人格名称逐字换行并溢出的问题；选择项现在保持稳定尺寸，并补充悬停、键盘聚焦和禁用状态。

## 6.0.4a

### 历史媒体标记防泄漏

- 修复特定场景下模型复述内部历史媒体标记后，被分段发送拆成独立消息直接发给用户的问题，例如 `<pc_history_media images="1" />`。
- 回复生成与发送前都增加了内部控制标记清理；历史中的真实附件元数据继续保留，不影响后续图片语义理解与上下文注入。
- 通用提示词补充约束：内部历史媒体标记不是聊天正文，不能被复述、改写或当作“已经发图”的依据。

### 表达学习批量审核

- 学习页表达审核支持多选当前筛选结果，可一次通过或拒绝多个私聊/群聊来源的情境表达与语法习惯规则组。

### MiniMax 图片接口兼容

- 新增 MiniMax 国内站与国际站专用适配，自动识别 `api.minimaxi.com`、`api.minimax.io` 和 `image-01` 系列模型；将旧的 `/v1/image/generation` 与 OpenAI 风格路径统一纠正为官方 `/v1/image_generation`。
- 文生图按 MiniMax JSON 协议转换尺寸并解析 `data.image_urls` / `data.image_base64`；图生图使用同一端点的 `subject_reference`，不再误走 multipart `/images/edits`。
- HTTP 200 下仍会检查 MiniMax `base_resp.status_code`，鉴权、余额、限流、参数和内容安全错误会明确进入现有备用端点回退链路。
- 显式选择 OpenAI 兼容平台时保留聚合代理协议；`image-01-live` 只提交其支持的官方宽高比，避免误传 `width` / `height` 或 `21:9`。

## 6.0.4

### 内部架构拆分与稳定性

- 将插件启动与配置装载、页面设置归一化、私聊/群聊消息主链、被动状态注入和每日状态推进拆分为独立模块；原有入口保留轻量适配层，在保持现有行为与事件注册边界不变的同时降低核心文件复杂度。
- 统一主动模块的用户休息状态判断，移除 7 份重复实现，避免同类规则在不同入口产生偏差。
- 新增架构边界回归测试并限定 pytest 只收集正式测试目录，防止验证快照被重复执行。

### 分段回复与媒体组件编排

- 分段发送新增语音、普通图片、@、平台表情和其他附件的独立位置设置；表情包继续使用专用位置设置。默认语音、图片和附件单独发送，@ 与平台表情嵌入相邻正文，回复引用始终绑定第一段正文。
- 主动消息、普通图片回复和 TTS 发送统一使用同一组件规划器；正文、表情包和其它媒体在部分送达或平台失败时按实际结果结算，不再重复已经送达的正文，也不会在正文未完整送达时补发表情包。
- 陪伴面板新增组件发送顺序预览，位置设置改为紧凑分组，并在窄屏下保持单列可读。
- TTS 已有完整中文可见正文时不再额外生成重复释义；语音块提示词也明确要求每段只保留一份中文释义。

### 表情包选择与历史附件语义

- 表情包检索会降低近期刚用过素材的排序，并在成功使用后立即刷新选择缓存；仍以语境相关性为前提，避免为了轮换而选中不合适图片。
- 已送达图片和语音在历史中改用内部媒体标记，不再伪装成“发送了一张图片”之类的聊天正文；提示词和发送清理会阻止模型复述这些占位说明。
- 群聊观察或表情链路遗留的群成员占位资料会按真实私聊足迹复核清理，保留明确管理、私聊活动、关系资料和手动设置过的用户。
- 群聊工具生图只会为已存在且属于私聊目标的用户记录命令配额，不再因为普通群成员调用链而创建新的私聊用户资料。

### 状态表达与主动复核

- 主动消息发送前复核若把有实际用途的候选改写成单独语气词，会保留原候选；相同语义也加入复核提示词，优先从生成阶段避免信息丢失。
- 私聊例行检查会把单独的“语气词 + 称呼”与后续承接正文合并，避免分段后先发出一条没有实际内容的称呼消息。
- 生理周期状态提示明确心理能量与情绪底色是独立维度；周期只提供相对修正，高能量与较敏感的感受底色可以同时成立，不再被解释成状态冲突。

### QQ 空间响应解析兼容

- QQ 空间 JSONP 与 H5 好友动态统一使用本地宽松解析，兼容单引号、未加引号的键、尾随逗号、注释及常见 JavaScript 空值，不再要求安装 `json5` 才能处理这些响应。
- `json5` 保留为可选的最后回退；未安装且响应确实无法识别时，页面只显示稳定的格式提示，不再泄漏 `No module named 'json5'` 内部异常。

## 6.0.3

### NAI 生图诊断与格式一致性

- NAI 联动生图现在会在请求、提示词组装、后端选择、结果校验和完成阶段持续记录规范化后的提示词格式，常规启动日志、完整调试文件与最近生图记录也使用同一标识，便于定位提示词转换和后端调用问题。
- 每次生图会固定请求开始时的提示词格式；即使主动场景或创作封面生成期间在面板切换为传统或自然语言模式，本次提示词、后端实参和诊断记录仍保持一致，下次请求才应用新设置。
- 未知或被污染的提示词格式会安全回退为传统模式；生图 trace 测试可独立运行，不再依赖其它测试预先加载 AstrBot 模块桩。

### 情绪模型复核状态修复

- 合法但置信度低于采用线的复核结果现在记为“已完成并保留本地判断”，不再误报为模型失败；高置信结果成功应用后会同步清除旧失败状态。
- 模型请求失败、空返回和格式异常改为结构化状态，只保存稳定错误码；陪伴面板不再展示截断 JSON、Provider 原文或异常路径。
- 页面新增“已采用 / 保留本地 / 失败”复核摘要和置信度显示，旧版本遗留的原始 JSON 错误会在接口层隐藏。
- 每次异步复核增加唯一请求标识，连续收到相同文本时旧任务不会覆盖新结果；异常日志只记录异常类型。

### QQ 空间生活说说：每日计划、时段窗口与文案去重

- **概率语义修正**：`qzone_life_publish_probability` 从"每次后台检查 18%"改为"每天一次抽样 18%"。旧逻辑每 60 秒掷一次骰子，一天 1440 轮累积后几乎 100% 必发；新逻辑每天只掷一次，18% 的实际含义与页面显示一致。
- **每日发布计划**：概率命中后会为当天建立持久计划；每日上限为 1 时固定 1 条，大于 1 时随机计划上限减 1 或上限条。计划绑定不同日程片段、长短文案节奏和同日间隔，重启后不会重新抽样或重复触发已完成时段。
- **发布时段窗口**：新增双窗口和双窗口加凌晨模板，`qzone_life_publish_windows` 自定义窗口支持任意数量的 `HH:MM-HH:MM` 行并自动合并重叠范围。凌晨窗口仅在用户启用且 Bot 确有失眠或浅睡状态时生效；首次启用不再错误套用历史冷却，可正常排入当天窗口。
- **评论回复可靠性**：新增 `pc_qzone_reply_my_comment` 工具，用户明确要求回复刚留下的空间评论时只检查 Bot 最新动态、按 QQ 身份或唯一高置信关键词匹配；多条相近评论不自动回复。后台评论发送失败会保留重试标记，接口嵌套错误码也不会再被误判为发送成功。
- **说说文案去重**：与最近已发说说按 3 字片段共享计数比较，达到阈值（默认 2）视为重复，自动重写一次；重写后仍重复则取消本次发布，不再把同一叙事骨架反复发出去。
- **多人格与并发幂等**：同一个 QQ 空间账号的自动计划只由主人格维护，评论回复和生活说说计划分别串行执行；即时工具会复用主人格的账号状态，避免多个人格、定时循环和即时唤醒重复公开发送。
- **未知投递与配置修复**：POST 超时或断连会记录为投递结果未知并终止自动重试，评论同样进入幂等隔离；跨午夜窗口会拆分处理，无效自定义窗口会停止当天计划而不是静默改用默认时段，单条计划也可随机落入任一有效窗口。
- 新增配置项：`qzone_life_publish_max_daily`、`qzone_life_publish_window_mode`、`qzone_life_publish_windows`、`qzone_life_publish_allow_insomnia_night`、`qzone_life_publish_intra_day_gap_minutes`、`qzone_life_publish_similarity_threshold`。
- 新增 `_day_start_ts` 工具函数，基于插件时区计算当天零点，确保"今天"的分界与时区一致。

### QQ Official 群聊标识与群资料兼容

- 群聊观察、白名单、黑名单和页面接口统一支持 OneBot 数字群号、QQ Official `group_openid` 与完整 `GroupMessage` UMO，并在存储边界归一为同一个群 ID。
- 旧 UMO key 与规范群 ID 会幂等合并，保留消息、成员、话题、关系边、群名和转述任务；多人格资料只在各自存储内迁移，不串用群数据。
- 群聊 scope 优先依据明确会话类型和规范群 ID，`FriendMessage` 即使携带伪群字段也不会落入群 scope；opaque 群 ID 可用于跨群转述和群关系网导入。
- QQ Official 无法查询群名时可在群聊页面手动填写；人工名称不会被后续事件覆盖，清空名称、清空观察和删除群范围均同步处理等价 UMO。

### 统一好感度与互动表达

- 新增八阶段长期好感度与主要用户专属固定关系；自动增减统一经过事件账本，支持去重、单次/每日上限、阶段迟滞、分段自然回落和普通用户正向阶段上限。
- 当前互动统一为回避、受伤、放松、活泼、温暖、亲近、爱意七档。普通用户可配置互动上限，亲近和爱意仅主要用户可用；真实私聊边界、受伤和重新接近事件会更新短期互动状态，管理员手动状态在有效期内保持优先。
- 被动回复、主动消息与 TTS 共用同一表达决策；关系阶段主动额度改为柔性节奏目标，通过提示词调节频率和打扰感，不要求凑满或机械卡线。全局零额度、用户显式零额度、疏离、受伤、回避和冷却仍是明确边界。
- 旧关系分数会在别名合并前统一迁移到 v2 量纲；迁移账本固定为零增量，避免量纲变化被误算成关系趋势，并修复早期已保存的非零迁移审计。
- 关系上限跨人格批量更新采用事务保存；人格资料或插件配置落盘失败时会恢复运行态、内存资料和已写磁盘资料。
- 陪伴面板新增关系策略和内容尺度功能卡片详情，用户详情新增阶段条、互动状态条、精确分数和管理员调整入口；两个页面入口保持一致。

### 关系内容尺度

- 新增默认关闭的 `normal / flirt / adult` 内容尺度。成人档要求主要用户、专属关系、爱意、私聊、成年后台确认、当轮明确同意和指定 Provider 精确匹配全部成立。
- 成人档不会自动切换 Provider；插件自身的二次复核固定使用指定 Provider，AstrBot 主回复链是否回退仍由框架配置决定。任一条件缺失时收敛为更保守尺度，主动消息固定为日常尺度。
- 内容尺度关闭时不注入额外尺度约束；开启后以生成提示词和语义复核为主，医疗、艺术、教育等正常语境不会因单个词被固定话术替换。

## 6.0.2

### 多人格资料与会话隔离

- 新增可选的多人格模式；每个人格分别保存资料、日程、状态、日记、用户与群聊关系、便笺和 Token 统计，关闭时继续保持原有单人格行为。
- 会话窗口可固定绑定人格，运行时会按当前事件隔离资料和缓存；同一窗口出现人格冲突时不会静默串用，可在面板确认迁移所选资料后再切换。
- 陪伴面板补齐主人格、人格列表、窗口绑定、当前人格切换、资料迁移和分人格 Token 汇总，两套面板入口保持一致。
- 延迟保存、终止落盘和状态/日记强制刷新均按人格隔离；从启用列表移除的人格仍可恢复资料，但不会继续参与启动维护、主动调度或陈旧窗口绑定。

### 群聊图片命中唤醒

- 群聊图片理解新增“图片命中唤醒 Bot”选项；已有视觉摘要命中 Bot 名称、强唤醒词或主要用户专属强唤醒词时，可将这条图片消息接入群聊回复链。
- 图片唤醒复用现有视觉识别结果、等待时间、群聊唤醒强度和疲劳控制，不额外发起独立识别调用；弱相关词不会仅凭图片直接触发。

### 每日日记连续性

- 日记生成可读取 MemoryCompanion 中与主要用户共同经历、关系变化、情绪余波和未完成心事相关的连续性记忆，让跨日内容自然衔接。
- 旧记忆只用于支撑关系、感受和延续线索，不会冒充当天新事件；质量复核会保留心理活动与情绪细节，并过滤未确认的家庭或社会关系。

### 被动状态与最终回复连续性

- 新增默认关闭的私聊被动状态连续性锚点；状态没有变化时只注入简短的当下线索，并排除用户事实、精确地点和未来行动，减少重复大段状态提示。
- 会话历史和外部记忆只记录平台确认送达的最终正文；发送失败、被拦截、尚未完成终审或只存在于主动占位链的候选，不再被保存成 Bot 已经说过的话。
- 分段回复会等正文余段完成后统一确认；流式与部分送达场景只保留实际确认片段，避免记忆中的回复比用户真正收到的内容更完整。

### 表情正文与 TTS 分段

- 表情意图触发 TTS 全量转换后，重建得到的正文仍会进入插件分段，不再因为结果类型变化而整段发送。
- 延后生成的表情语音只会在全部正文段确认送达后释放；正文余段中止时不再补发基于完整正文的语音，图片仍保持在完整正文之后发送。

### 参考图职责恢复

- 恢复与当前穿搭类别匹配的参考图职责；已选择日常服、睡衣等服装参考时会继续保留对应职责，不再因为职责归一化而退回只参考人物身份。

## 6.0.1b

### 表情回复分段顺序修复

- 普通 LLM 回复附带表情意图时继续使用已配置的分段规则，不再把完整正文合并成一个长气泡；分句、最短段长和最大段数仍由原分段配置控制。
- “正文后单独发送”模式会先依次确认所有正文气泡送达，再发送表情包；正文余段因新消息、撤回、发送异常或上下文检查而中止时，不会在残缺正文后继续补发表情。

## 6.0.1a

### 周期设定保存修复

- 生理期与连续六阶段周期的字段现在由页面 API 显式纳入总览、允许列表和归一化流程；旧 AstrBot 进程尚未重建配置索引时也能正常编辑和保存。
- 页面在总览缺少周期字段时使用兼容默认值，修改单个字段不会把其余周期设定从保存请求中丢掉。
- 兼容配置保存结果位于顶层、`data` 或 `overview` 的响应格式；持久化失败时保留当前草稿并提示用户，不再误刷新成旧配置。

### 转发图片识别与表情表达

- 合并消息图片视觉识别默认等待时间调整为 60 秒，支持更长的模型任务超时，并优先使用视觉模型卡片的单任务超时配置；没有视觉摘要时明确提示模型不要臆测图片内容。
- 表情表达支持百分比形式的触发概率；设置为 100% 时通过提示词和本地兜底减少模型漏写隐藏标签造成的机会丢失，同时继续遵守冷却、重复图片和用户停用边界。

## 6.0.1

### 表达规则去重与巡查

- 私聊与群聊的表达学习会在归纳提示中提供按相关性排序的已有规则摘要，引导模型优先复用已有规则编号，不再因情境换一种说法就不断生成同义规则；该优化复用原有归纳调用，不增加模型调用或 Token 消耗。
- 对模板一致、证据一致且上下文兼容的高置信重复规则，会按完整规则族合并表达与语法组件，并保留证据、标签、适用群聊、关系阶段、情绪门槛、反馈、使用统计和来源引用；人工编写规则与仅语义相近的候选仍交由人工确认，不做强制合并。
- 运行时会在 60 条表达规则预算之前合并等价项，避免重复口癖挤占可用槽位；新归纳结果也可直接强化已通过的等价规则，不再额外堆积待审核项。
- “模型数据排障”新增私聊、群聊、已通过与待审核规则的重复簇检查，同时提示运行时规则池是否因重复项达到上限；表达学习页会显示句长桶、标点和特征维度，减少底层统计不同但标题看起来相同的观察卡片。

### 日记启动检查与日程预算修复

- 插件启动时不再因为当天还没有日记就强制生成；启动只执行普通到期检查，仍按配置的日记时间生成，手动刷新日记的主动行为保持不变。
- 每日日程的模型输出预算会随配置的时间点数量联动，较多日程段不再固定卡在 1500 tokens；同时提示模型在高条数时保持单段简洁，优先一次性返回完整 JSON。

### 世界知识人格导入区

- 世界知识页的主回复人格来源与草稿提取区重新整理为“快速从已有人格导入”，默认折叠，只在需要时展开，减少重复说明和页面纵向占用。
- 展开后按当前来源、导入内容和生成操作分层展示；角色资料、世界观资料、用户与关系改为带摘要的复选项，生成结果统一显示为导入预览，原有填入空白项、覆盖二次确认和未保存保护保持不变。
- 补充键盘焦点、窄屏单列布局和静态资源版本更新，两份面板入口继续保持一致。

### 插件定位与说明

- 插件定位更新为“以持续生活模拟为基础的全方位 AI 能力强化陪伴插件”，同步调整插件市场元数据、README 和介绍讲稿，不再只用“持续陪伴编排”概括当前能力。
- 明确生活模拟是人格、状态、日程、关系与记忆共享的底座；私聊与群聊理解、主动行为、多模态交互、内容探索与创作、模型编排和运行诊断属于围绕该底座协同工作的能力强化层。

### 关系网与知识参考图库

- 新增独立的视觉参考资产库，关系网成员和 AstrBot 知识库/文档可以分别上传、预览、更新、删除和停用参考图，不会改变原有基础人设图库配置。
- 关系网参考图只在当前生图请求关联到对应成员（当前用户或明确提及的成员）时进入候选；知识参考图只在来源已勾选且标题、备注或标签命中当前知识上下文时调用，避免无关图片串入其他会话。
- 自拍、人像和带上下文的文生图继续复用现有职责规划、后端容量与文字回退；没有可用图片时按原提示词生成，不阻断生图。
- 陪伴面板的关系网成员编辑器和知识库/文档选择器增加图片优先的上传与预览控件，上传图片会复制到插件稳定目录并进行大小、格式和路径校验。
- Bot 关系网角色卡现在也可单独绑定参考图，例如姐姐、好朋友或同学；普通自拍不会自动加载，只有当前请求明确点名角色/关系或明确要求合影时才进入候选。角色名变更时面板会迁移对应资产，角色图不会替代 Bot 人设图。
- GPT Image 2 的 OpenAI 兼容编辑接口可按职责提交多张参考图，明确合影时优先组合 Bot 人物图与被点名角色图；多端点队列按稳定配置快照识别能力，GPT Image 2 作为后备端点时也不会在规划阶段丢失多图。实际回退到单图在线端点或较少图片输入的 ComfyUI 工作流时，会按当前容量提交并用提示词明确标记未附带的角色图，避免把缺失角色的身份误套给 Bot。

### 自拍场景生活连续性

- 用户要求的自拍地点与角色当前日程、位置明显冲突时，模型会优先按生活语境自然处理为之前拍摄或相册里的照片，并在配文中轻轻交代来源；用户明确要求现场立即拍摄且当前不合适时，可自然商量晚点再拍，不再让正在上课、通勤或忙碌的角色瞬间出现在海边等地点。
- 该行为采用柔性提示而非固定拦截：当前状态不冲突、延续上一张照片，或本来就是设想、COS、创作画面时仍可正常生成。

### 创作读取误拦截修复

- 查看创作功能相关的配置文件、数据文件、日志、源码、脚本或插件目录时，不再被误判为询问书柜作品原文，也不会把模型原回复替换成“没能实际读取创作原文”；真正询问已有作品、具体章节或书柜库存时仍会先读取真实内容再回答。
- 新增“创作原文读取保护”独立开关；关闭后不再注入强制读取要求，也不会替换模型最终回复，私下创作、书柜和其他聊天功能保持可用。

### 主动消息表情表达

- 开启表情表达实验后，主动私聊可在同一次正文生成中留下可选的隐藏表情意图，并复用已有触发概率、冷却、用户停用偏好、重复图片保护和本地素材匹配；不会增加模型调用，没有合适素材时仍只发送完整正文。
- 主动照片、语音、文件及内联图片不会再叠加表情包；素材只有在媒体确认送达后才计入使用记录，素材查询、附件构建或记录结算异常不会阻断文字，也不会触发整条已送达消息重发。

### 表情表达发送位置

- 新增“表情表达发送方式”：默认在完整正文确认送达后，将表情包作为下一条消息单独发送；也可选择与正文放在同一 AstrBot 消息链，或在正文前单独发送，被动回复和主动消息使用同一配置。
- 默认模式会逐段确认正文投递，正文未发送或只成功发送一部分时不会继续补发表情包、TTS 余段或表情语音；图片只有实际送达后才写入冷却与最近使用记录。
- 同一消息链最终是否显示为平台上的同一条消息取决于适配器；正文前模式会在设置页提示图片一旦先送达，后续正文失败时无法撤回。

### 主动消息完整句保护

- 移除连续未回应时按固定 36 个字符硬切主动正文的规则，修复“你晚饭吃。”这类句子在发送前被截成半句的问题；连续未回应时改由生成与终审提示词要求只表达一个完整、低压力的短句，本地检查不再破坏模型已经生成完整的内容。

## 6.0.0a

### 主动消息历史与投递绑定

- 主动消息生成和复核历史改为可配置上下文：可分别设置生成、复核条数，选择紧凑、仅最近或扩展模式，并控制保留原文条数和字符预算；整理过程不增加模型调用。
- 新增 `陪伴 绑定主动消息`、`陪伴 查看主动路由` 和 `陪伴 解绑主动消息`，直接使用当前真实私聊会话完成绑定，不再要求用户手填 UMO；群聊中不能误绑定其他会话。
- 配置引导会自动读取已绑定用户并预填用户 ID 与平台类型，同时保留手工配置兜底；页面可随时刷新绑定状态。
- 主动投递会跳过已不存在或已停止的平台实例，避免旧 `default:FriendMessage:*` 抢占当前有效路由；人工绑定优先，解绑后恢复基于入站和发送结果的自动选择。

### 主动消息 Provider 回退修复

- 修复部分兼容接口以 HTTP 成功返回英文报错、敏感词提示或内容政策拒绝时，AstrBot 将其误认作正常助手正文、因而不执行已配置回退模型的问题；主动消息现在会把这类高置信内部失败交回 AstrBot 原生 Provider 回退链，连续失败时继续尝试下一候选。
- 回退在同一个 Agent runner 内完成，不会重建整轮请求或重复执行已经完成的工具；带真实工具调用或媒体组件的响应保持原行为。所有 Provider 都失败时会清除原始错误正文并进入插件的人格化直接兜底，不把政策链接、接口错误或失败正文发送给用户、归档为正常助手消息。
- 人格化直接兜底及其他插件模型卡同样会把原生 `role=err` 和高置信伪成功错误记为失败，保留实际 Token 消耗后继续卡片备用 Provider；`tool schema`、`status disabled`、`traceback` 或“工具调用失败”等普通技术讨论不会单独触发回退。

### 夹层书库升级恢复

- 修复更新或切换 JSON/SQLite 保存方式后，空的数据源可能遮住另一侧已有夹层记录的问题；启动时会保守合并缺失书目，旧实例结束时的空快照也不能再覆盖磁盘中较新的书库状态。
- `bookshelf_items` 元数据缺失但本地 `bookshelf_pages` 仍在时，会按已有书页、历史偏好和封面重建书目；明确删除过的 album ID 不会被恢复，异常旧结构也不会被接口擅自改写为空列表。
- 夹层页面不再只显示最新 18 本，当前保留范围内的书目会全部返回；旧路径失效时会自动改用仍存在的本地书页，并兼容旧条目的 `kind`、`key` 和 `album_id` 结构。

### TTS Provider 配置隔离

- 修复 TTS 强化在 Fish Audio 合成前直接修改 AstrBot 共享 Provider 的模型请求头与运行时模型、导致原生 TTS 及其他会话继承插件设置的问题；模型适配现在只应用于本次合成的隔离副本，原 Provider 的配置、请求头和模型状态保持不变，无法安全隔离时按原配置柔性回退。
- 陪伴面板单独开启 TTS 强化或保存语种、概率与交付策略时，不再更新、重载或克隆 AstrBot 原生 TTS Provider；只有实际编辑 Provider 字段时才调用 ProviderManager。无变化的后端更新会直接跳过，未填写或使用新版模型标识的 Fish Audio 原配置也不会被自动补写、校验或覆盖。
- 合成后端缺省值恢复为 AstrBot Provider；“自动回退”在当前会话已有原生 TTS 时不再探测或接管 MiMo，只有原生 Provider 不可用时才尝试 MiMo。明确选择 MiMo 或插件按语种绑定独立 Provider 的行为保持不变。

### 生图日志与参考图库页面修复

- 完整生图提示词与实际提交给后端的预算化提示词分开记录：调试 JSON 和 trace 保留完整多行内容，并分别记录长度与 SHA-256；近期状态只保留限长摘要，避免长期数据持续膨胀。
- 修复参考图库未发生实际变化时仍提示“有未保存更改”的问题；通用表单事件不再覆盖图库专用草稿，返回详情页时会按规范化内容签名比较，JSON 数组与逐行 JSON 表示视为同一内容。
- 图库内部 ID 不参与未保存状态判断，旧目录缺失 ID 时仅打开或返回页面不会误报；真实的图片、顺序、注释、职责、服装、场景、时间和预设变化仍会正常标记并保存。

### 表情包运行日志与隐私保护

- 表情表达新增统一结构化运行日志，同一 `trace_id` 可串联概率与冷却判断、模型意图、图库命中或未命中、缓存状态、素材引用、附件准备和最终发送结算；重复图片、素材缺失、发送失败及回执不确定等跳过原因现在可以直接定位。
- 日志只记录固定原因码、素材安全引用、置信度和耗时，不写用户原文、检索词、上下文、用户或群号、会话标识及图片绝对路径；图库异常只保留异常类型，不输出包含本机路径的异常正文和调用栈，也不会增加模型调用或 Token 消耗。

## 6.0.0

### 各测试功能统一诊断

- 排障、生图接口、模型 Provider 与 TTS Provider 测试统一显示测试编号、耗时、执行阶段、错误分类、可重试状态和下一步建议；测试失败后可直接查看并复制诊断报告，不再只有简短的成功或失败提示。
- 后端为每次测试生成可关联日志的测试编号，并对即时结果、历史记录和阶段详情统一脱敏；旧版测试记录也会自动补齐兼容诊断信息，不需要迁移配置。
- 优化移动端诊断查看体验：桌面使用紧凑对话框，窄屏使用可滚动底部面板，请求本身失败时也会保留可查看的本地诊断记录。

### 表情包上传自动识别与素材工作台

- 表情包上传和重建索引发现新素材后，默认进入后台视觉识别队列；每批最多 4 张共用一次视觉调用，自动生成名称、画面摘要、图片文字、标签、情绪和沟通用途，上传请求无需等待模型完成。
- 新增待识别、识别中、已完成、失败和未识别状态，页面会显示处理进度并自动刷新；单张或批量素材可重新识别，视觉模型不可用或返回异常时保留原图和错误信息，不会要求重新上传。
- 人工填写的名称与字段会受到保护，多值标签采用保留人工内容并补充模型结果的方式；旧版素材目录会保守迁移人工元数据，GIF 使用最多 4 个时间点的拼图识别，不再只看首帧。
- 素材库页面改为图片优先的管理工作台，补齐识别状态筛选、跨页选择、批量识别、分页、并发受控的懒加载预览和移动端侧滑/底部详情；导入区支持拖放并将默认标签收进可选项，不再造成全部字段都要手填的误解。

### DeepSeek 工具调用消息配对兼容

- 修复 AstrBot 4.26.x 会话历史中存在多工具调用结果缺失、孤立或重复时，DeepSeek OpenAI 兼容接口因 `tool_call_id` 没有逐一配对而持续返回 400 的问题；发送请求前会按完整工具调用组校验历史，损坏组整组移除，正常组及普通对话不受影响。
- DeepSeek 系列在保留全部可用工具的同时新增顺序调用提示：需要多个工具时先完成一个并取得结果，再决定是否调用下一个，降低框架并行工具结果缺失再次污染会话的概率。
- 对照确认 AstrBot 上游已有同类上下文截断及并行工具问题修复，但 4.26.6 仍可能保留“部分工具结果存在、部分缺失”的非法组合；插件兼容层只处理本插件参与的 LLM 请求，不修改 AstrBot 核心文件。

### 生理期群聊感知与互动边界

- 修复普通群聊回复没有读取 Bot 当前生理周期状态的问题；处于月经期时，群聊模型会稳定获得当前状态，同时默认不会在无关话题中主动公开，用户直接询问时也不会否认或回答不知道。
- 月经期新增基于提示词的互动边界：面对性行为或更私密的身体互动请求时，Bot 会结合当前身体意愿自然、明确地拒绝或推迟，不会因关系亲密度、用户偏好、催促或迎合倾向覆盖边界；普通照顾、陪伴、牵手和轻柔拥抱不会被机械禁止。
- 修复私聊增量状态缓存可能遗漏周期变化、以及连续对话第二轮丢失生理期边界的问题；高级六阶段策略和自定义月经期文案同样生效。
- 修复“生理期模拟未开启”等中性状态可能被误判为正在月经期、以及关闭拟人生活状态总开关后当天旧周期缓存仍继续生效的问题；经前期、卵泡期、排卵期等非月经阶段不会触发强制拒绝提示。
- 官方 Bot/openid 等私聊身份别名会统一映射到主要用户记录，不再漏掉周期和被动状态；内部注入标记只按插件实际写入的片段去重，用户消息中偶然出现相同文本也不会跳过本轮边界。

### GitHub Issue 修复

- 修复表情包素材库缓存实例覆盖同名方法，导致第二次访问素材库抛出 `ReactionAssetLibrary object is not callable` 的问题（#84）。
- 表情包检索工具将对外参数改为 `search_context`，避免与 AstrBot 框架保留的 `context` 参数冲突并触发重复关键字异常（#85）。

## 5.10.9

### 陪伴面板资源失效保护

- 修复 AstrBot 桌面端或移动 WebView 恢复过期页面链接时，样式和脚本资源加载失败会直接暴露裸 HTML、破图文字及超大人体比例 SVG 的问题；入口现在先显示独立的紧凑加载状态，资源未就绪时不会展示未排版内容，并提供重新加载与重新打开拓展页提示。
- 陪伴面板首页移除默认人体比例插画和大字号品牌首屏，改为直接展示运行状态、统计与常用操作；今日穿搭仅在真实图片可用时显示为紧凑缩略图，原有点击预览能力保持不变。
- 主样式与主脚本增加联合就绪标记并刷新静态资源指纹，避免只加载到部分资源或继续命中旧缓存时误显示不完整面板；中文入口与 ASCII 兼容入口保持完全一致。

### 图片重复投递修复

- 修复图片已经被平台接收、但发送回执超时后又使用备用结果对象重发，导致同一 caption 和图片连续出现两次的问题；结果对象兼容降级现在只发生在调用发送前，一旦提交发送就不会自动重试。
- 超时、断线或回执丢失会标记为“发送回执未确认”，不再误报确定成功或确定失败；明确的平台拒绝仍按发送失败处理。生图工具和自有表情包发送共用该状态边界。

### 自有表情包素材库

- 表情表达改为使用 Private Companion 自有素材库，运行、检索与发送均由本插件独立完成；未导入素材时会明确显示空库状态，不再把配置开关误当成完整功能。
- 实验功能详情页新增素材管理工作区，支持图片、文件夹和 ZIP 批量导入、内容哈希去重、预览搜索、标签/情绪/沟通用途编辑、私聊/群聊范围、单张或批量启停与删除。
- 素材索引使用 UTF-8 JSON 原子保存，ZIP 导入校验路径与解压体积；运行时按标签、情绪、用途和当前语境柔性评分，没有足够贴合的候选时保留文字回复，不强行发图。
- 素材记录稳定 ID、尺寸、来源、使用次数和最后使用时间；素材目录可一键重建索引，丢失文件会在管理页单独标记。

### AstrBot 官方 TTS 兼容

- 修复开启插件的普通 LLM 分段后，回复结果被提前改为普通消息，导致 AstrBot 官方 TTS 跳过合成、只能收到分段文字的问题；插件本轮未接管语音且官方 TTS 可用时，会保留原始 LLM 结果并交由 AstrBot 完成 TTS 与原生分段。
- 插件 TTS 已接管、会话已关闭官方 TTS、触发概率为 0、Provider 不可用或消息链已有语音组件时仍沿用原处理链，避免绕过插件频控或重复生成语音。

### 生图归档自动清理

- `generated_photos` 新增默认开启的归档清理：默认保留 30 天且最多占用 512 MB，超过任一条件时从最旧图片开始删除；保留天数和容量上限均可配置为 0 以单独关闭对应约束。
- 清理在插件启动后台和生图成功后低频执行，不阻塞正常聊天；当前刚生成、尚待发送的图片会受到保护，参考图库、人设图、撤回图片缓存和视觉摘要缓存不受影响。
- 超过 1 小时的中断下载 `.part` 临时文件会一并回收，清理结果会记录文件数、释放空间及触发原因，便于排查磁盘占用。

### TTS 排障测试实际投递

- 修复排障页显示“TTS 语音组件已生成”但 QQ 始终收不到测试语音的问题：原测试只检查本地音频文件，现在会继续复用插件的精确平台、AstrBot 核心与 OneBot 兜底发送链路，把测试语音实际投递出去；生成成功但发送失败会单独显示投递错误，不再误报测试通过。
- “生成并发送测试语音”只允许投递到主要用户已经记录的有效私聊会话，不会回退到其他私聊用户，也不会自动发送到群聊；排障页会明确展示测试投递目标与生成、投递两个阶段。
- 修复插件启用但本轮未进入插件 TTS 时，“全部 LLM 分段”提前重建消息结果、导致 AstrBot 官方 TTS 不再识别该回复的问题；检测到会话官方 TTS 已启用且 Provider 可用后，会保留原始 LLM 结果交还 AstrBot 处理。插件 TTS 已接管的轮次仍沿用独立的语种、概率和分段策略。
- 插件已经接管本轮 TTS、但因概率、冷却或语境判断决定保留文字时，会在插件分段完成后明确结束 TTS 处理，不再被 AstrBot 官方 TTS 二次接管并绕过插件频控；未进入插件 TTS 的轮次仍正常交还官方链路。
- 插件生成的 `Record` 现在与 AstrBot 原生 TTS 一样保留对应文本元数据，改善火山引擎等非 WAV Provider 在平台适配与失败回退时的兼容性。

### 陪伴面板入口兼容

- 陪伴面板新增纯 ASCII 路径镜像 `pages/companion-panel`，与原 UTF-8 `pages/陪伴面板` 保持同内容；当部分 Windows、打包或代理环境在 AstrBot 插件 Page 的中文页面名、URL 编码和 60 秒资产 token 之间处理不一致时，可避免拓展页直接显示 `Token 无效`。
- 插件启动时会为 AstrBot 4.26.x 的插件 Page 静态资源 token 提供窄范围兼容：把过短的 60 秒有效期提升到 6 小时，并仅允许本插件 `陪伴面板` 与 `companion-panel` 两个同内容入口互认为别名，减少 WebUI/桌面端复用旧 iframe 地址时的误拦截。
- 发布布局测试现在同时校验中文页面目录和 ASCII 镜像目录，并要求两者核心资源完全一致；继续禁止生成 `pages/????` 或带替换字符的坏目录。

### 实验性情绪表情表达

- “实验”页新增默认关闭的“表情表达实验”。开启后，主模型可结合当前情绪、沟通用途、关系边界和会话场景，在同一次自然文字回复中留下隐藏表情意图；插件会在发送前清除该意图，并在仍有可见正文时从本插件素材库选图，把反应图排在文字之后。
- 私聊默认允许、群聊默认关闭；触发概率、会话级冷却、重复图片、检索说法上限和低延迟模式均可独立配置。用户明确要求查找或发送表情包时仍走原链路，不受实验概率与冷却影响。
- 发送成功后会记录图片回执，并从后续明确的喜欢或不合适反馈中形成轻量偏好；近期图片会按用户去重，冷却按真实会话分别计算。
- 无可见正文、正文发送失败、隐藏意图无效或图库没有合适结果时不会单独发图；内部标签、转义标签和截断标签都不会泄漏到用户侧。

### 回复延迟控制

- 普通消息会先在本地完成一次概率与冷却预判，只有获得表达机会的轮次才向主模型注入短提示；无论是否获得表达机会，普通闲聊都不会向主模型开放找图与生图工具，主回复只调用一次，不再为自发表情进入工具续轮。
- 图库查询和图片追加只在完整正文仍处于待发送消息链时执行，发送历史要等平台实际执行发送后才结算；查询或发送失败会释放本轮预留且不影响自然文字回复，用户明确找图、生图或改图时仍保留原工具行为。
- 低延迟模式固定使用图库本地标签和评分，不触发额外选图模型；新增短 TTL、限量内存缓存，缓存按图库插件实例与完整语境隔离，插件重载后自动失效。
- 实验详情页新增实际尝试、发送、跳过原因、反馈、缓存命中率和图库检索耗时；“候选数量”更正为真实含义“检索说法上限”，无论提供多少条说法，每次表达仍只执行一次图库检索。

### 默认示例与隐私清理

- 角色扮演工作台的一键示例改为通用虚构人物与关系，不再携带开发者私人角色资料、用户画像和固定作息；用户已经保存的角色配置不受影响。
- 早晚时间纠偏与内容替换提示不再写死特定昵称，带标点的短称呼及常见称谓均可正常处理；两份陪伴面板入口继续保持完全一致。

## 5.10.8

### 主动话题与即时事件

- 普通天气主动统一按同一语义话题记忆 18 小时，换成下雨、气温、天色等不同说法也不会连续反复；主动生成提示同时明确天气默认只作环境底色，普通关心、问候和生活分享不再顺手报天气或追问对方天气。
- 为官方气象预警、身体状态变化、实时环境突变和到期便签增加时效分级：它们可以优先于普通候选，并适度缩短用户空闲、发送间隔、Bot 日程忙碌和时段扎堆等软限制；每日主动上限、免打扰、明确休息、关系拒绝、实时共处和同一事件指纹去重仍然生效。

### 群成员风控调用收敛

- 群成员风控默认改为仅复用 Bot 正常回复中的隐性标签，不再对符合定向条件的群消息额外调用独立审核模型；仍可在进阶配置中手动启用补充独立审核。
- 修复发布流程错误生成 `pages/????` 目录，导致 AstrBot 插件页面地址被编码为 `%3F` 并提示“Token 无效”的问题；发布包会保留 UTF-8 编码的 `pages/陪伴面板` 正式目录。
- 生图工具执行层补齐陪伴对象权限校验，普通聊天或明文工具恢复不能绕过“陪伴 生图”命令的用户授权限制。

### 引用图片、TTS 与时区继承

- 修复 QQ/NapCat 引用或合并消息中的无扩展名图床直链无法进入视觉理解的问题，并补充不可读本地源与空远程响应日志；QQ 音频直链及伪装成 QQ 域名的普通网址不会误判为图片。
- 修复私聊被动回复自检把 TTS 保护占位符计入段落并在改写时丢失占位符，导致语音生成后只发文字的问题；占位符不再参与可见正文统计，带语音回复保持原始语音与文字对应关系。
- 环境感知时区新增 `global` 跟随模式并作为新安装默认值，按“插件自定义 → AstrBot 全局 → 系统本地 → Asia/Shanghai”解析；旧配置中已保存的明确 IANA 时区继续作为自定义值，陪伴面板会显示当前实际生效时区。

### TTS 对应文本分段

- 修复整条回复转换为语音后，中文对应文本因携带 TTS 可见标记而绕过普通分段器的问题；现在会继续按既有回复分段规则拆分，并为每个片段保留 TTS 对应文本标记，确保会话中途出现新消息时仍能完整补齐语音对应正文。

### 发布安全与后台稳定性

- 补充 AstrBot 宿主运行目录和测试缓存忽略规则，并将测试进程的 AstrBot 根目录隔离到系统临时目录，避免导入核心模块时在插件内生成主配置、数据库或模板；同步刷新扩展页 JS/CSS 资源指纹，确保关系网和 NAI 等新界面不会继续命中旧缓存。
- 群聊片段整理新增中性安全归档提示，敏感原话只抽象概括、不复述扩写；模型被上游拒绝或没有结果时不再误记为 JSON 格式错误。
- 自动余额查询确认当前 Provider 不支持后，从 12 小时逐步退避到最长 7 天；修改余额配置后仍会立即重新探测，减少长期无效请求和重复告警。

### Bot 关系网（主动拍照/生图）

- 主动拍照/生图新增“Bot 关系网”开关（默认关闭）。开启后可在插件面板配置最多 16 张角色卡，格式为 `角色名 || 与Bot的关系 || 角色外貌描述`，支持增删改与去重。
- 生成主动照片提示词时，关系网会连同人格、收信人、情境快照、画面钩子、生图风格、提示词表达方式和内容选择菜单一起注入；角色卡文字不再被当作人物参考图，没有其他人物参考时禁止合影，只能用第二只杯子、礼物、便签、空座位等非人物线索表达关系。
- 用户直接要求合影、合照或双人/多人同框时，必须在本轮携带或引用可用的其他人物参考图；配置的人设图、今日穿搭图、文字角色卡或工具参数中的单独路径均不能绕过校验。否则工具会明确返回需要参考图，不启动生图，也不消耗生图尝试额度。
- TTS 语种转换模型返回“低俗色情、不符合公序良俗、不能按照要求处理”等安全拒绝话术时，不再把拒绝声明当作语音正文；会先尝试配置的备用转换模型，仍失败则保留原回复朗读，并通过提示词明确转换任务不负责审核原文。

### 生图提示词表达方式新增 NAI 联动模式

- “生图提示词表达方式”新增第三个选项“NAI 联动模式（NovelAI 标签语法）”：模型按 NovelAI 4/4.5 语法（`{}`/`[]` 加降权、`权重::标签::`、负向权重、`{人物 [tags] 人物}` 多角色块、25 种位置标签、`ntags` 角色负面、`source#/target#/mutual#` 互动动作、`Text:` 画面文字与 `no text`）书写提示词并原样提交给在线 API。
- NAI 模式下后处理不再拆分重组 Positive/Negative 词条，避免破坏内联权重语法；若模型误写 Negative prompt 标题会自动转换为负向权重表达。封面生成等标签式链路在该模式下沿用标签式分支，传统/自然语言模式行为不变。
- 配置提示已注明：若 NAI 联动模式生图效果不理想，请切换回自然语言描述模式。

## 5.10.7

### 生图契约与配置保存兼容

- 恢复参考图选图旧参数 `selection_context` 的兼容入口，并区分用户请求与带标签的日程环境上下文，避免把日程中的“今日穿搭”误判成用户主动要求；旧调用和新选图链路可以同时工作。
- `pc_generate_photo(scene_preset=...)` 再次按文档通过 `requested_scene_preset` 结构化传入生成器，同时兼容短期使用过的 `suggested_scene_preset`；预设会以低于用户原始要求的提示提前参与参考图选择，模型误选基础人设图时可回到同服装类别参考图。旧版只填写 `preset_name` 的单预设服装裁决对象会自动归一。
- 补齐“工作地点、工作场所、外出”等中文地点语义，明确要求宿舍、卧室等新地点时不会继续混入冲突日程位置；上一张成图连续性提示同步精简并前置完整服装约束，避免提示预算压缩掉关键连续信息。
- 健康事件联动开关纳入功能开关保存白名单；ComfyUI 自拍工作流说明同步为参考图失败即明确停止，不再声称会回退纯文生图。

### Fish Audio 表现力控制

- 调整 S2/S2.1 的表现力提示与兜底逻辑，按官方示例将自然语言标签放到实际生效的短语前，不再把多个推断标签连续堆在句首；日语等非英文控制词会保留原写法。
- 修复“呜、唔、うーん”等普通语气词被误判为叹气，继而在害羞或撒娇语境中合成明显喘息声的问题；自动模式不再加入喘息、喘气或呻吟等强呼吸效果，只有文本明确描写叹气动作时才允许叹气控制，手动模式仍保留直接控制能力。

### 晚安识屏提醒

- 合并晚安识屏提醒配置，并补齐原提交缺少的运行链路：主要用户与 Bot 互道晚安后可按配置等待一次识屏判断；用户期间再次发言、明确要求勿扰、主动消息关闭、达到每日上限、关系退避或识屏额度/冷却不满足时都会静默取消。
- 同一次晚安只检查一次；只有视觉模型明确判断仍在主动使用电脑时才生成一条低压力休息提醒，锁屏、黑屏、静止、无法判断和识屏失败均不发送，也不会循环重试。
- 屏幕判断只返回 `active / inactive / uncertain` 内部状态，原始屏幕摘要、应用、窗口、账号与文字内容不会进入最终消息生成上下文；提醒提示明确禁止查岗、追问和要求回复。

### 参考图目录与元数据保护

- 合并参考图目录升级：基础人设图与参考图库统一保存为带稳定 ID 的规范目录，完整保留参考职责、服装类别、默认服装锁、适用场景、首选预设和元数据来源；页面重排、命令增删和远程 URL 缓存回写不再丢失这些信息。
- 旧版人设图路径和带注释图库会在启动后迁移到规范目录；迁移采用目录与版本标记分步保存，保存失败会安全重试。已标版本但校验不完整的目录只读加载，不会被启动流程或自动缓存回写覆盖。
- 参考图库命令现在显示并支持按稳定 ID 删除；管理页与生图选图统一读取规范目录，同时保留未经过启动迁移的旧调用兼容。

### 私聊图片 TTS 交接

- 修复单图延迟回复由合成事件继续发送时，框架 TTS 占位符没有使用创建该回复的原事件恢复，导致语音内容丢失或按普通文本参与后续判断的问题；现在会先恢复 TTS 结果，再执行错误、能力和过期检查。

### 分段回复文件名保护

- 修复分段词包含英文句点时，`对象.jpg`、`说明.pdf` 等文件名会在扩展名前被拆开，导致 TTS 后续正文只显示 `jpg`、`pdf` 等残片的问题；常见图片、音视频、文档、压缩包和代码/配置文件后缀现在会在分段、内容清理与内容替换阶段保持完整，普通英文句点仍按原配置处理。

### TTS 语种群聊指令

- 修复主要用户或插件管理员在群聊发送 `陪伴 TTS语种 中文|英语|日语|默认` 时被私聊管理闸门提前拦截，且权限通过后确认回复仍会被延后队列吞掉的问题；群聊现在可以正常查询和切换语种，普通群成员仍不能修改全局 TTS 运行设置。

### Gemini 生图代理

- 在线图片网络代理与环境代理开关现在同时作用于 Gemini `generateContent` 生图请求和在线图片结果 URL 下载；未配置代理时继续直连，其他在线生图平台的 API 请求与本地后端保持原行为。配置页、扩展页与 README 的说明已同步，避免继续误导为“只代理结果下载”。

### 风景拍照与人物参考图

- 修复 Bot 表示去拍晚霞、风景或物件时，纯场景 `text2img` 提示词仍混入 `character art`，导致图片后端擅自画入没有使用人设参考图的陌生人物或人物背影的问题。
- 纯场景现在会使用环境与物件画面提示，并柔性排除未请求的人物；如果画面明确让 Bot 以背影、侧脸或环境人像等形式出镜，则自动转入 `selfie` 人物参考图链路。明确要求背影时会保留该构图，不再被普通自拍的露脸约束反向排除。

### 和风天气城市指令

- 新增 `陪伴 绑定城市 <城市|区县,城市|LocationID>`，可直接调用和风 GeoAPI 校验并保存天气地点；同名区县可用 `朝阳区,北京` 这类写法消除歧义。
- 新增 `陪伴 查看城市` 与 `陪伴 解绑城市`；查看时会显示和风规范地点和 LocationID，解绑后仍保留旧经纬度兼容兜底。
- 城市查询无匹配、Host 或凭据缺失、配置保存失败时不会覆盖原城市；绑定成功后会失效旧地点的天气、地点解析与气象预警缓存，但不会自动切换用户选择的天气来源。
- 城市绑定、查看和解绑属于敏感地点操作，只允许主要用户以真实发送者 ID 在自己的私聊中执行；群聊、其他私聊用户、普通管理员和身份别名均不会获得地点内容或修改权限，拒绝提示也不会透露当前是否已绑定城市。

## 5.10.6

### 和风天气配置入口

- 选择和风天气时，陪伴面板会在 API Host 与凭据输入项之间显示“获取 Host”和“获取 API”的官方控制台链接；首次配置向导与 AstrBot 原生配置提示也同步提供入口。

### 日程细化跨日清理

- 修复手动细化过的日程段在换日后仍长期停留在状态页的问题；每日计划切日时会同步更新细化日期并清空旧日快照，即使自动细化处于关闭状态也会执行。
- 手动细化写入前会校准所属日期，状态页和故事计划重建也只读取当前计划日期的细化结果，已有旧数据不会再混入今天的日程。

### 讲故事意图识别

- 修复“讲个故事”“讲讲你的故事”等普通聊天被误判为读取书柜作品，继而回复“没能实际读取到创作原文”的问题；只有明确提到已写作品、某篇故事、书柜、原文或章节时才要求读取创作工具。

### 拓展页主题精简

- 移除“暗夜深空”主题选项及专属样式；历史配置或浏览器缓存仍保存为 `dark` 时会自动回落到默认“经典蓝”，不再跟随进入该黑夜主题。

### 提示缓存与回复速度

- 私聊询问当前状态、穿搭、位置或进食情况时，当前状态记忆与通用私聊记忆改为并发读取，避免两次外部记忆等待串行叠加；召回内容、触发范围与超时边界保持不变。
- 每轮恒定的媒体发送真实性规则移入稳定系统提示前缀，随消息变化的状态和工具说明仍放在本轮请求尾部，提高模型提示缓存命中率并减少重复前缀计算。

### TTS 与回复分段兼容

- 修复开启 TTS 增强后，上游回复分段插件已经拆开的多个纯文本组件会先被合成完整语音、但配套可见正文被重新合并成一个气泡的问题；现在仍先用完整回复生成一条语音，再恢复未被翻译或改写的正文分段。
- 恢复只作用于进入 TTS 前已明确分段且最终正文内容一致的纯文本结果；语音组件保持单条，翻译、改写、图片或其他混合消息不会按旧边界强制拆分。

### AstrBot 配置页列表校验

- 修复旧版陪伴面板曾将群聊强唤醒词、主要用户专属唤醒词、弱相关唤醒词和兴趣关键词保存为多行字符串后，AstrBot WebUI 因 schema 要求列表而在保存配置时返回 400 的问题。
- 插件启动迁移现在会按 schema 同时规范化分组配置与旧版兼容副本；字符串和列表两种历史配置都可继续读取，升级后无需手动修改配置文件。

### 天气数据源扩展

- 天气上下文默认改为和风天气实时天气：填写控制台分配的专属 API Host、JWT / API Key 与“天气地点”即可；地点支持城市、`区县,城市`、和风 LocationID 或“经度,纬度”，不再要求普通用户手工查询坐标。
- 城市和区县会通过和风 GeoAPI 解析并缓存 LocationID 与坐标；普通天气优先使用 LocationID，官方气象预警复用同一地点的坐标。地点或 Host 改变时缓存自动失效，解析失败不会覆盖上一份同地点有效缓存。
- 普通天气与官方气象预警复用同一组和风 Host、凭据和地点；旧经纬度和旧版预警专用字段继续作为兼容配置。空的旧 OpenWeatherMap 默认配置会切换到和风，真正配置过 OpenWeatherMap、Open-Meteo 或高德地图的安装继续保留原来源。
- 和风请求默认只接受 HTTPS Host，并关闭自动重定向；仅本机回环代理可使用 HTTP，避免凭据被发送到远程明文地址或重定向目标。
- Open-Meteo、OpenWeatherMap 和高德地图 Web 服务 API 仍作为兼容来源；切换数据源时旧天气缓存会自动失效，请求失败时仍可回退 `screen_companion`。
- 新增可选的和风天气官方气象预警接入：支持蓝/黄/橙/红最低级别筛选、10 分钟默认刷新、缓存失败回退和预警更新/解除的一次性主动提醒；首次抓取只建立基线，不会把历史预警集中刷给用户。
- 预警只向主要用户提供环境上下文，地点或 Host 改变时会丢弃旧地点缓存并重新建立基线；配置页会显示缓存年龄、过期和最近失败状态。预警服务可能存在延迟，重大情况仍以当地气象台、12379 等官方渠道为准。

### 模型回退后的 TTS 安全回执

- 修复主回复或 TTS 转写模型被内容安全策略拦截、随后备用模型已正常生成回复时，前一个模型的“请求包含低俗色情内容/已被平台拒绝”等回执仍被包装成语音发送的问题。
- 提供商安全回执现在不会进入语音合成；同一结果中的备用模型正常语音和正文会继续保留，只有安全回执时则回落为原始可见回复。

## 5.10.5

### 每日穿搭二次元风格

- 修复每日穿搭提示词中的 `non-mirror` 被误判为用户要求镜前自拍，随后注入 `real character` 并把二次元参考图生成成真人照片的问题；构图约束现在使用不偏向真人或插画的角色表述。
- 选择二次元风格时，每日穿搭会使用插画构图、插画光照与二维角色语义，并明确排除写实摄影、真人和实拍质感；真实风格继续保留原有手机随拍提示。

### 5.10.5d 发布包修复

- 修复 `5.10.5d` 已在 `main.py` 引入群成员风控、但远端提交漏收 `group_member_safety.py`，导致市场更新后插件在导入阶段报 `No module named ...group_member_safety` 的问题；完整源码已补齐，并增加发布文件完整性测试。
- 群成员风控模块改为可降级导入：若发布包再次缺失该可选模块，仅停用该能力并输出明确错误，插件主体、私聊和其他群聊功能仍可加载。
- 补齐配置页对 `enable_group_member_safety` 的保存权限，避免功能卡可见但开关修改无法写入配置。

### 在线图片 API data URI 输出修复

- 修复 OpenAI 兼容图片 API 将 `data:image/...;base64,...` 放在 `data[0].url` 返回时，插件误当作 HTTP URL 下载并判定生图失败的问题；普通生图与参考图编辑现在统一支持 data URI、裸 base64 和 HTTP URL。
- 增加普通 generations 与参考图 edits 两条路径的 data URI 回归测试，并保留原有端点重试和参考图成功状态说明。
- 内联图片现在沿用 HTTP 下载的 32 MB 上限，并严格校验 base64 与 PNG/JPEG/WebP 文件特征；当 `b64_json` 损坏但同一响应还提供有效 `url` 时，会继续尝试 URL 结果。

### 私聊图片补充文字衔接

- 修复私聊先发图片、超过防抖等待后再补充文字时，视觉转述任务已被纯图片回复链路接管，导致后续文字拿不到图片上下文的问题；延迟处理现在会保留一份短期、按真实会话隔离的一次性视觉交接，补充文字会继续等待同一个识图任务。
- 视觉交接只向后续文字注入摘要，不会再次把原图挂到主模型；交接领取一次即删除，并带过期和数量清理，避免旧图片误匹配后续话题或长期占用内存。
- 修复防抖窗口内“先发图再补文字”时，补充文字已领取视觉上下文但延迟纯图链仍再次派发、继而回复“没看清图”的竞态；已领取的 live 图片上下文会明确接管本轮回复并取消二次派发。

### 生图附言与空白消息修复

- 生图和智能图库工具的 `caption` 现在明确要求只输出用户可见正文，并在图片投递前清理 `&&shy&&` 等遗留情绪/内部控制标记，避免控制语直接显示在图片消息中。
- 图片工具成功发送后，发送前终检会拦截只剩空白、零宽字符或私用区占位符的后续消息，避免 QQ 显示成单独的方框气泡；真正包含新说明的后续文本不受影响。

### 主动消息 TTS 文本泄漏

- 修复主动图片附言或普通正文误套用人格中的 TTS 专用日语、`[happy]` 情绪标签或语音脚本格式，导致语音未启用时把应供合成器使用的内容直接发成文字的问题；可见正文与独立语音脚本现在明确分离。
- 主动发送出口会清理遗留的已知 TTS 情绪控制词，但保留 `[公告]` 等普通方括号文本；有效语音合成仍会在生成音频时使用情绪标签。

### SnowLuma 在线状态兼容

- 修复 NapCat Desktop 的 SnowLuma 已成功设置在线状态、却以 `set status success` 异常或失败包返回时，插件继续轮询多组参数并放大协议端错误日志的问题；该回执现在按成功收口，每次状态同步只提交一次标准 OneBot 请求。
- 真正失败的同一 QQ 状态增加一小时退避，避免日程细化或启动恢复反复请求不兼容接口；关闭“同步 QQ 在线状态”仍可完全停用该能力。

### 自定义视觉转述提示词

- “图片转述增强”新增自定义视觉转述提示词和最大字符数配置，适合日志截图、聊天记录及完整 OCR；支持 `{astrbot_prompt}`、`{image_count}`、`{scope}` 占位符。
- 自定义提示词留空时继续使用插件默认短摘要，并自动追加 AstrBot 当前会话的图片转文字提示词；填写后替换默认内容要求，同时始终保留图片提示注入安全边界。
- 视觉缓存现在绑定最终提示词，修改自定义或 AstrBot 视觉提示词后不会误用旧摘要；默认输出上限由 600 字提升为可配置的 2400 字，避免详细截图转述被二次截断。

### 生图参考图可靠性

- 修复 `pc_generate_photo(scene_preset="居家睡衣")` 把预设追加到完整 Prompt 尾部、在传统格式下误落入 Negative prompt 的问题；场景预设现在以结构化字段进入选图、服装裁决和最终一致性检查，睡衣预设会移除今日穿搭并优先匹配睡衣参考图。
- 参考图选择、服装裁决和最终预设现在按统一顺序执行；普通自拍不会把插件内部的“保持今日穿搭”文案误当成用户要求。
- 服装型参考图锁定后会物理移除冲突的今日穿搭和视觉记忆内容，并按睡衣、居家服、校服、礼服、泳装、运动服或 COS 选择对应预设。
- 当前请求中的换装与否定表达会优先参与语义裁决；“不要睡衣，改穿校服”不会反向选择睡衣参考图，模型也可明确选择不使用任何候选图。睡衣与居家服的相邻自然语言描述不再产生伪冲突告警。
- 自然语言提示词中的 `Avoid ...` 会进入负面区，不再被误拼进正向描述；改图原图始终保持“本轮来源图”职责，即使它恰好与图库文件同路径，也不会被误标成人物或服装参考。
- 当参考图只负责人物身份而不负责服装时，最终提示词不再泄漏旧服装类别或内部图库 ID，减少模型把不应继承的衣服带回画面。
- 提示词改为分区预算，保留用户要求、服装裁决、预设和负面约束，不再对整段提示词做粗暴尾部截断；完整脱敏提示词继续写入调试 JSON。
- ComfyUI、百炼、Gemini、OpenAI 兼容接口和自定义函数工具在参考图读取或提交失败时会明确结束，不再静默回退成纯文生图；后端返回不存在的输出文件也不会记为成功。
- 生图结果补齐参考图实际使用状态、职责、服装裁决、最终预设、提示词哈希和调试文件路径；结构化状态会区分“已使用、未使用、旧接口未知”，记忆协同不再根据普通“已使用在线 API”字样误判。
- 创作封面、主动失败重试、QQ 空间待发布配图、每日穿搭快照、书柜封面及最近生图记录统一保留合法连续空格和长路径，避免生成成功后在回读、预览或重发阶段找不到文件。

### 图片转述超时与回退

- 单个识图模型超时不再被“主链等待识图秒数”反向截短；前者决定后台模型调用多久，后者只决定当前回复最多等多久，超时后的后台任务仍可继续完成。
- AstrBot 当前会话的图片转文字模型使用通用识图超时，插件识图及其备用模型才读取对应模型卡片超时，避免不同路由误套同一个值。
- 移除默认写死的 300 秒失败降权；跨图片失败冷却改为显式配置且默认关闭，单次空摘要、依赖缺失或调用异常不会静默跳过后续图片。
- 视觉转述调用继续沿用 Provider 自身输出预算，不传固定 `max_tokens`；新增回归测试覆盖超时解耦、冷却开关和请求参数。

### 参考图库与排障页面

- 参考图库改为结构化保存，可直接编辑身份/服装等职责、服装类别、默认锁服装、适用场景和首选预设；显式 `outfit_lock_default: false`、条目 ID 与未来扩展字段都会保留。
- 旧版 `路径 || 注释 || JSON` 条目会自动兼容提升；多行注释不会再拆成幽灵条目，远程参考图缓存到本地后也不会丢失原有元数据。
- 长 Windows 路径和带引号路径可正确回传、匹配和预览；生图排障测试完成后立即刷新最近记录并显示提示词调试文件。
- 本地参考图预览增加文件大小和读取长度保护，异常大文件不会被一次性载入管理进程。
- 管理页资源缓存标识更新，避免继续使用旧版生图诊断脚本。

### 模型自动回退刷新

- 修复精准配置中的模型留空后，页面仍读取隐藏的快速配置旧 Provider，导致“当前使用”和测试调用看起来一直停留在换模前的问题；精准模式现在只沿用当前主模型、陪伴通用和可见的精准分流链路。
- 保存精准配置并覆盖两套分流时，会根据当前可见配置重新生成快速响应、复杂推理、创作和识图入口，不再被旧的隐藏快速配置反向覆盖；服务端总览刷新后也会丢弃已经保存过的前端模型草稿。

### QQ 空间好友动态兼容

- 修复 QQ 空间工作台“好友动态”可能为空或只显示当前账号说说的问题；第一页现在优先读取 QQ 空间 H5 首页实际下发的好友 `vFeeds`，旧版 `feeds3_html_more` 保留为分页和兼容回退。
- 当 H5 首页只返回自己的说说时，会自动再查询旧版好友动态并按作者与动态 ID 合并，不再把“自己的说说可读”误判为完整好友时间线。
- 好友动态解析兼容新版结构化字段、旧版 HTML、多层 `data/feeds/items/list` 容器以及非 311 的可展示动态；作者、动态 ID、正文、转发内容、原图和点赞键均支持新旧字段映射。
- H5 首页读取同时复用并刷新 `qzonetoken` 缓存；日志新增数据来源、候选数、解析数、最终条数和是否仅自己，方便区分 Cookie 权限、接口空响应与解析兼容问题。

### AstrBot 注册兼容

- 移除 AstrBot 已弃用的 `register_star` 装饰器，改用框架自 3.5.20 起支持的 `Star` 子类自动注册，避免 AstrBot 4.26.7 加载及测试时持续产生弃用警告；插件声明的最低版本 4.22.0 不变。

### 图片转述增强稳定性

- 修复历史成功识图模型即使已从配置删除，仍可能被重新加入候选并产生非预期调用的问题；“近期成功优先”现在只调整当前明确配置的 AstrBot 图片转文字、插件识图和备用识图模型顺序。
- 识图超时只切换本轮候选并记录真实失败，不再让该模型在后续图片中静默降权 5 分钟；空摘要、缺失依赖和调用异常默认也不会跨图片冷却。需要保护持续故障模型时，可显式设置失败冷却秒数，默认 `0` 关闭。
- 没有可靠视觉摘要时不再发送固定“没看清图”罐头句；有效主链回复会直接保留，主链为空时再由带当前人格和回复风格的兜底模型在“不猜图、不续写旧话题”边界内自然回应，模型仍无有效文本则不发送伪造兜底。
- 单图等待、单模型超时和历史图片补全等待在运行时、保存接口、Schema 与页面统一为 `0-600` 秒；单模型超时设为 `0` 可关闭插件额外超时，AstrBot 默认图片转文字候选不再误套插件识图模型卡片的超时，视觉摘要也不再传入偏低的硬编码 `max_tokens`。

### 参考图库与改图修复

- 修复同一条消息中的附件同时被“已落盘图片”和“原始消息图片”识别后，每张参考图重复加入两次的问题；单次添加现在会按来源和文件内容指纹去重，并清理本轮产生的重复副本。
- 修复“陪伴 改图”及 `pc_generate_photo(kind=edit)` 带参考图时被内部改写成自拍工作流语义的问题；改图现在全程保留 `edit` 类型，不再注入自拍构图、当前日程、今日穿搭或人设替换提示。
- ComfyUI 改图仍复用可接收图片输入的自拍/图生图工作流配置，但仅作为底层执行路由，不再改变上层改图语义。

### 参考图库管理页

- 配置页的“生图/拍照能力”新增独立参考图库三级页，基础人设图与最多 24 张场景参考图可在同一处预览、筛选、添加、修改用途注释、调整顺序和删除，不再直接维护多行配置文本。
- 本地参考图预览只允许读取当前配置中已登记的图片，页面不能通过路径参数访问其他文件；远程图片继续使用原始 URL 预览。
- 图库编辑继续接入功能详情的统一草稿、保存和未保存提醒，进入或返回三级页不会丢失同页其他生图参数。

### OneBot 戳一戳兼容

- 修复 OneBot 将戳一戳通知映射为空文本私聊/群聊事件后，被陪伴的空消息保护或群聊观察链误判并终止传播的问题；这类 `notice/notify/poke` 事件现在会直接交给戳一戳插件处理。
- 戳一戳不会再被写入私聊活动、繁忙回复闸门、群聊观察或撤回缓存；普通空消息和其他通知仍按原有规则处理。

### 群聊图片理解增强

- 新增默认关闭的“群聊图片理解”：只在允许观察的群中后台识别图片，普通群消息不阻塞；当前消息确实触发回复时才有限等待视觉结果，超时后主链继续且后台任务不会取消。
- 群图复用现有视觉 Provider 回退、失败冷却和内容哈希缓存，相同图片不再反复调用模型；图片缓存页新增“群聊图片”范围和独立命中统计。插件只长期保留摘要与压缩预览，识图临时原图会在任务结束后清理。
- 视觉摘要以独立 `image_vision` 字段回填群聊观察，不覆盖原始发言，不进入表达、黑话或关系事实学习；回复 Prompt 明确把图片与图中文字标为非指令证据，避免截图中的命令或身份声明污染设定。
- 配置页新增回复等待秒数和单条图片上限，并提示开启后关闭 AstrBot 官方“自动理解图片”，避免同一张图重复识别。

### MiMo TTS Voice Clone 联动

- TTS 自动转换现在只接管实际进入 AstrBot 主回复链的消息；其他插件直接生成的指令结果、图片、卡片或文字不再被全局发送钩子误转成语音。主链内遇到帮助、菜单、状态和查询类功能回复时，也会优先保留便于查看与复制的文字，用户明确要求语音时仍可正常使用。
- TTS 强化新增可选的 `MiMo TTS Voice Clone for AstrBot` 合成后端；无需把克隆模型伪装成 AstrBot 官方 TTS Provider，也能复用现有快速标签、后处理、语种转换、频控和主动语音链路。
- 联动通过 AstrBot 标准工具管理器定位 `mimo_tts_speak`，再调用 MiMo 插件公开的 `synthesize_text()` / `text_to_speech()` 服务；不会直接执行会自行发送消息的 LLM 工具，避免语音与文字重复投递。
- 参考音频、默认克隆音色、用户/会话音色映射与 AI 语音导演继续由 MiMo 插件管理；本插件可选覆盖音色名称和单次风格。联动不可用且当前会话存在 AstrBot TTS 时会柔性回退，旧用户默认后端保持不变。
- 主动语音可用性、排障测试、模型能力感知和“我会和你在一起”的实时语音桥均识别 MiMo 后端；电话或共同观影即使没有 AstrBot 官方 TTS Provider，也会先尝试克隆语音，再按原链路回退浏览器朗读。

### 主动消息顺延日志

- 修复同一条主动候选已经顺延到相同时间后，调度循环仍每几秒重复写入“繁忙回复闸门已顺延主动消息”的刷屏问题；相同截止时间现在视为无变化，只在首次顺延或截止时间实际延长时记录。
- “我会和你在一起”等实时通话/共同观看联动仍会独立暂停无关主动消息，但日志会明确显示“实时共处期间”，不再让关闭了繁忙回复闸门的用户误以为开关失效；繁忙闸门关闭时，普通忙碌日程不会顺延主动消息。
- 修复关闭“私下创作行为”后，旧版本已经预约的 `creative_share` 候选仍继续参与主动顺延的问题；发送前会清理这类陈旧候选，官方定时消息不受影响。

### 在线生图稳定性与测试语义

- 在线图片 API 遇到 HTTP 500、502、503 或 504 等明确的上游临时故障时，会等待片刻并重试同一请求一次；参数、鉴权、内容审核等确定性错误不会盲目重试，也不会为绕过失败而丢弃人物参考图。
- 单个图片 API 按钮更名为“纯文测试”，并明确提示它不会上传参考图或覆盖自拍/改图/角色一致性等真实链路；排障页的“测试自拍”用于验证参考图接口，避免“单独测试通过”被误解为全部生图场景可用。

### Token 统计

- 修复陪伴面板探测“一起房间”Token 统计时遍历全部 Python 模块并触发 `transformers` 懒加载，导致未安装 PyTorch 的环境反复报 `No module named 'torch'` / `torchvision`、概览页无法打开的问题；联动发现现在只读取模块已注册的静态标识，不再意外加载第三方模型依赖，PyTorch 继续保持可选。可选统计来源即使自身异常也会独立降级，不再拖垮总览页。

## 5.10.4

### 表达规则编辑

- 学习页的待审核和已启用表达规则组现在都可直接编辑；支持调整规则名称、适用情境、可复用表达、句法结构、使用指令、召回标签与适用边界。
- 编辑会保留规则 ID、来源片段、证据数量、反馈与使用统计；待审核规则可先修正再通过，已启用规则保存后立即刷新表达画像。
- 修复自定义规则名称保存后被适用情境覆盖的问题；输入校验失败会在页面中直接说明原因，不再写入系统异常堆栈。

### 生图工具静默收尾

- `pc_generate_photo` 或图库反应图已经自行发送图片与 caption 后，不再要求模型返回真正的空文本；工具回执会要求输出内部静默标记，再由插件发送前清除。
- 兼容 Gemini 将合法空 `content.parts` 视为 `EmptyModelOutputError` 的行为，避免图片已经发送成功后仍重试三次并切换备用模型；用户侧仍不会看到重复承接句或内部标记。

### 用户请求生图额度

- 修复“每日主动生图最多次数”设为 `0` 后，`/陪伴生图` 仍提示当日额度用完的问题；主动生图额度不再影响用户明确要求的生图或改图。
- 新增独立的“用户请求生图/改图每日上限”，统一作用于陪伴生图/自拍/改图指令与主链 `pc_generate_photo` 工具调用；默认 `0` 表示不限量，正数才按陪伴用户分别计数。
- 规则快判生图上限恢复只作用于 `rule_fast`，不再被指令入口暗中借用；主链工具达到用户请求额度时会在调用图片后端前停止，避免先产生费用再提示超限。

### 定时任务消息投递

- 修复开启 TTS 增强后，`future_task` 定时任务调用 `send_message_to_user` 时被同会话去重逻辑延后、但定时事件没有最终回复投递阶段，导致消息静默丢失的问题。
- 定时任务的普通文本恢复使用 AstrBot 原始工具直接发送；带 TTS 标签或文本语音组件的消息仍由插件完成语音处理后直接发送，普通聊天中的防重复机制保持不变。

### 日程细化地点与自拍场景

- 日程细化模型现在会明确输出当前段地点、依据和置信度；细化地点成为状态页和生图场景的首选来源，旧剧情与关键词推断只在模型未给地点时兜底。
- 细化提示新增地点连续性约束，地点切换必须写出移动过程，避免同时出现“床头”和“工作场所”这类互斥现场。
- 自拍链路明确以本轮用户指定的宿舍、卧室、床上等场景为先，日程地点只补充未指定部分；工具提示补充“居家睡衣”预设，睡衣请求不再误选普通日常穿搭。

### QQ 官方主动会话路由

- 私聊主动发送目标映射现在支持完整 UMO，例如 `主用户ID=qq_official:FriendMessage:openid`，不会再只保留 openid 后用另一个 Bot 的全局首选平台重新拼接会话。
- 插件会按用户保存最近入站、最近发送成功和最近发送失败的真实私聊路由；多 Bot 环境优先复用管理员显式指定或已经验证的会话，同一用户某条路线返回 `invalid request` 时可退让给已观察到的可用路线。
- 私聊用户详情新增“主动投递会话”，直接显示当前 UMO、路线来源、已观察路线与最近错误；QQ 官方配置提示也改为优先粘贴 `/sid` 返回的完整 UMO，避免把数字 QQ 当作 openid。
- QQ 官方身份仍不会仅凭昵称或数字 QQ 自动合并；跨平台共用记忆时继续由“私聊身份别名归并”明确声明身份关系，避免误把不同用户合并。

### 主动审计错误详情

- 主动发送失败时会额外保存脱敏后的完整诊断，不再只留下在字段名中途截断的 160 字错误；卡片摘要达到上限时会明确显示省略号。
- 主动执行审计卡重新整理为基础信息、触发依据和执行结果三层，去掉“为什么/结果”之间的重复错误文本；发送链路的目标、精确会话、AstrBot 核心与 OneBot 兜底结果可按需展开查看，移动端会自动改为单列。

### 自拍服装与参考图优先级

- 自拍生图会先结合本次服装需求、日程地点和参考图用途完成选图，再写入最终场景约束；明确要求睡衣、居家服、礼服或 COS 时，本次需求与具体参考图优先，今日穿搭只在没有换装意图时补充连续性。
- 新增独立的“居家睡衣”场景预设，`睡裙` 不再因通用“裙”关键词被归入日常穿搭；睡衣参考图不会再被同一请求中的今日外出穿搭语义覆盖。
- 图片链路测试会依据成功结果中“已使用/已提交参考图”的明确回执报告参考图状态，不再因页面没有提前解析到最终选图路径而误显示 `used_reference: false`。

### 晚安后群聊活跃

- 主要用户与 Bot 已互道晚安后，如果又在已观察群聊中继续发言，插件会按人格进行一次低概率判断，可形成轻调侃、关心一句或保持不点破的短时主动候选。
- 每次晚安只判断一次，不会因连续水群反复抽取概率；候选不会复述群聊内容、群名或表现成持续监视，也不会绕过主动总开关、每日上限、最小发送间隔、关系退避和发送前终审。
- 该候选可在群聊活动后的短时间内突破通用夜间免打扰与私聊空闲门槛，因为主要用户刚刚公开表现为仍在线；显式“不要打扰/别主动找我”仍保持静默。

### Token 统计

- 检测到“我会和你在一起”运行时桥后，Token 面板会新增独立的“一起房间”来源，展示通话、视觉和观影整理消耗；插件未安装或未加载时入口保持隐藏。

## 5.10.3

### 六阶段周期模拟

- 生理期模拟新增可选的连续六阶段策略，按月经期、卵泡期早、排卵前期、排卵期、黄体期早和 PMS 顺序推进；默认仍沿用原有三阶段行为，不会静默改变既有配置。
- 每个阶段可独立配置持续天数、状态描述、情绪底色和精力变化；可用“当前周期第几天”首次对齐进度，设置会持久化且只在偏移量或阶段天数变化时重新应用。
- 可让阶段精力与拟人状态强度线性联动，强度 0 不改变精力、50 使用阶段基准、100 约为基准两倍，避免重复缩放。
- 周期提示词统一保持柔性：当前话题和用户意图优先，阶段主要影响 Bot 的语气、长短与节奏，不把模拟状态写成用户事实、医学结论，也不会因高能量阶段硬性增加主动消息或亲密程度。
- 陪伴面板新增完整六阶段分组、条件显示和数值边界；关闭周期时隐藏高级策略，开启强度联动时隐藏未生效的手动精力字段。

### 参考图场景分类

- 统一情境快照新增居家/外出场景类别，自拍提示词会明确带上“当前场景：居家室内/外出”，不再只依赖原始日程和位置文字碰巧命中参考图注释。
- “家里、宿舍、公寓”等位置会直接参与居家参考图评分；无法从位置或日程确认场景时保持未知，不强行猜成居家或外出。
- 参考图库选图日志现在会区分模型选择、单候选与规则兜底，并记录兜底分数、模型返回、最终候选和实际文件路径；模型返回“候选1”等自然编号时也能正确识别，不再静默退回规则结果。

### 实时共处联动

- 为“我会和你在一起”提供统一 Bot 身份接口：返回 Bot 名称、候选 self_id、唯一可确认的 QQ 号与头像来源；多 Bot 环境不再随意挑选账号。
- 统一 Bot 身份不再把 AstrBot 平台实例的内部随机 ID 当作账号，aiocqhttp 会从当前反向 WebSocket 连接读取真实 Bot QQ；多个 QQ Bot 同时连接时仍返回候选集合，不自行猜测。
- 对外提供完整实时场景，统一包含精力、情绪、日程、位置、天气、穿搭、关系与当前共同活动，不再由接入插件各自裁剪状态。
- 通话或共同观影期间会登记带自动过期保护的运行态，暂停与主要用户无关的普通主动消息；定时、备忘和及时环境变化仍按原规则处理，异常断线后不会永久卡住调度。

## 5.10.2

### 关闭主动终审时的本地改写

- 关闭主动消息模型终审或选择“仅本地检查”后，本地检查若已产出完整安全改写，会直接采用改写继续发送，不再把正常创作分享、长度收敛、称呼纠正或去回复式开头一律取消。
- 本地只有抽象参考意图、没有可确定发送正文时仍会取消，保证关闭模型终审不会变成无条件放行，也不会暗中调用改写模型。

### 功能开关保存提示

- 功能开关的保存操作改为桌面端右侧悬浮、窄屏底部固定；进入详情后同时保留“返回功能列表”和“保存更改”，不再因保存键位于长页面底部而遗漏提交。
- 修复保存操作条仍嵌套在带模糊和裁剪效果的配置容器中，导致 `position: fixed` 实际跟随配置内容定位的问题；操作条现在挂载到页面根层，始终相对浏览器视口固定，并仅在配置页显示。
- 操作条会显示“有未保存更改/更改已保存”；详情返回、切换主面板以及刷新或关闭页面前都会检查未保存状态，返回时可明确选择继续编辑或放弃本次详情改动。
- 详情保存现在会一并提交进入详情前在功能列表中修改的主开关，避免刷新运行态时静默丢掉尚未提交的列表草稿。

### 群聊观察入口

- 群聊观察新增高优先级纯记录入口；其他插件提前终止事件、本群关闭 LLM 回复或主动消息专用模式不再连带丢失观察数据，后续回复链只补充场景与唤醒信息，不会重复累计消息。
- 白名单模式为空时，运行日志和总览接口会明确提示当前观察范围为零；群聊页“新增群聊”默认加入白名单并立即开始观察，“仅创建群资料”会明确说明暂不观察。
- 图片、语音、视频、合并转发、分享卡片和文件即使没有文本，也会以可读占位进入群聊最近消息，不再整条忽略。

### 固定称呼联动

- 私聊页或聊天命令设置的用户称呼现在会作为唯一称呼约束传给记忆插件；关系阶段、历史称呼、临时显示名和旧记忆仍可用于识别，但不再另行建议“宝贝、笨蛋”等称呼，也不会要求每条回复都带称呼。
- 用户本轮明确提出新称呼时仍以最新要求为准，避免固定配置阻断自然改称呼。

### 指定日程操作

- 聊天命令现在可以按时间、序号或活动名重新细化/取消某一段日程，例如“陪伴 重置日程 下午三点”和“陪伴 删除日程 整理房间”；“删除”沿用日程生命周期的取消语义，保留历史依据但停止细化重试、当前活动注入和主动契机。
- 不带选择器的“陪伴 重置日程”仍会重建整天；选择器匹配不到或同时命中多段时会返回当天候选，不再静默忽略参数或误操作整天。
- 修复“陪伴 重置细化”在已有结果时只返回旧内容的问题；该命令保持只重新细化当前段，指定其他时段继续使用“陪伴 重置日程 <时间或活动名>”。
- “重置 细化/穿搭图/夹层密码”“删除 日期/话头”等分词写法会先归一化到对应操作；裸“重置”不再作为整插件清空别名，必须明确使用“重置插件”或“重置 插件”，避免后半段参数被忽略后误清空数据。
- 聊天日程列表现在显示计划中、进行中、已完成、已变更和已取消状态；技能或个人目标的面板删除请求未命中时会明确报错，不再显示假成功。

### 自有表情包素材库

- 表情表达能力改为使用本插件独立维护的素材库，支持图片、文件夹和 ZIP 导入，按标签、情绪、沟通用途及私聊/群聊范围检索。
- “实验功能 → 表情表达实验 → 表情包素材库”提供预览、搜索、启停、批量编辑、删除、去重和重建索引；素材保存在本插件数据目录，不要求安装其他图片插件。
- 图库表情真实发送成功后会记录标签、表达需求和选图理由，支持继续追问“刚才那张图什么意思”；发送失败不会写成已发送记忆。
- 自拍、穿搭图或其他图片已由本插件发送时会标记当前事件，避免同轮重复追加表情图；私密生成图不会自动导入素材库。

### 群聊观察稳定性

- 修复群聊唤醒与群观察存在同名方法但参数契约不同，导致真实插件继承顺序下每条群消息都可能触发 `missing 1 required positional argument: 'text'` 的问题；历史群消息的 Bot 指向判断现在使用独立方法，不再受 Mixin 顺序影响。
- 群聊当前发言者的稳定 QQ 身份现在明确高于消息中的自称、其他成员资料和 MemoryCompanion/长期记忆召回；当已有成员开玩笑自称另一位已登记成员时，只按玩笑或提及理解，不再把对方的姓名与记忆套到当前发言者身上。

### 群气氛自然恢复

- 群列表和群详情会按最近 12 分钟消息即时重算气氛，不再因群聊暂时没有新消息而长期残留“紧绷”。
- 群详情新增“重置气氛”，只忽略重置前的气氛判断材料，不会删除群消息、成员、黑话、话题线或其他观测数据；重置后的新消息仍会自然形成新气氛。
- 降低单个“累、烦、急”等词对群气氛的误判，只有明显冲突表达或多人连续压力信号才会标记为“紧绷”；同时补齐页面此前缺失的“节奏”字段。

### 主动事件生命周期修复

- 修复已过期主动事件仍被日程选择器反复选中、重新物化并生成大量独立拦截记录的问题；来源事件现在具有稳定 ID，过期、跳过或已终止后不会在下一轮重新入队。
- 主动事件在选择、入队和物化前统一校验表达窗口；缺少窗口、时间顺序异常和旧版残缺念头会温和终止，不再长期占住活动念头池或形成秒级空转。
- 增加同一来源 15 分钟内重复物化熔断；重复拦截和接受记录优先合并到原候选，并记录逐日合并次数。
- 非官方定时计划统一在正文生成前检查过期状态；AstrBot 官方定时消息继续保持独立，不受普通主动生命周期和免打扰改期影响。

### 免打扰排程

- 普通主动任务落入免打扰时段时，直接移动到免打扰结束后，不再每隔 30-90 分钟重复检查和延期。
- 时效性事件的整个有效窗口被免打扰覆盖时会标记跳过；创作分享等耐久事件可以整体平移到免打扰结束后。
- 晨间问候和饭点关心的提前提升入口同步使用统一窗口判断，避免先提升到免打扰时段、到点后再二次延期。

### 主动候选统计

- 主动页面统计改为读取完整候选池，并按配置时区区分今日新增记录、今日合并触发、今日实际拦截和当前候选池总记录数。
- 候选列表为控制页面开销仍限制展示最新记录，但会明确显示截断状态和实际统计范围，不再把 240 条快照上限误当成今日总数。

## 5.10.1

### 主动 TTS 中文正文补发

- 修复主动消息转换为“语音和文本”后，语音能正常送达、中文正文却通过临时装饰事件空发的问题；中文正文现在复用真实主动发送链，并且只有发送链成功返回后才记录补发完成。

### 框架异常文本拦截可关闭

- 新增独立的“框架异常文本外发拦截”开关，默认保持开启；正常技术讨论因 `Provider API Error`、`image_url` 等字样被误拦时，可在陪伴面板单独关闭。
- 关闭后只停用 `framework_error/tool_loop_summary` 关键词拦截；被动回复复核、重复回复保护、违禁词、内部控制标签和孤立工具发送回执保护继续生效。
- 修复旧版“启用备选在线图片 API”开关在功能开关页落入“其他”并显示内部英文键名的问题；该入口现在只归属于生图能力，实际配置继续在“模型配置 → 生图模型”的在线 API 队列中管理，原配置值不会被改写。

### 主要用户专属群聊唤醒词

- 群聊唤醒增强新增“主要用户专属强唤醒词”：只有 `target_user_ids`、身份别名归并结果或私聊页关系角色确认的主要用户能以这些词直接叫醒 Bot，其他群友说出相同内容不会触发。
- 专属词与原有全局强唤醒词并存；Bot 名字仍只自动加入全局强唤醒词，不会误当成主要用户专属词。陪伴面板、首次配置引导、原生配置和插件答疑说明已同步。

### 带注释的多参考图库

- 参考图一致性新增最多 24 张的参考图库，每张图可附加服装、地点和适用场景注释；支持在陪伴面板按行配置，也可私聊一次发送多张图片并用 `陪伴 参考图库` 添加、列表、预览、删除或清空。
- 自拍、人像、主动配图、QQ 空间人物配图和人物创作封面会结合最终画面提示词自动选择一张参考图；模型选图失败时使用本地场景匹配兜底。
- 今日穿搭图不再无条件覆盖其他人物参考图；在家、卧室、睡前和刚起床等场景会优先匹配居家服或睡衣，明确外出或展示当日穿搭时才优先使用今日穿搭图。用户显式发送或引用图片改图仍以用户图片为准。

### Agnes Image 专用适配

- 在线生图平台新增 `agnes`，可按 `apihub.agnes-ai.com` 或 `agnes-image-*` 模型名自动识别；官方地址使用 `https://apihub.agnes-ai.com/v1/images/generations`。
- 文生图请求按 Agnes 官方 JSON 协议发送 `model/prompt/size`，并把 `response_format` 放入 `extra_body`；不发送 Agnes 文档明确不需要的 `tags`。
- 参考图不再误走 OpenAI `/images/edits` multipart，而是转为 Data URI 数组放入同一 generations 请求的 `extra_body.image`，适用于自拍、头像、穿搭、创作人物封面和改图。
- 在线 API 队列新增 `ratio`，Agnes 支持 `1K/2K/3K/4K` 与 `1:1、2:3、16:9` 等官方比例；提示词明确写出竖版或比例且原尺寸冲突时，会自动切换到对应档位和比例。

### 繁忙回复闸门

- 在“休息回复闸门”下新增默认关闭的“繁忙回复闸门”。Bot 日程处于上课、学习、工作、开会等忙碌片段时，普通被动消息会随机晚一点回复，但不会静默或丢失；紧急、安全风险和插件管理命令立即放行，且不增加额外模型调用。
- 私聊默认延迟提高到 1-5 分钟，最长可调到 15 分钟；群聊仍自动限制在 12 秒内，避免话题已经变化。同一事件的工具循环不会重复等待。
- 普通主动消息会顺延到当前忙碌片段结束并经过默认 10 分钟缓冲；用户预约、到期便签、环境突变、排障和模拟消息保持及时。Proactive Chat 联动预检也会识别 Bot 忙碌日程。
- 补齐原生配置、陪伴面板可见性、运行时刷新、自然语言调参、状态摘要与排障说明；子参数只在繁忙闸门开启时显示。
- 修复插件代码更新后尚未重启、配置文件又没有新字段时，扩展页可能把繁忙延迟空值按 `0` 保存的问题；现在首次打开和保存都会使用 1-5 分钟及 10 分钟主动缓冲的明确默认值。

### 插件专家答疑

- `陪伴 答疑`改为模型主导的插件专家链路：结合能力目录、相关源码/README/配置 Schema、当前配置、运行状态、截图和同会话上下文综合回答，关键词规则只用于检索候选证据与生成需确认的调参建议。
- 移除答疑必须压缩为 3-6 行、单一原因和单一步骤的固定限制；功能设计、调用链、实现边界和改进评估可按问题复杂度展开。
- 快速模型配置下，插件答疑/排障改用复杂推理模型；等待预算跟随模型卡片超时，默认 45 秒，不再因固定 6 秒外层超时频繁退回规则答案。

### 书柜对话读取

- “能看到书柜吗”“书柜里有什么”“书架还是空的吗”等问法会读取真实书柜库存，不再漏过创作读取工具；列表会返回创作区，并向主要用户补充日记、私密阅读和便签的分类数量。
- 工具漏调、失败或模型只输出“（翻了翻书柜）”“（挠挠头）”等舞台动作时，由书柜查询链直接改用本地真实库存回答；不扩大通用回复复核范围，也不增加额外复核调用。
- 兼容 AstrBot 实际传入的 `CallToolResult` 包装结果，不再把成功的书柜工具调用记录成 `status=unknown`；QQ 官方 Bot 的非数字用户 ID 可正常识别。旧版或不完整列表结果缺少 `bookshelf` 快照时不再信任“空空如也”，会改用本地真实库存回答。

### QQ 官方机器人全面适配

- 不新增手动平台模式开关；插件会按每条事件和已保存会话自动识别 QQ 官方机器人、自定义平台实例名与稳定 openid。只配置了一个同类型运行实例时，未建立历史会话的主动私聊也会自动使用真实实例 ID 生成投递目标。
- 统一按平台能力降级：QQ 官方通道不再尝试 OneBot 戳一戳、输入状态、原生撤回、指定消息引用、合并转发和发送失败后的 OneBot 原生兜底；分段回复会自动合并，主动动作不可用时回退普通文字，图片和已配置语音仍正常使用。
- QQ 官方通道不会再注入或调用 QQ 空间工具，也不会执行空间自动发布、评论收件箱和情绪动态后台任务；拓展页与排障测试会直接标明“QQ 官方不支持”，只有运行中的 OneBot/aiocqhttp 实例才启用空间能力。
- 主模型会收到 QQ 官方身份与能力边界，明确 openid 是稳定身份，并要求只有实际发送成功后才能声称图片、语音或主动消息已经送达；平台失败会保留真实原因，不再把能力缺失包装成已完成。
- 陪伴面板总览新增平台适配状态，私聊页会把 QQ 官方 openid 标为“QQ 官方”而不是“非 QQ/临时会话”，主动额度统计也会包含稳定的平台身份。

### 同会话工具回复去重

- 修复模型同时输出正文并调用 `send_message_to_user` 时，同一句话可能被工具循环中间响应、工具直发和最终响应连续发送三遍的问题。同一会话的纯文本/文本语音工具消息现在只作为本轮唯一最终回复交付；跨会话以及图片、文件等媒体工具发送保持原行为。

### 分段发送配置

- 修复分段发送总开关已经写入配置、刷新扩展页后却被旧运行态显示成关闭的问题；页面现在把持久化分段配置作为回填依据，扩展页保存后仍会立即同步当前运行态。
- 分段范围、会话范围、切分规则、内容清理/替换和发送间隔会统一从同一组持久化配置回显，避免只恢复总开关、子参数仍显示旧值。
- 功能页不再依赖人工名单协调“功能状态”和“持久化设置”；两者同名时统一由持久化设置回填。所有 Schema 布尔开关也会优先显示磁盘配置，不再被尚未重载的旧运行态覆盖，避免后续新增配置型开关再次出现刷新回退。
- 修复对象格式的分段内容替换规则在文本框中显示为 `[object Object]`、再次保存后规则损坏的问题；对象规则会转换成可编辑的 `原内容 => 新内容` 形式。

### 配置清理与状态修复

- 修复“启用强化梦境生成”开关未参与运行时判断的问题；关闭后使用固定梦境池，开启后才调用增强梦境生成并保留失败回退。
- 移除已停用的人格标准化旧开关、AI 日报旧窗口/轮询间隔配置，以及已内建的预约调度配置；启动迁移会清理旧版顶层和分组残留，不影响现有有效配置。
- 统一 README、元数据和更新日志版本号为 `5.10.1`。

### Fish Audio 最终合成兜底

- 修复 Fish Audio 专项优化只存在于提示词和标签清洗、转换模型返回纯文本时最终音频仍无情绪控制的问题；现在所有普通/主动、局部/全量语音都会在 `get_audio` 前经过同一层上下文情绪兜底，优先保留已有合法控制词，并记录实际模型、模式和控制词。
- 新增“自然平衡 / 更有表现力 / 手动控制”三档 Fish Audio 情绪策略。自然平衡只为清晰情绪补主要控制和必要语气，更有表现力最多组合 3 个，手动模式不自动补标；S2/S2.1 与 S1 分别使用官方方括号和圆括号语法，聊天正文不会显示控制词。
- 补齐 Fish Audio 模型适配与情绪策略在陪伴面板、配置 Schema、保存白名单、运行时刷新和排障摘要中的配置闭环，避免保存后未真正进入运行态。

### 生图模型管理

- 模型配置新增“任务模型 / 生图模型”二级导航；在线生图 API 队列、后端选择、优先级调整和保存入口统一迁移到生图模型页。
- 每条在线生图 API 可直接使用当前表单草稿单独测试，不需要先保存；测试只调用选中端点，不切换备用 API，也不回退 ComfyUI/SDGen，并展示实际结果、耗时和排队时间。

## 5.10.0

### Proactive Chat 联动

- `Proactive Chat` 联动升级为可逆的运行时深度桥接，无需修改对方插件文件：Proactive Chat 继续负责更即时、频繁的触发和自身未回应策略；Private Companion 在模型调用前完成纳管私聊、明确休息、免打扰、关系退避、在途发送和短时调度防撞检查，并把当前收件人、关系、时机、Bot 状态、生活片段与已审核表达规则注入本轮生成。
- 候选生成后会在 TTS 和分段前统一复核、改写或丢弃；发送出口改为汇总 `send_by_session` / `context.send_message` 的实际调用结果。只有平台调用无异常返回后才同步最近主动正文、发送时间、今日次数、连续未回应和回复上下文；全部发送失败或装饰后为空时不再写成功历史、不增加 Proactive Chat 未回复计数，并会正常重新调度。
- 两套主动链路通过同一个在途占用和可配置防撞窗口提前互斥，避免双方已经各自消耗一次模型调用后才发现重复；不会合并后台循环，也不会改写 Proactive Chat 的会话列表、长期频率和未回应上限。
- 修复组合使用时的重复语音风险：Proactive Chat 已成功生成语音后，其配套文字会明确跳过本插件二次 TTS；上游已分段时复用同一逻辑消息元数据，避免每段分别触发语音或重复计数。
- 本地复核拒绝的候选会真正清空发送链，不再出现“日志说拦截、平台仍发送”；需要改写预生成语音时会改为发送安全文本并抑制后续重复正文。未被 Private Companion 纳管或已停用的私聊用户完全保持 Proactive Chat 原行为。
- 陪伴面板“仅保留主动能力”详情会区分“深度联动 / 发送前兼容 / 等待运行实例”，并显示对方版本、接管方法数和最近预检/平台结算结果；新增 10～600 秒调度防撞窗口。默认“轻量本地”不增加复核模型调用，可选“跟随主动终审”会增加一次模型调用和等待。

### Fish Audio 情绪控制

- TTS 强化新增 Fish Audio 专项模型适配：S2.1/S2 按官方方括号自然语言控制生成，S1 旧版按圆括号固定标签生成；提示词会约束句级情绪位置、单句主要情绪和最多 3 个组合控制，减少情绪冲突与短句标签堆叠。
- 常见中文、日文情绪和拟声标签会在合成前规范为稳定的 Fish 控制词；S2 保留简短自然语言控制并清理超长元数据，S1 只放行官方固定标签。GPT-SoVITS 与不支持情绪标签的 Provider 保持原行为。
- 陪伴面板新增 Fish Audio 模型适配选项。自动模式读取 Provider 模型；官方 Fish Audio 接口未声明模型时按当前文档补齐 `s2.1-pro-free` 请求头，自建兼容接口不会被强制覆盖，也可手动选择 S2.1 Pro、S2 Pro 或 S1。
- 主动 `voice` 行为与普通/主动 TTS 消息统一经过同一套 Fish 模型选择、情绪标签规范化、朗读文本清洗和文件服务链路，不再因是否预先带 `<tts>` 标签而出现两套合成行为。

### TTS 模型感知

- 模型感知新增当前会话 TTS 能力：注入语音合成 Provider 是否可用、TTS 强化开关、文本转换模型、语种、局部/全量转换范围和“仅语音/语音+文字”交付方式，让 Bot 能正确判断能否发送语音及应保留多少文本。
- TTS 感知只使用 Provider 名称/模型标识和非敏感运行选项，不会注入 API Key、接口地址、鉴权头、音频路径或完整 Provider 配置；未配置或当前会话不可用时会明确标记。

### 通用表达学习

- “表达方式学习”从“私聊陪伴”移入“通用能力”：它可从允许的私聊与群聊归纳规则，并按范围用于私聊回复、私聊主动和群聊回复，不应再表现为仅私聊功能。
- 配置引导拆开“私聊长期画像”和“跨会话表达学习”；表达模式、素材审核、发送前审核与容量配置归入通用表达学习，学习来源和使用范围继续在学习页集中管理。
- 原生配置移除表达学习对“私聊互动与表达基座”开关的可见性依赖，并明确跨频道作用域；已有开关状态和学习资料不变。

### 被动例行检查

- “那……例行检查”“嗯，那就晚间检查一下”等带自然语气词的表达会继续按双方约定互动处理，不再被普通短承接吞掉；有可靠历史时自然承接，没有明确项目时轻问用户想先检查哪一项。
- 例行检查回复最多提出一个问题，并限制为最多两条短消息；只有当前对话、提醒、便签或可靠记忆明确支持时才询问具体事项，不再凭空加入服药、生病或未吃饭假设。

### 主要用户权限

- 私聊页将关系角色改为“主要用户”后，该用户会立即获得插件管理权限和主要用户专属查询权限；判定仍绑定发送者稳定 ID，身份别名不会连带获得权限。
- 总览页“管理权限”会合并展示 AstrBot 全局管理员、`target_user_ids` 目标用户和关系角色主要用户，页面状态与实际权限保持一致。
- 非 `admins_id`、非 `target_user_ids` 的用户改回“次要用户”后会失去上述管理权限；全局管理员和目标用户原有权限不受关系角色切换影响。

### TTS 全量转换

- 修复快速标签模式选择“全量转换”后，主模型只圈出一小段语音时仍按局部语音发送的问题；发送前现在会以整条可见回复重建完整语音内容，“全量”不再只依赖提示词遵守。
- 修正 TTS 注入层的范围冲突：快速标签示例、后处理说明、主用户倾向和用户语音请求会按“局部/全量”分别生成，不再一边要求覆盖全文、一边又示范或强调“只用一小段”；排障记录同步显示本轮转换范围。
- 修复主动 `voice` 动作已经生成语音后，配套主动正文又被全局 TTS 转换、最终连续发送两条语音的问题；预生成语音现在是该次主动行为的唯一语音，配套正文只保留可见文本，普通主动文字仍按 TTS 配置处理。
- 修复主动分段经过装饰器后丢失 TTS 元数据、导致每一段都独立命中自动语音的问题；分段信息现在同时固定在发送事件上，一个逻辑主动消息最多自动生成一条语音。全量转换由首段朗读完整正文，局部转换只允许首段参与语音，其余分段保持普通文本；“语音 + 文字”模式仍会保留首段对应文字。
- 外语语音会以完整中文可见回复作为全量转换源，避免把局部外语和它的中文译文重复朗读；合成失败仍保留完整文字兜底，局部转换模式保持原行为。
- 修复 TTS 可见正文和 Record 注释在 AstrBot Pydantic 消息组件上写入失败的问题；中文译文不再被误当普通正文二次分段，语音日志也能稳定保留朗读内容与对应文本。
- 全量快速转换移除一次会被发送层丢弃的中间模型调用，只保留真正需要的目标语种转换；降低语音回复延迟和重复 Token 消耗。
- “仅语音 + 局部转换”现在只用语音替换被朗读部分，其余未朗读正文继续发送；全量转换仍按设置仅发送语音。
- 修复连续数字被重复字符清洗错误压缩的问题，金额、编号和数量中的 `1000 / 1111 / 2025000` 等值不再被改写。
- 修复引用、@ 等控制组件可能脱离语音单独发送，以及含 Record 的混合消息链跳过残留 TTS 标签终检的问题。
- 群聊出现新消息时仍会补齐当前语音对应的完整中文正文，不再在“语音和文字都发送”模式下只留下外语语音。
- `send_message_to_user` 工具直发的 TTS 标签同步遵守全量转换；插件不再将超过 1600 字的完整回复静默截断，长文本交由实际模型与 TTS provider 明确处理。
- TTS 本机播放连续失败后按 30 秒至 30 分钟指数退避，播放恢复成功后自动清零，避免每条语音重复启动失败的后台播放器。

### 表达规则组

- 同一组支持片段归纳出的“情境表达”和“语法习惯”现在会合并为一个规则组；新归纳通过 `family_key` 明确关联，旧数据会按脱敏支持片段、批次和情境相似度安全补充分组，无需重新学习。
- 审核页改为按组审核：队列和详情只显示一张组合卡，可在同一情境下分别核对可复用表达与句法习惯，并一次通过、拒绝或删除整组；单独规则仍作为单组件规则组正常管理。
- 运行时先按原有频道、关系、情绪、意图、反馈和证据筛选组件，再按规则组合并召回；同源表达与语法只占一个召回名额，提示词只生成一条组合规则，证据数取组内最大值而不是重复相加。
- 表达库统一改为“已启用规则组”列表，类型筛选只要组内包含对应组件即可命中；顶部统计同时展示规则组、含表达组、含语法组、待审核组与使用次数，桌面和移动端都保留紧凑可读布局。

### 主动外链与群聊链接守卫

- B 站视频、新闻和主动搜索三类主动外链新增统一冷却，默认 72 小时；不同来源不能再绕过各自短冷却轮流发链接。可在新闻阅读详情中调整为 `0-168` 小时，设为 `0` 只关闭统一限制，不影响各来源原有冷却。
- 统一冷却兼容读取旧版 B 站、新闻、网页探索和外界信息自我关联时间，升级后立即生效；进入主动候选和真实发送都会更新统一时间锚点。用户主动询问链接与 AstrBot 官方定时消息不受影响。
- 移除网页探索同时进入“主动念头”和普通候选的重复入口，只保留带自我关联、偏好、概率、去重和冷却判断的完整候选链路；无判断模型时，单独出现“AI、模型、搜索”等泛关键词不再自动制造分享欲。
- 群聊当前消息或引用链中包含普通 URL、分享卡片链接时，未明确 @ Bot、引用 Bot、叫到 Bot 名称或承接正在进行的 Bot 对话，不再触发提问唤醒或主动插话；适配器提前标记的错误唤醒状态也会被清理。纯图片引用不会被误判为网页链接。

### 创作封面与预览

- 生图设置新增全局“提示词表达方式”，可选传统文生图提示词（`Positive/Negative prompt`、逗号分隔标签/短语）或自然语言描述（连贯英文场景句子）；主动拍照、每日穿搭、创作封面、自然语言生图和函数工具生图统一生效，不改变后端、参考图或画面风格配置。排障页最近生图记录会标出实际使用的提示词格式。
- 创作封面涉及人物且参考图可用时，会改用人物参考图生图链路，只保留参考图中的唯一人物；提示词明确禁止第二个人物、群像，以及镜子、窗户、传送门、屏幕、相框、雕像、阴影或倒影中的额外人物形象。
- 人物题材没有可用参考图时改为无人物的象征性物件/环境封面，不再随机生成与人设无关的人脸；不涉及人物的作品同样使用无人物构图，减少模型擅自添加角色。
- 旧版自动生成的人物封面如果尚未记录单人物策略，并且当前已启用可用的人设参考图，会获得一次自动升级机会；生成失败时保留旧封面并按原有冷却重试，不会先删除现有图片。
- 书柜封面放大预览新增弹窗自身的关闭按钮、遮罩点击和捕获阶段 Esc 监听，不再只依赖页面统一事件委托；同时提高关闭按钮的点击层级和触控稳定性，并继续在关闭后恢复到原封面按钮的键盘焦点。

### 书柜创作稳定性

- 修复创作连续推迟或模型返回空结果后，书柜长期反复写入“她把那个念头又往后推了一小步”“笔尖在纸上停了一秒”等固定兜底句的问题；空结果、近期重复或质量复核未通过时不再写入正文，也不会在重试耗尽后强制接纳。
- 重复检测范围由最近 5 段扩大到最近 12 段；失败推进按 30、60、120、240、360 分钟逐级退避，成功后自动恢复正常创作间隔，避免过期任务每轮高频重试。
- 几天后才到期的主动消息、模拟或定时计划不再长期占用创作空闲窗口，只有正在发送或 20 分钟内即将执行的主动计划会暂缓写作。
- 插件重载后的后台维护会清理旧作品中与内置固定兜底文本匹配的正文片段及对应创作记忆，并重新计算真实字数；正常正文、人工大纲和其它创作资料不受影响。

### 群聊重复发送防护

- 修复部分 OneBot/QQ 适配器把 Bot 刚发出的群消息重新送回事件总线后，同一正文在约一秒内再次进入发送链、导致聊天窗口出现两条完全相同回复的问题；Bot 自己的群聊回流现在会在观察、唤醒和 LLM 处理前终止。
- 发送出口新增短窗口幂等保护：同会话、同正文且同来源的并发副本会在发送中或发送确认后被拦截，覆盖平台回显和并发双链路；不同群友、不同引用/@目标以及带图片、语音或文件的消息不会被合并。
- 重复拦截会写入明确日志“发送前拦截短时间重复正文”或“已终止 Bot 自己的群聊回流事件”，便于区分真实双发、客户端显示异常和正常的相似回复。

### 历史关系网协同

- 为 MemoryCompanion 历史对话导入提供只读身份候选接口，按世界书成员名称、别名、观察名、Bot 称呼和已知自身 ID 辅助识别说话人；最终身份仍由用户人工确认。
- 历史聊天提炼出的称呼、信任、边界和关系变化只进入关系网待确认观察，保留导入批次、历史观察时间和原消息证据，不会直接改变正式关系。
- 支持按导入批次回滚尚未确认的历史关系观察；历史来源最多保留 24 条且不会挤掉原有普通待确认观察。
- 关系观察接口与页面读取上限同步扩充，适配长聊天记录跨片段去重后的审核需求。

## 5.9.11

### 学习工作台信息架构

- 学习页改为与总览页一致的紧凑概览和次级导航结构，固定分为“技能学习、表达学习、社交关系网”；原顶级关系网页完整合并进学习工作台，旧“打开关系网”入口继续自动跳转到新位置。
- 表达审核从表达库中拆出独立审核页，使用待审核队列与规则详情主从布局，可按来源、类型和关键词筛选；支持键盘切换规则，桌面端双栏审核，移动端自动转为单列，审核后直接进入下一条。
- 表达库只展示已启用规则和次级观察素材，不再把待审核规则混入正文；待审核数量可从顶部概览或表达页入口直接进入审核队列。
- 行为习惯迁回私聊对象详情，与关系、情绪和对话片段一起检视并可单独清空；专用清理动作不会误删表达、关系情绪或对话片段，学习页也不再重复维护用户名单和私聊行为详情。

### 回复复核范围标识

- 被动回复复核与智能沉默归入“通用能力”；主动私聊发送前终审回归“私聊陪伴”，不再为单个开关单独占用一整个“主动私聊”分组，配置引导同步采用相同位置。
- 三项卡片与原生配置提示仍直接标明实际范围：被动复核用于私聊普通回复及群聊答疑唤醒保护，主动终审仅用于主动私聊，智能沉默同时用于私聊和群聊。此次调整只改变配置位置，不改变开关状态、保存路径或运行逻辑。
- 记忆协同总开关改名为“外部记忆插件协同”，面板英文标识改为中性的 `enable_external_memory_integration`，避免旧兼容键让人误以为只支持 LivingMemory；内部仍保留 `enable_livingmemory_integration` 读取既有配置，LivingMemory 专属工具名子项继续单独标注。

### 书柜备忘便签

- 书柜首页新增独立备忘便签工作区，可新建、编辑、完成、恢复、置顶和删除便签，并支持暖黄、浅蓝、浅绿、淡粉和雾灰五种颜色；进行中、全部和已完成便签可直接筛选，无需进入书籍详情页。
- 便签支持到期时间、到期提醒以及每天/每周/每月/每年重复。重复便签完成后自动滚动到下一次日期；普通便签保留为已完成记录。
- 到期便签接入现有主动候选、免打扰、额度和发送复核链路，只提醒主要用户；成功投放后 24 小时内不重复，没有可投递对象时按 10 分钟退避，不会让逾期便签高频唤醒调度循环。
- 临近便签可进入日程和私聊上下文，但始终标记为待办而非已发生经历；用户询问便签、备忘或待办时可读取完整进行中列表，其他私聊对象不会收到或读取主要用户便签。
- 便签正文按原始用户文本保存，存储安全清理不会改写其中的伪标签或代码片段；页面输出继续执行 HTML 转义。
- 主要用户现在可以直接在私聊中用自然语言新增、查看、修改、完成、恢复和置顶便签；提醒时间支持绝对日期以及“明早 9 点”“两小时后”“周五下午”等常见表达。
- 聊天删除便签必须经过二次确认；同名匹配、越权访问和持久化失败都不会落库，也不会让 Bot 误报已经操作成功。
- 聊天列表支持进行中、已完成、全部和关键词筛选，长正文先给轻量预览并可继续读取完整详情；“取消删除”和确认期间内容变更也会被安全处理。
- 明确“便签/备忘/待办”请求与普通“提醒我/叫醒我/定时”请求的路由边界；在 AstrBot 补齐内置工具后的 Agent 启动阶段再次移除明确便签请求中的 `future_task`，带提醒的便签保存成功后也会抑制同轮 `<timer>`；普通提醒则按 `future_task` 的真实成功结果去重，避免一件事产生两份提醒。
- 表达学习改为 WaifuBot 式双分类归纳：分别生成“具体情境 -> 可复用表达”和“具体情境 -> 语法习惯”，支持短表达、梗和占位模板；抽象的句长/标点/场景统计仅作为观察素材，不再冒充已学规则。
- 私聊与群聊复用现有片段整理调用完成归纳，不增加独立 LLM 请求；明显独特的单次表达也可送审，同一情境下的不同说法独立保存。群聊会拦截昵称、@、长账号、关系事实、秘密、罕见专名和长句照搬，只保留安全脱敏模板。
- 所有模型归纳规则必须在学习页审核通过后才能召回；未审核样本、节奏和标点统计已彻底移出回复注入。支持片段只供审核查看，永远不会写入回复提示词。
- 学习页重构为简洁的统一表达学习库，可按来源和类型筛选，集中展示情境、模板/句法、使用指令、适用上下文、证据、使用次数和反馈；待审核规则置顶，观察素材默认折叠，并可快捷配置从哪些私聊/群聊学习、在哪些会话使用。
- 继续打磨学习页管理体验：新增规则状态筛选、搜索与筛选一键清空、长列表分批显示、顶部统计快捷跳转和待审核首条证据展开；移动端筛选/审核按钮统一扩大触控区域。旧的场景、句长、标点统计改名为“未归纳观察”并降级为紧凑列表，明确提示它既不是情境表达也不是语法习惯。
- 收紧 WaifuBot 式规则质量门：情境表达必须是 2–32 字可直接复用的原话或脱敏模板，包含“偏好、语气、口语化、短句、铺垫”等分析词的抽象描述会被拒绝；语法习惯必须带句长、主语省略、拆句、问句等可验证结构。旧的无效待审核/已启用规则会在读取学习库时清理，不能通过审核或进入回复。
- 已审核规则继续接入频道、关系、情绪、意图和真实反馈过滤，召回最多两条时优先兼顾情境表达与语法习惯；连续负向反馈会自动停用并退回复审。固定优先级仍为“工具事实 > 安全能力 > 人格 > 关系情绪 > 已审核表达规则”，工具失败不会被语气改写成成功。

### 生图工具降级兼容

- 修复首选模型失败并切换兼容模型后，`pc_generate_photo` 工具调用被写成普通 JSON、随后由 TTS 分段直接发给用户的问题；明确生图请求会恢复执行真实工具，其他工具 JSON 只拦截不执行。
- TTS 前处理、TTS 后台分块、普通分段补发和最终发送链都增加明文工具调用清理；恢复失败时明确告知图片未发送，不再泄漏参数或假装成功。

### 每日 Bot 日记

- 日记改为先整理当天经历账本，再生成正文：成功发送、当天搜索/阅读、实际创作、生图与空间发布等可确认记录，与日程计划、目标运行记录、日程推演、状态底色分层使用，不再把“计划做了什么”直接写成“实际经历了什么”；旧日期快照和无时间记录不会混入今天。
- 日记正文与后台素材拆成两步生成，正文不再同时安排当日主动消息；分享素材、梦境碎片、连续性线索和跨日事项只能从已成稿正文中提取，关闭“提取自然分享素材”后会在最终归一化层强制留空。
- 新增日记写作形式、篇幅、写实程度、创作方向和分享素材开关，并补齐拓展页可见性联动；自动形式会在场景短记、碎片手记、心绪自述和观察记录之间按日期稳定选择。
- 增加一次性质量复核，拦截后台术语、状态播报、未确认事项冒充完成经历及近期高度重复内容；失败后最多修订一次，仍不合格则写事实克制的短记录，不再用桌面、窗光、茶水或便签等固定场景兜底。
- 日记连续性线索支持“出现、变化、淡出”，近期日记只提供承接与避重参考，没有新依据时不会强行续篇。

### 主动消息文案去 AI 味

- 清理主动消息引导文案中普遍存在的模板化口吻：兜底分享不再使用"刚在X刷到《标题》，莫名觉得你可能会多看一眼，顺手丢给你"等填空句式，改为只给事实（标题/印象+链接），不解释为什么分享、不预写语气。
- 内部 motive 动机笔记全面精简：去掉"只轻轻提一句""想短短说一句""感觉和对方平时会在意的东西有点贴"等千篇一律的预写发言，改为简洁的事实陈述（如"这条视频和对方兴趣相关"），让人格模型自由发挥而不是被模板带跑。
- context 线索标签去掉叙述性文案（"B站视频分享线索：刚刷到一个视频"→"B站视频分享线索"），表达要求从"可以只轻轻提起,也可以按人格吐槽、担心、好奇或转移成日常感受"简化为"按人格正常说话即可"。
- topic 占位符去掉"刚X到的"前缀（"刚刷到的 B 站视频"→"B站视频"、"刚查到的新东西"→"新发现"等），消除可识别的 AI 签名。
- 早中晚问候、生日、用餐关心、静默回访、失眠、活动分享、日记分享等场景的 motive 文案统一去掉了过度解释和假装随意的措辞。
- topic 软化方法 `_soften_topic_hook` 去掉"刚刚那点X""窗外那点X"等机械前缀和硬编码替换表，短 topic 直接原样返回，不再套模板。
- motive 规范化方法 `_normalize_internal_motive_text` 精简替换表：去掉"冒个头→想跟你说一句""刷存在感→想跟你说一句"等把一种模板换成另一种的替换，改为直接删除；同时删除"忽然想→有点想""碰你一下→跟你说一句"等制造新统一口癖的规则。
- motive 泄漏清洗 `_strip_proactive_motive_leak_text` 扩充安全网模式：新增"莫名觉得""顺手丢给你""多看一眼""只轻轻提一句""想短短说一句""感觉和…有点贴""想跟你说一句"等模式的清洗，防止模型自行生成已从源头删除的 AI 措辞。
- 社交润色 `_soften_social_proactive_text` 不再用替换产生新 AI 签名："发现你好像在忙→看你还没从那边抬头""辛苦啦→别太累""免得你又忘了我→怕你忙过头"等替换改为直接删除。
- 主动消息生成 prompt 新增约束："开口动机是内部决策依据，不是你要说出口的话；不要照抄动机里的措辞"。
- 问候 topic 从标签式改为场景碎片式："早安"→"刚醒"、"午后犯困"→"午饭后那会儿"、"晚间问候"→"天暗下来那会儿"。

### 群聊引用理解

- 引用卡片和多层引用链会明确区分本轮发言者、引用作者与被提到的第三方，人物和动作优先按原句对应，避免把引用里的信使、地点动作或旧记忆动作顺手换到本轮提到的另一个人身上。
- 归属提示采用柔性优先级：旧记忆仍可补充语气和连续性，但人物关系冲突时优先采用当前文字与直接引用，不会把普通接话写成生硬的逐项复述。

### 群聊链接接话

- 裸链接、分享卡片和普通链接附言不再触发群聊主动插话、复读跟随、兴趣唤醒或冷群唤醒，避免 Bot 引用链接后自行猜测内容并强行碰话题。
- 链接旁有明确问题时会先剥离 URL，只按用户实际写下的提问走群聊提问唤醒；已进入正常唤醒回复的消息不会再并发触发主动插话。

### 生图与创作

- 主动生图配置的“每日穿搭”附近新增“为创作内容生成封面”开关，默认关闭；开启后，有正文但没有封面的作品会在空闲创作推进时调用现有文生图后端生成封面，并根据悬疑、科幻奇幻、诗歌散文、古风、日常治愈等作品特征自动匹配封面画风。失败最多重试 3 次并带冷却，成功封面会保存到独立目录并显示在书柜作品详情中；关闭开关不会删除已有封面。
- 修复书柜创作详情和阅读页提前返回、未执行封面图片 hydration 的问题；创作封面改走 Page 桥接可读取的图片数据接口，并支持点击放大、Esc/遮罩关闭、键盘焦点恢复和移动端自适应预览。

## 5.9.9

### 陪伴面板

- 日程与每日穿搭读取 MemoryCompanion 快速上下文时会携带当前 Bot 所有者身份；有事件时使用事件账号，后台任务仅在账号唯一可确定时传递，多 Bot 无法唯一判定时不会猜测，避免其它 Bot 的自我时间线进入当前计划。
- 优化启动时群聊黑话维护：成员昵称、身份名和关系资料索引从“每条黑话重新构建”改为“每个群只构建一次并复用”，保持原有清理结果，避免黑话和成员较多时出现 `group_slang_cleanup` 慢任务告警及事件循环短暂停顿。
- 修复群内常见词把引用和分享协议字段当成群梗的问题：`引用消息`、`MSG_ID`、`Reply`、`share_source`、`share_medium` 及同类消息 ID 字段不再参与学习，启动清理会同步移除已经累计的污染词和对应释义，真实群名词不受影响。
- 优化持久化数据安全清理：原始群消息、成员原话和 Markdown 代码片段不再被未知标签规则改写，避免用户讨论 `<bubble/>` 时出现记忆缺字；模型派生字段仍会清理伪控制标签。延迟快照清理后会同步修复内存并补写一次，不再对同一批字段反复扫描和告警；快照线程不再逐次打印，同阶段清理日志最多每十分钟汇总一次并附带累计抑制数，正常自愈日志调整为 INFO。
- 修复私聊追问 Bot 自己刚发的 QQ 空间时发生主客体颠倒：空间相关对话现在会携带 Bot 最近成功发布的正文与配图数，并明确区分“Bot 自己的动态”和“当前用户的动态”，不再让用户自行查看或误查用户空间。
- 修复私聊静态图绕过模型卡片“插件识图链路”的问题：已配置可用识图模型时，静态图和 GIF 统一优先走视觉转述及其备用链，只有没有识图模型时才直挂主模型；视觉模型返回“不支持视觉/无法看图”也会判为失败并继续备用链，同时保留截图中真实出现的同名错误文字。当前配置模型固定优先于历史成功模型，清空视觉配置后历史记录不再强行启用识图；图片后续补充文字时使用完整视觉等待预算，不再固定等待 12 秒后提前降级。
- 原生基础配置与拓展页“配置 → 存储方式”同步新增“数据安全清理”开关，默认继续清理 `<bubble/>` 等误入陪伴数据的伪控制标签；拓展页会显示当前状态并随存储表单保存。关闭后保留原始存储文本，但不影响消息发送出口已有的安全过滤。

## 5.9.8

### 陪伴面板

- 页面字体从原始字体与单一艺术字库扩展为 8 种选择，新增微软雅黑、等线、思源黑体/苹方、宋体、楷体和仿宋；系统字体使用清晰稳定的本机回退栈，表单控件与正文同步切换，并保留原有 `13.woff2` 艺术字体。
- 收紧早间饮食关心：早安只做自然问候，不再一开口就追问早餐；早餐候选必须等当天早安成功发送且用户随后回应，旧队列也会在发送前复核。饭点关心默认由每天最多两次降为一次，并与饥饿状态分享共享 7 小时饮食话题冷却，避免两条主动来源连续询问吃了吗或吃什么。
- 重整天气配置入口：天气上下文、OpenWeatherMap Key、城市/经纬度、刷新间隔和环境突变设置统一移入“配置 → 功能开关 → 环境感知”详情，并保留当前天气来源状态提示；“运行设置”只保留主动调度和资源配额，不再重复承载环境能力。
- 修复扩展页日程配置没有稳定入口的问题：原“拟人身体状态”调整为“拟人生活状态”，每日粗日程、节点数、当前段细化、每日 Bot 日记、重要日期前瞻和高级日程提示词统一归入其详情页；各子开关独立生效，并按开关状态收起无效参数，不再落入不可见的“其他”分组。
- 优化书架分类与书脊排版：字数进度统一为紧凑横排并固定在书脊底部，书名限制为稳定的单列竖排，长标题自动缩小和截断，不再挤成多列；移除每本书重复的类型标签与创作分类说明，收紧分组留白并使用固定窄书宽，让同一层书架容纳更多作品且不随视口拉伸变形。
- 排障页支持按稳定语义类型屏蔽警告：同一故障在常见检查、最近问题和深层运行诊断中共享屏蔽状态，SQLite 原始警告与生图链路风险也可单独处理；主动审计按超时、Provider、发送、存储、媒体等原因和动作细分，不会一并隐藏整个来源。屏蔽后不再计入当前统计但不会删除原始记录或影响运行；已屏蔽区域显示当前命中数，并支持单项或全部恢复。错误级故障始终保留，不提供屏蔽入口。
- 每日穿搭读取 MemoryCompanion 时自动使用本地快速上下文，并携带主用户身份，只读取近期日程、历史穿搭、相关服装偏好和最近图片；不再因完整 Embedding/Rerank 检索超过 1.2 秒而频繁跳过。
- 写入 MemoryCompanion 的梦境碎片改为 Bot 自我范围，不再使用伪私聊用户 `private_companion:dream`；已有记录由 MemoryCompanion 启动迁移安全归位。

## 5.9.7

### 总览工作台与稳定性

- 重整陪伴面板总览：按“今日与当前状态、统一时间轴、动态与记忆、运行能力”组织首屏，近期动向与记忆概况使用扁平列表，互动策略和观察记录按需展开，移除重复快捷卡片及大型“配置与运行健康”区域。
- 时间轴支持在固定容器内独立滚动，自动定位并高亮当前日程；起止时间、活动正文和生命周期分层显示，用户介入与生活变化按时间并入同一条时间轴，不再拆成独立卡片。
- 运行能力改为右侧紧凑状态卡，只展示私聊陪伴、群聊观察、主动消息和模型分流四项核心能力，并可直接进入对应管理页。
- 修复总览网格样式覆盖通用面板隐藏规则、导致切换页面后总览仍遮挡其他页面的问题；主导航事件限定到顶部导航，内部标签不再被误当成页面切换按钮。
- 修复 `personal_goal_share_cooldown_hours` 读取上限时调用 `_cfg_float` 参数不匹配、导致插件在 AstrBot 4.26.4 初始化阶段载入失败的问题；浮点配置读取现在与整数配置一致，支持可选最小值和最大值。
- 修复主动消息人格在多阶段链路中逐步丢失的问题：插件指定人格现在通过本轮会话副本进入 AstrBot 系统人格层，完整人格、主动风格和收件人边界会贯穿主生成、回复空气改写、兜底及发送前复核；人格缓存按会话/指定人格隔离，复核改写后再次校验称呼，并停止机械删除人格明确使用的开场口癖与“想起你”等自然表达。
- 修复私聊短句承接时把 Bot 与用户事实归属说反的问题：生成提示现在明确区分 Bot 视角记忆中的“我”和当前收件人，短句中无依据的“用户上次/先做过”断言会进入严重级审校；“明明是……”“记反了”等当前纠正会作为短期高优先级锚点保留两轮，回复审校同时携带完整人格、风格与纠正内容，避免刚承认后下一句又翻回旧错误。
- 修复配置页已有创作项目、聊天中却否认写过作品的问题：“写过书吗、有没有自己的作品、那本书进度”等作者语义会退出轻量私聊并读取真实创作项目；上下文提供有正文项目的完成/进行中数量及近期标题，同时区分“写过文本作品”和“正式出版实体书”。若模型仍否认已有作品，发送前严重级审校会依据同一份创作记录改写；普通“在看什么书/这本书好看吗”仍走阅读语义，不会误触发作者身份。
- 修复穿搭轮换提示可能被生图模型理解为“同图展示两套衣服”的问题：每日穿搭和自拍链路统一要求单人、单套穿搭、单一连续画面，并禁止左右对比、分屏、双联画、拼图、人物复制和多套服装；轮换差异只用于选择这一套新造型，不再要求同时展示历史造型。
- 优化单图视觉回复：用户没有明确询问图片内容时，按人格自然评价、接梗、回应情绪或追问重点，最多顺带一个显眼细节，不再把视觉摘要逐项复述成看图报告；“图里有几个人”等明确问题仍会按画面作答。
- 优化陪伴数据存储：事件循环中的连续保存会合并后在线程中写盘，避免每次状态变化都同步重写整份 `companions.json`；卸载时会等待后台写盘并保存最终快照，防止旧快照覆盖新状态。
- 自动压缩重复历史负载：保护正在计划和等待执行的主动候选，将已结束候选历史收敛到总计 600 条；新闻与网页探索历史只保留摘要和近期必要元数据，不再为每一期重复保存整批原始文章/搜索结果。现有约 10 MB 数据预计可缩小四成左右，不影响当前日程、用户状态、人格、记忆或待发送任务。
- 将“每日主动上限 = 0”改为主动系统硬停：该设置优先于高强度/无限额度预设，保存后立即清理现有主动计划、念头、待跟进和重发任务，阻止新的候选、模拟、排障主动及后台主动行为生成，也不会继续发送；被动聊天与日程、状态、日记等基础维护仍正常运行。
- 补齐主动硬停与存储写盘的竞态处理：每日主动上限恢复为正数后会立即唤醒普通主动调度，AstrBot 官方定时消息仍独立运行、不受主动额度影响；切换 JSON/SQLite 前等待合并保存完成，卸载时也不再放弃仍在写入的旧快照，避免旧数据覆盖最终状态。

## 5.9.6

### 主动消息与扩展页

- 新增统一出站凭据防泄漏：被动回复、主动消息、媒体说明、普通分段、合并转发、回复拦截转发以及 `send_message_to_user` 工具直发都会在最终发送前检查已配置密钥、`sk-` 等常见平台令牌、Bearer Token、JWT、URL 查询凭据和 `api_key/token/secret/password` 字段，并只将凭据替换为“密钥已隐藏”；TTS 工具路径会在语音转换前脱敏，避免密钥被朗读。
- 在线生图链路的 401/403 提示现在区分“未携带/未识别 Authorization”与“Bearer 已送达但 Key、模型权限、IP 白名单或 WAF 拒绝”，方便直接定位代理网关鉴权问题。
- 按 SenseNova 新开放平台官方文档适配 U1 Fast 信息图：识别 `token.sensenova.cn` 和 `sensenova` 平台，兼容纠正旧写法 `senova-u1-fast` 为官方 `sensenova-u1-fast`，将常见图片尺寸映射到官方 11 个固定 2K 尺寸并提交 `n=1`；明确禁止该模型不支持的参考图接口，生成结果返回后立即下载短时效 URL。扩展页同步新增“SenseNova 日日新”平台选项和配置提示。
- 优化陪伴面板页面切换动效：按导航前后关系使用方向一致的短距离滑动与淡入淡出，支持原生 View Transition 面板快照并为旧浏览器提供轻量回退；连续切换会中断旧动画，不堆积任务或同时显示多个面板，同时完整遵循系统“减少动态效果”偏好。
- 新增非创作型个人目标：可在观察页明确创建阅读、学习、健康与生活整理等目标，记录进度、下一步、匹配词和证据日志；活跃目标会轻度影响日程，只有匹配日程实际完成后才自动推进，仅跨越里程碑、持续停滞或完成时形成有真实依据的主动候选，不会自行编造目标或要求用户监督。
- 收紧个人目标结算：取消的日程不再推进目标，分类标签不再作为完成证据，避免一个“阅读”片段同时推进多个阅读目标；冷却中的里程碑会保留到可投递时，停滞事件只在实际进入候选后标记，手动进度更新会写入证据日志并保持完成状态与进度一致。目标卡片默认折叠，展开后可查看最近推进依据。
- 新增“环境突变”即时主动来源：独立于普通 90 分钟天气缓存，默认每 10 分钟刷新一次实时天气；首次仅建立基线，之后在开始降雨、雷暴、强降雨、降雪、雨雪停止、明显起雾/大风及短时温差达到 5°C 时，向主要用户投放 1–3 分钟内到点、约 45 分钟失效的主动候选。配置页可关闭或调整检测与去重冷却；相同变化会抑制重复，普通变化至少间隔 20 分钟，高强度升级仍可及时进入候选，夜间休息时段不发送。
- 每日粗日程保持默认 10 个节点、上限允许手动提升到 24；生成提示会为 17:00 后保留约三分之一节点，质量检查要求覆盖晚间，解析时先按时间排序再截取，避免上午和下午过密导致后半天被截断。
- 重做用户习惯准入与生效证据：过滤转发占位、插件探针、跨用户转述、单次问答和无信息短句；作息、饮食、工作与娱乐只接受明确的用户自身状态；问候、摸摸和近况询问单独归为互动习惯；必须跨多个自然日重复才会注入提示词或触发习惯主动，旧版仅靠同日次数堆出的记录自动失效。
- 模型页新增可选的 DeepSeek 峰谷价格路由：可按北京时间高峰窗口把插件任务中识别为 DeepSeek 的 Provider 临时替换为指定模型，离开高峰后自动恢复且不改写原模型分工；默认高峰时段按最新公告设置为 `09:00-12:00`、`14:00-18:00`，支持自定义时区、时段和匹配关键词，并显示当前状态与下次切换时间。由于公告仅说明预计 2026 年 7 月中旬启用而未给出精确日期，功能默认关闭。
- 扩展 TTS 输出控制：新增“仅语音 / 语音和文字都发送”、外语文字显示“朗读原文 / 中文译文 / 双语”，以及同时作用于快速标签和后处理路径的“局部转换 / 全量转换”；语音合成失败始终保留文字兜底。旧“自动语音完整转换”开关隐藏并继续兼容历史配置，TTS 参数页按最终发送效果、生成与转换、触发概率、会话间隔、自动语音、旧版兼容和本机/直播重新分段，并按语种、发送形态、生成路径、频控模式及子开关动态隐藏无效参数。
- 修复主动戳一戳在正文生成失败时被候选重试放大为连续多次动作的问题：poke 改为正文确认可发送后才执行，Token 硬限额耗尽时会在任何主动动作产生副作用前取消；显式次数会再次夹紧到“每次最多次数”，同一动作不会再叠加消息前置 poke，并增加并发锁和默认 30 分钟用户级冷却。主动框架同时移除旧 Context 代理，AstrBot 4.26.2+ 始终传入真实 `Context`，避免 `AstrAgentContext` 类型校验失败。
- 修复长日程被短时进食动作概括成“连续吃几小时”以及当前片段混入模型草稿的问题：父级日程与细化摘要现在必须覆盖完整时间窗，超过两小时的单一用餐描述会自动补全餐后推进；读取旧数据时会清理 `dream_seed`、Markdown、英文角色台词前缀和无依据的英文具名同行者，并把进食类 QQ 自定义状态限制到实际细化事件时长。
- 重整每日粗日程与分段细化的参考来源：统一按“硬约束、当前事实、连续性参考、软灵感/主动规划”分级，日程专用设定优先于默认人格，记忆、屏幕节奏、用户习惯、旧日记和梦境不再具备制造当天事实的权限。细化阶段不再重复读取最近日记和未来重要日期，只承接宏观日程、当前状态与必要连续记忆；JSON 示例改为覆盖同一时段开头、中段和收尾的 3 条事件，模型若仍只生成局部事件、用短动作概括长时段或混入草稿字段，会自动携带具体原因重试一次。
- 日程升级为显式起止时间和完整生命周期：每段可区分计划中、进行中、已完成、已变更与已取消，跨午夜与日程空档会按真实区间判断；旧日程会自动补齐结束时间，用户介入会记录变更原因和作用范围，不再默认影响全天。
- 陪伴面板新增日程质量、来源依据和置信度展示，并允许单独重新细化或取消某一时间段；局部重做会保留该段已有的用户介入记录，失败时恢复旧结果并显示原因，取消后的事件不会再进入当前片段或主动契机。粗日程和细化结果都会检查时间重叠、异常空档、活动时长、日期冲突、抽象/瞬时内容及覆盖密度，低质量结果会带问题自动纠偏；细化事件数量会按时段长度动态调整，睡眠段保持轻量。
- 修复日程局部操作的状态一致性：取消现在会终止失败重试、当前活动注入和主动契机，进行中的自动/手动细化若晚于取消或新日程返回会被丢弃；重做失败及生成中断会恢复原生命周期。跨午夜子时间段统一按递增时间轴判断和展示，用户介入固定到发生时的时间段，`proactive_only` 不再改写生活事件，“直到回家”会在明确共同回家后解除；长时段最多 5 条细化事件会在面板完整展示。
- 总览顶部将原本混合展示的“今日见闻”拆为“新闻阅读”和“主动搜索”两张独立状态卡：两边不再互相借用标题、时间和状态，点击后会分别定位到对应详情卡。
- 修复 AstrBot 官方配置页中大量分组参数修改后可能被隐藏旧值覆盖的问题，包含 Token 硬限额、软限额开关等：官方可见分组值现在是唯一权威来源，顶层兼容字段只跟随同步；真正缺少分组值的旧配置仍会正常迁移。
- 修复 TTS 生成路径从后处理切回快速标签时可能出现“配置已落盘但面板或运行态仍保留旧模式”的问题：切换重绘前会保留整张表单草稿，后端保存后显式同步规范化运行模式。
- 修复 TTS 后处理模式仍可能执行主模型或工具输出的快速标签：`<pc_tts>` 与 `<tts>` 现在会先降级为普通可见文本，只允许发送前后处理模型生成最终语音块，终检也不再走快速标签路径。
- 主动人格判定改为稳定语义签名和每用户多结果缓存，增加高置信本地短路与每日模型调用上限；达到上限后平滑回退本地规则，并明确 Token 面板展示的是累计调用。
- 余额感知支持自动查询：自定义 URL 留空时，会按插件实际使用模型与 AstrBot 默认模型顺序，复用已配置 Provider 的同源 API Base 与 Key 尝试常见只读余额协议；成功端点会缓存，Token 页会显示自动/自定义查询方式。不同服务没有统一余额标准，不支持的 Provider 会安全跳过并继续尝试。
- 修复次要用户主动消息在主链降级时可能沿用主要用户专属称呼的问题：正常生成、直接兜底和最终复核统一携带稳定 ID、当前昵称/显示名与关系边界；本地只纠正误用为收件人称呼的固定名字，不干预正文中正常的第三方提及，也不硬编码任何用户名称。
- “临时预约与动作查岗”改为内建主动类别，不再额外显示或依赖独立功能开关；旧字段仅保留为不可见迁移兼容，实际发送仍受主动权限、免打扰、关系边界和官方定时计划约束。
- “吃什么候选”新增批量选择、全选和二次确认删除；后端在同一数据锁内完成批量过滤并只保存一次，同时返回已删除与缺失的候选 ID。

## 5.9.4

### 主动消息与模型工具

- 新增独立饭点关心闭环：早餐、午饭、晚饭窗口可主动询问主要用户是否吃过，默认每天最多选择两餐；没有得到具体内容时每餐只低压补问一次。用户答“吃了”会自然追问具体食物，答“没吃”会改为关心和少量菜单建议，明确说出的菜品、店或餐食会自动新增/合并到“吃什么候选”。
- 补齐余额感知的扩展页入口：开关和接口、阈值、冷却等参数现归入“环境感知 → 余额与补给”；Token 页新增只读余额卡片，展示最近成功拉取的余额、剩余比例、档位、检查时间和失败状态，不暴露接口地址或 API Key。
- 每张模型配置卡新增可选“备用模型”：主模型调用报错、超时或返回空内容时，同一任务最多自动尝试一次备用 Provider；配置按卡片独立保存，并覆盖统一文本调用、插件识图、夹层阅读视觉和 TTS 文本后处理链路。
- 生理期模拟注入收束为四行直观描述：边界、底色、影响和周期状态；经期前、经期中与恢复期使用不同影响文案，并轻度影响主动频率。经期中私聊普通主动间隔约放大 18%，群聊主动插话间隔约放大 25%、自然插话概率约降至 76%；恢复期逐步回到正常，不影响用户预约定时消息。
- 新增“动作查岗”主动类别：用户暂时去洗澡、吃饭或短时办事时，模型会在生成念头时估计结束时间、按人格控制回访强度并预约一次官方定时消息；用户提前回来会自动取消，明确提醒/叫醒预约始终优先。
- 每张模型配置卡支持单独设置 `5-600` 秒超时；留空继续使用 Provider 默认值，快速/精准模式和视觉、语音等任务会按实际选中的卡片生效。
- 修复 `tool_call` 生图后端：自定义 `@llm_tool` 现在通过 AstrBot 标准工具管理器获取 handler，并使用有效的合成会话事件调用，不再因旧注册表查找或无效事件上下文导致无法执行后回退。
- 修复普通人物生活照使用 `text2img` 工作流时不带人物参考图的问题；场景模型会显式判断画面是否出现 Bot，本人出镜时优先使用当日穿搭图并回退到人设参考图。
- 主动发图成功后会保存短期画面语义快照并写入会话归档；用户紧接着发送“？”或询问刚才的图片时，以真实画面摘要承接，不再从旧梦境、旧日程猜测图片内容。
- 兼容 AstrBot 4.26.4 主动 Agent 的上下文类型校验：改用真实框架 `Context`，并仅在本轮请求工具集中隔离不兼容工具，避免主动正文因代理上下文类型错误而生成失败。

## 5.9.3

### 主动消息与创作连续性

- 新增可选的余额感知：可定期读取通用 HTTP JSON 余额接口，支持鉴权、JSON 路径、单位换算、绝对值/剩余比例阈值和失败退避；余额偏低时只向主要用户投放经过完整主动边界与发送复核的候选，允许按人格自然表达为“零花钱/补给”，并限制重复提醒、催促施压和内部信息泄露。
- 在线生图 URL 下载改为复用连接、流式临时落盘和原子保存，减少重复握手与整图内存拼接；新增显式下载代理和环境代理开关，便于与命令行下载使用同一网络路径。
- 主动分享创作后会按用户保存短期的实际分享快照；用户马上追问时优先读取刚发出的标题、设定与正文，不再只依赖可能截断或尚未写入的会话历史，也不会把后续新写的片段当成刚分享的内容。
- 修复扩展页遗漏函数工具生图参数的问题：现在会在“主动拍照/生图”详情中单列函数工具名、提示词/类型/参考图参数名和额外 JSON 参数；更新后会自动刷新前端脚本缓存。

## 5.9.2

### 答疑与主动编排

- 优化创作相关问答中的上下文连续性：涉及已分享标题或片段时，优先沿用“我之前写过/发你看过”的自然说法，不主动展开作者归属解释。
- `陪伴 答疑`改为现场诊断式回答：优先说明最可能链路、现有证据和一个下一步检查，不再默认堆叠整套配置；只有明确询问配置或参数时才生成待确认调参建议。
- 答疑帮助与 README 同步改为“先说现象”的提问方式，并明确`答疑确认`、`答疑取消`和`答疑设置`的使用边界。
- 主动行为由多来源念头池统一仲裁：定时预约、待跟进、重要日期等事件型候选优先于随机主动；同题候选跨来源自动合并，避免重复开口或随机候选抢占高价值事件。
- 主动候选物化会同时参考时效、内容评分和编排优先级；临近窗口内优先处理更重要的行为，原有免打扰、冷却、未回应降速和关系边界仍继续生效。
- 修复即时拍照/生活分享在被延后后仍按原场景补发的问题：原窗口内可自然顺延，超过最佳窗口会丢弃旧由头并重新安排，不再把午饭、眼前画面等延迟数小时发送。
- 延后但仍处于自然窗口的生活分享会改用已发生时态，不再伪装成发送当下才拍到的现场消息。
- 主动候选新增统一交付版本与新鲜度校验：渲染完成后会复核候选是否已替换、是否越过自然窗口及期间是否出现新私聊，避免慢生图或模型调用后发送陈旧内容。
- 发送失败不再把依赖即时语境的正文或媒体原样重试；仅时效稳定的事件型候选可在短窗口内技术重试，新私聊会立即使旧重试失效。引用触发消息也会在用户补充新消息后自动取消。

### 日程与沉浸感

- 每日穿搭新增本地造型轮换：成功图会保留近期穿搭档案，后续生成会结合日程和天气避开重复的主色、外层和轮廓，并保证与最近一套至少有两个可见维度不同；手动重生同样生效，可在“每日穿搭”设置中调整冷却天数。
- 日程细化不再把假设中的主动收发、用户回复或未来时段的聊天写成既成生活事件；聊天只作为状态余味参考，真实发送与回应仍由实际事件链路确认。
- 日程不再强制把多处时间段写成围绕用户的“关系伏笔”；普通回应、玩笑和轻微正向互动不再自动扩写为后续生活剧情。
- 收紧对话驱动日程偏移：用户单独描述自己出门、到家、休息或喝水时，不会再改写 Bot 的位置或作息；只有明确同行、约定或直接照顾 Bot 的语义才会承接到日程。
- 日程与细化注入不再重复携带用户原话，减少模型把聊天内容改写成旁白事件或未来既成事实的概率。

### 扩展页与配置体验

- 重做扩展页的总览、关系网、排障、实验能力等信息组织，减少重复卡片和错位堆叠，强调当前状态、关键证据和下一步动作。
- 修复“常用配置”提前闭合导致配置迁移、存储方式、拓展页设置、群聊名单覆盖与群聊名单落在外层的问题；这些日常维护项现统一收纳在同一折叠入口中。
- 配置页“常用配置”现在按“日常使用 / 数据与维护”整理：外部主动能力、动态提示词注入和群聊名单在前，迁移、存储与页面外观集中在后，减少来回查找。
- 群聊名单不再同时出现在“群聊总开关”详情与常用配置中；白名单、黑名单和覆盖范围统一由常用配置管理。
- 配置引导收敛首次必填项为目标用户/平台与核心模型测试；世界知识、日程、关系网和主动测试明确改为可后补的增强项，不再阻塞基础配置保存。
- 配置引导首页会正确显示已通过的主动测试；测试说明与完成按钮同步标注可选验证，减少“必须全部配置才能完成”的误解。
- 统一优化主配置页的卡片、折叠入口、表单控件与操作区视觉节奏；新样式只作用于配置主页，不影响模型、关系网、排障等二级页。
- “环境感知”新增天气配置入口和实时状态说明，明确独立 OpenWeatherMap、screen_companion 回退与未就绪状态；城市优先、经纬度成对填写和缓存刷新规则可直接在页面完成。
- 调整“群聊成图安全审核与私聊回退”：只有审核为安全的图片发群，其余结果（包括无法确认、超时或审核不可用）都仅尝试私聊原请求者；私聊失败时绝不回发群。

### 稳定性与兼容

- 后台主动循环任务增加生命周期追踪与终止回收，降低重载或停用插件时遗留任务的风险。
- 合并重复事件标识处理，并补齐旧版 TTS 配置别名迁移，减少历史配置升级后的兼容问题。
- 修复 B 站插件在 AstrBot 4.26.5 群消息流水线中的回调参数兼容：群聊处理器现可接收框架附带的额外参数，不再因参数数量不匹配反复报错。
- 修复用户明确请求语音但当前会话未配置可用 TTS Provider 时可能静默的问题：现在会明确回退为文字提示，不再产生空发送结果。
- 在线生图 API 返回 URL 后的下载改为独立 75 秒上限、分块读取和 32 MB 大小保护；CDN、代理或响应流卡住时会尽快将真实下载失败原因返回给工具，不再占满数百秒工具超时。
- 功能开关页不再将“主动消息终审”和“备选在线生图 API”误归为“其他”；它们分别收纳在回复/主动复核与主动拍照/生图的详情设置中。
- 外部主动能力与动态提示词注入位置收纳为“日常使用”并统一为同一配置卡样式。
- 删除与“功能开关”详情重复的环境、私聊、群聊、关系网、记忆和长线行为模块表单；“运行设置”仅保留主动节奏、资源配额与天气来源，旧创作速度别名不再作为独立参数展示。
- 收紧私聊关系纠错：单句吐槽、玩笑、软边界和反讽不再直接写入长期疏离/受伤状态；只有明确、持续且指向 Bot 的边界才会保留关系降速，旧版本误触发的临时疏离会在下次互动自动清理。

## 5.9.1

### 人格标准化与稳定风格校准

- 新增并持续打磨“人格标准化问卷”：可读取当前 AstrBot 人格或粘贴人格文本，生成可编辑审核稿；始终保留人工审核，不会自动覆盖 AstrBot 人格。
- 基础设定稿强化长参考资料吸收：长文本保留首尾、提高输出预算，初稿过短会自动扩写；硬事实仍保守处理，稳定性格、关系互动、亲近方式、压力反应、兴趣厌恶和边界偏好会更多参考补充资料。
- 性格特质改为立体结构：按底色与气质、内在驱动、外显表现、亲疏变化、压力与冲突、矛盾感整理，避免只写“温柔、傲娇、理性、敏感”等空标签。
- 稳定对话风格校准改为单情景审核与三条一组懒加载：界面一次只显示一个情景，完成当前批次第 2 个情景后会后台预取下一组三个。
- 已加载情景支持回头修改：顶部数字进度点可点击跳转，当前场景底部提供“上一个已加载 / 下一个已加载”，修改旧场景会自动让已生成风格规则失效。
- 对话风格校准新增独立“风格参考资料”输入框，最多 4000 字；默认可承接第一步补充资料，但修改它只会让情景候选/风格规则过期，不会把基础设定稿标为过期。
- 情景校准页改为更像“模拟对话”而不是模板展示：被动情景显示抽象的“模拟用户消息/上文意图”，主动情景显示“模拟主动意图”；不再给具体台词模板，避免模型从情景阶段就被带偏人物设定。生成结果会强制对齐回抽象情景说明，旧已生成结果也会在前端展示时归一化。页面新增三步操作引导、模拟上文提示块和更清晰的候选卡片层级。
- 情景候选生成更稳：20 个情景小批量/并行生成，慢批次、空返回或 JSON 异常会自动用本地候选补齐；被动一句式、主动一句式和连续场景都有逐场景兜底，不再出现前三个情景共用同一组三句候选。
- 连续场景预设改为更通用的对话结构：理解偏差补正、信息不足补充、期待差异调整、共同事项协调和话题转向延续，避免把角色默认推向监督、查岗、投喂/服务、威胁式温柔或现实承诺。
- 最后五个连续对话场景进一步去模板化：改为“模拟连续上文”描述互动压力，移除箭头式处理流程；本地兜底候选也改成更轻的日常参考句，避免模型失败时退回客服腔或流程腔。
- 最终人格导出进一步防污染：风格总结只保留抽象说话规则和错误格式，不再生成具体“格式示例”台词；基础稿和最终合并会轻清洗“例如/比如 + 具体原句、食物、单次玩笑”等片段，避免把短期聊天样例固化成人格。
- 候选回复与用户自填只作为风格证据，不会原样写入最终人格；三句候选都不满意时，可写建议后单独重生成该情景。
- 最终人格编辑器会标注来源、冲突和长期可写入性，并可把风格规则分通道保存到对话、创作、计划、内心活动和主动开口链路。

### 配置、兼容与运行安全

- 移除插件配置页里的“启用插件”旧总开关：插件启停只通过 AstrBot 官方插件开关控制；旧配置残留的 `enabled=false` 不再关闭主动循环、命令、工具和事件链路。原本只通过旧配置关掉插件的用户，更新后若 AstrBot 官方插件开关仍为开启，插件会恢复运行；启动日志会提示旧字段已废弃，需要停用请到官方插件管理页关闭。
- 修复 `/陪伴` 与 `/陪伴 帮助` 在新版 AstrBot 指令解析中把 `*args/**kwargs` 误识别为必填参数的问题。
- 兼容 QQ 官方机器人等非 QQ 数字平台用户 ID：私聊目标用户、管理员权限、主动投递映射和 TTS 主用户识别不再强制要求纯数字；误粘贴私聊 UMO 时会尝试提取用户 ID。
- 加固外部屏幕工具安全边界：群聊、次要用户和未授权私聊不再向模型暴露 `screen_peek` / `screen_usage_context`，并注入屏幕隐私边界提示，避免群友通过普通群聊让 Bot 查看部署电脑。
- 修复跨用户互动查询的主人权限误判：`pc_query_interaction` 会使用归一化后的私聊用户 ID 和关系角色判断，启用的主要用户不再被误判为无权查询。
- 优化私聊管理命令权限排障：询问管理员命令、夹层密码或权限问题时会直接进入插件答疑说明，并明确应填写发送者用户 ID。
- 优化模型与 TTS 失败日志：通用 LLM 调用失败会记录任务名、Provider 和错误摘要；TTS 后处理同一次模型失败不再重复记两条 warning；若框架把插件日志来源位置误放进待发送文本，会在发送前静默拦截。
- 新增“今晚晚点睡/陪我熬夜/陪我到凌晨几点或几点再睡”的临时晚睡识别：只写入今晚的睡眠运行态，休息回复闸门在截止前不会把当前睡眠段当成必须沉默；过期后自动回到日程休息，也不会被用户习惯学习误记成长期熬夜偏好。

### 主动消息、生图与内容推荐

- 主动带图与每日穿搭不再把普通“穿搭/衣服/外套”默认导向全身镜/对镜自拍；新增“日常穿搭”预设，只有明确镜前/对镜/镜子时才启用“镜前穿搭”，并默认限制手机挡脸和全身镜突兀构图。
- 修复主动消息主链模型/Provider 失败文本可能进入复核并被改写成空泛主动消息的问题；模型/工具失败诊断会直接视为失败，不再发送给用户。
- 重做主动发送前复核职责边界：本地前置规则负责硬风险、内部泄漏、串台和必要清洗，模型复核负责语境与表达，最终由统一策略层归一化 `send/rewrite/defer/drop`。
- 新增 `tool_call` 生图后端：可调用其它 AstrBot 插件通过 `@filter.llm_tool` 注册的生图函数，并从返回结果中解析本地文件、URL、Base64 或 JSON 图片字段。
- 新增 5 个 tool_call 生图配置项：函数工具名、提示词参数名、类型参数名、参考图参数名和额外参数 JSON。
- 优化 B 站视频和新闻主动分享：对每个目标用户单独读取画像兴趣、弱偏好、边界、近期消息和外部事件评分，从候选新闻/视频中选择更贴合的一条。
- 新闻/视频分享审计会记录用户偏好命中分、选中内容 key 和 `preference_match`，便于在排障页判断为什么分享或跳过。
- 修复 B 站分享 motive 中直接带 QQ 号、像“分享给某用户”的机械表达，改为更自然的用户偏好匹配由头。

### 实验性功能与排障

- 实验性功能区继续收束人格标准化、需求强化、情绪余波和角色贴合校准等实验能力；人格标准化作为一次性工具入口，不再作为运行时功能开关展示。
- 修复候选需求层级全显示“未分类”的问题：英文层级名会正确映射为中文标签。
- 首次配置的日程细化步骤增加外层等待预算；细化超时会转入后台继续生成并在弹窗中说明，不再把配置流程长时间卡死。

## 5.9.0

- 兼容 AstrBot 4.26.5 事件处理器额外上下文参数：事件消息、发送前、LLM 请求/响应和命令入口统一允许额外参数，修复群聊消息与撤回增强报 `takes 2 positional arguments but 3/4 were given` 导致插件链路中断的问题。
- 优化在线生图失败链路：OpenAI 兼容接口遇到 `/images/generations` 或 `/images/edits` 404 时会自动补试 `/v1` 端点；固定在线 API 模式失败后可继续回退备选在线 API、ComfyUI 或 SDGen；生图工具返回会保留真实失败原因和排障提示，避免把 404 误说成超时。
- 修复持久化清理误把 QQ/网页图片占位 `<img .../>`、At、表情等平台消息标签当作非标准控制标签的问题，减少“保存数据前清理非标准控制标签”误报，同时仍会清理 `<bubble/>` 等模型伪动作标签。
- 实验性功能新增“人格标准化问卷”：可读取当前 AstrBot 人格或粘贴人格文本，按社交软件文字聊天模板整理为可编辑审核稿；始终保留人工审核，不会自动覆盖 AstrBot 人格。
- 人格标准化问卷调整为实验页的一次性工具入口，不再作为运行时功能开关展示；实验性功能也不再出现在“功能开关”列表下，避免和正式能力开关混淆。
- 调整人格标准化流程为两阶段：第一步只生成不含强语气约束的基础设定稿；第二步按多个聊天情景生成三句候选回复，由用户选择或自填后再归纳说话方式与对话习惯。
- 稳定对话风格校准改为明确审核流：先选择/粘贴人格并生成基础设定稿，用户确认基本信息后才显示风格参考与情景候选；候选选择只作为证据，最终归纳稳定规则。
- 人格标准化情景试答扩充为 20 个校准项：10 个被动回应、5 个主动开口和 5 个连续互动场景；候选生成、单项重生成和最终归纳会参考已确认基础稿、用户选择/自填与本页风格偏好。
- 首次配置的日程细化步骤增加外层等待预算；细化超时会转入后台继续生成并在弹窗中说明，不再把配置流程长时间卡死。
- 人格标准化最终稿改为提取稳定风格规则：候选回复和用户自填只作为风格证据，不会原样写入人格；当三句候选都不满意时，可填写建议后单独重生成该情景。
- 优化人格标准化审核流状态：修改问卷、来源、基础稿或第二步偏好后，旧基础稿/情景/风格规则会标记为过期并禁用继续操作，避免用户误用旧结果。
- 优化私聊管理命令权限排障：用户自然语言询问“管理员命令/夹层密码/权限为什么用不了”时，会直接进入插件答疑说明，不再交给角色闲聊；权限提示明确填写发送者用户 ID，误粘贴私聊 UMO 会尝试提取，UID、default、平台名或群聊会话串仍无效。
- 在线生图平台新增豆包/火山方舟 Seedream 与 Gemini 适配：主/备在线图片 API 可选择 `doubao` 或 `gemini`，`auto` 也会按地址和模型名识别；豆包走方舟 `/api/v3/images/generations`，Gemini 走 `generateContent` 图片返回并支持本地参考图 `inlineData`。
- 优化 B 站视频和新闻主动分享：不再只按全局最新/全局摘要转给所有目标用户，而是对每个目标用户单独读取画像兴趣、弱偏好、边界、近期消息和外部事件评分，从候选新闻/视频里挑更贴合的一条。
- 新闻/视频分享审计会记录用户偏好命中分、选中的内容 key 和 preference_match，方便在排障页判断为什么给某个用户分享或跳过。
- 修复 B 站分享 motive 中直接带 QQ 号、像“分享给某用户”的机械表达，改为更自然的用户偏好匹配由头。

## 5.8.2

- 修复记忆插件协同开启后保存又被关闭的问题：运行态可用性不再覆盖用户配置；拓展页会显示当前使用的可协同记忆插件，并在“我会牢牢记住你”/MemoryCompanion 与 LivingMemory 同时启用时提示只保留一个。
- 优化主动消息内部模板清洗：移除“这个念头前面忍过一次”“还没散”“想和用户说一句/确认用户在不在”等容易泄漏到正文的后台措辞，旧候选和模型 rewrite 产出的类似 motive 也会统一归一化。
- 优化实验性功能页操作体验：二级页新增粘性返回/跳转工具条和底部返回入口；返回总览不再隐式保存参数或等待运行态刷新，避免页面卡顿和误操作。
- 优化实验性功能页刷新性能：诊断、排障和名单数据在实验页内改为静默加载，最后只重绘一次；开关和保存参数不再触发整页 `loadAll()` 重载，运行态刷新改为后台防抖刷新。
- 实验性功能页重排为更明确的理论可视化页面：总览新增“实验功能地图”和每项运行信号，详情页新增研究型 Hero、理论轴矩阵与量化视图；情绪四维、Plutchik 八情绪、Gross 调节策略、马斯洛需求层、动机调度因子和角色贴合诊断轴都能看到数值与行为映射。
- 补齐实验性功能页类似的运行态断点：需求强化候选摘要会返回需求层、驱动和评分/压力偏移；动机调度页可直接读取主动运行态里的动机分，不再只能依赖排障测试记录；实验页刷新时同步补全私聊/群聊名单。
- 情绪余波实验页补齐运行态展示：用户列表接口会返回轻量情绪状态，实验页可显示四维/基本情绪/调节策略、平稳样本、等待模型复核和复核失败，避免优化后看起来“完全没变化”。
- 修复 5.7.4 后主 LLM 缓存命中可能明显下降的问题：私聊身份锚点包含当前用户 ID、昵称、关系备注和改名记录，不再作为静态 `system_prompt` 前缀；动态提示词优先作为临时 `TextPart` 注入，避免写入 AstrBot 会话历史；请求前会清理历史里旧版本遗留的 `private_companion_*` 动态块，防止旧状态、引用链、群聊防注入和工具提示反复进入主链。
- 修复实验性功能页运行态“看起来没生效”的问题：进入实验页会同步刷新排障/诊断数据，诊断加载完成后会自动重绘当前实验页。
- 动机调度模型的运行态改为读取主动消息链路测试里的“实验动机调度”阶段，不再误从普通诊断项里查找导致长期空白。
- 需求强化功能在暂无带需求层级的主动候选时，会明确提示需要等待主动候选刷新或运行主动消息测试，而不是只显示空卡片。
- 实验性功能页顺序与官方配置页对齐：需求强化功能、动机调度模型、角色贴合校准、情绪余波。
- 角色贴合校准新增“允许自主调节参数”子开关：默认只给建议；开启后可按诊断结果临时覆盖少量主动策略参数，用户手动修改会更新手动值，关闭角色贴合校准或关闭自主调节时恢复到用户最后一次手动设置的值。

## 5.8.1

- 修复用户已经询问 Bot 当前状态后，后续仍可能主动重复分享同一段状态/小日常的问题；私聊里问“在干嘛/忙什么/现在怎么样”等状态问题后，会清理对应状态分享念头和候选。
- 修复次要用户连续多次不回应后仍继续生成或发送普通主动消息的问题；达到未回应阈值后会暂停普通主动、阻断新候选并清理已有待发念头，直到用户重新发消息恢复。
- 收紧次要用户可见上下文边界：地址、当前位置和天气只作为 Bot 自身状态/主要用户侧生活背景使用，不再注入给次要用户，也不会把主要用户所在地天气当作次要用户主动话题。
- 修正插件公开版本号不一致的问题，注册版本、元数据和 README 统一为 `5.8.1`。

## 5.8.0

- 实验性功能页面大修：为情绪余波、需求强化、动机调度和角色贴合校准四项功能定制专用二级页面，新增理论框架与行为映射（伊扎德四维、Plutchik 基本情绪、Gross 调节策略、马斯洛需求层级、驱力/诱因/唤醒理论、艾森克 PEN、大五人格、依恋风格、自我决定理论）和功能效果链路（触发 → 内部行为 → 可观察结果），直观展示功能运作方式。
- 实验性功能运行态数据：从系统实时状态提取用户情绪分布、需求层级柱状图、候选评分和角色贴合诊断建议，不再只是静态文字说明。
- 配置页移除实验性功能冗余入口，统一通过实验性功能标签页管理。
- 修复 `_conf_schema.json` 中 JSON 字符串内未转义双引号导致的解析错误。
- 修复多个 Python 文件开头的 UTF-8 BOM 导致的导入失败。
- 修复实验性功能页"功能说明"排版异常（全局 `dl` 样式覆盖问题）。

## 5.7.11

- 功能开关分类调整：移除独立“情绪模拟”大类，将情绪模拟及其模型复核、阈值、恢复、QQ 空间心情动态参数统一迁入最下方的“实验性功能”。
- 官方配置 schema 同步迁移，原“情绪与关系状态”更名为“意图与关系状态”，只保留意图快判、关系距离和回复策略相关配置，避免重复分类。
- 进阶配置引导新增最下方“实验性功能”板块，情绪余波、需求强化、动机调度和人格迭代建议都在这里选择；情绪模拟保留旧默认值，不会因迁移丢配置或突然关闭。
- 优化未完话头/记忆插件旧话题回钩：只有和当前话题贴合、用户主动回问，或能一句轻轻带过时才作为回复参考；不再因为存在旧话题就生硬打断当前聊天。

## 5.7.10

- 情绪模拟继续参考 Gross 情绪调节过程模型：在受伤/修复、四维状态、基本/复合情绪之外，新增内部“调节策略”层。
- 调节策略会按当前余波、紧张度、激动度、确信度和活跃情绪，在避开高压、换低压问法、转移注意、重新理解和短答降压之间选择，不把理论名写进实际回复。
- 用户详情页“情绪余波”新增调节策略与策略候选展示，方便排查 Bot 为什么选择退开、换问法、安抚落点或收住外显反应。

## 5.7.9

- 情绪模拟继续引入 Plutchik 基本情绪/复合情绪视角：在四维强弱调性之外，新增喜悦、信任、恐惧、惊讶、悲伤、厌恶、愤怒、期待 8 个短期情绪强度。
- 新增复合情绪推导：例如喜悦+信任会形成亲近/喜欢，期待+喜悦形成期待/乐观，厌恶+愤怒形成反感，愤怒+期待形成顶回去倾向；这些只作为内部拟人化倾向，不要求模型说出理论名。
- 回复提示从“心理学标签”改为自然行为约束：比如火气明显时少解释先降压、失望难受时放慢承认感受、亲近感带软时自然靠近但不过度黏。
- 用户详情页“情绪余波”新增基本/复合情绪与活跃情绪展示，便于检查 Bot 变安静、别扭、期待或贴近的原因。

## 5.7.8

- 情绪模拟接入伊扎德情绪四维视角：在原有 `mood_score`、受伤/修复闸门之外，新增愉快度、紧张度、激动度、确信度四维短期状态。
- 情绪四维会随伤害、道歉、安抚、夸奖、自我低落、边界表达等事件变化，并随时间自然回到基线；旧的受伤阈值、恢复速度、公开心情动态触发逻辑保持兼容。
- 被动回复提示现在会在四维状态明显异常时给出更细的语气约束，例如紧张且激动时短答降压、确信度低时不要替用户下定论、愉快且放松时允许自然靠近。
- 用户详情页“情绪余波”新增四维展示，方便排查 Bot 为什么变得安静、柔和、犹豫或更贴近。

## 5.7.7

- 新增独立“实验性功能”大类，并加入第一项“需求强化功能”；可用类似马斯洛需求层级的内部结构给主动念头分类和轻量排序。
- 需求强化目前默认只影响主动候选价值分、打扰压力和排障/审计标记，不会把“状态/安全/归属/尊重/成长/意义”等层级词写进实际回复正文。
- 新增“允许影响日程生成”子选项，默认关闭；开启后日程生成会把需求层级当作轻量倾向，影响休息、探索、等待、准备和低打扰互动的安排。
- 新增“需求强化影响强度”参数，支持 `0–100` 调整；`0` 仅记录层级，默认 `35` 为温和影响。

## 5.7.6

- 在线生图平台新增“魔搭社区 / ModelScope”适配：可在在线生图平台与备选在线生图平台中选择 `modelscope`，也可在 `auto` 下通过 `api-inference.modelscope.cn` 地址自动识别。
- 魔搭社区文生图现在按异步任务链路处理：提交 `/v1/images/generations` 后会读取 `task_id` 并轮询 `/v1/tasks/{task_id}`，兼容 `images`、`output_images`、`results`、`data` 等常见图片结果字段。
- README 和扩展页同步补充魔搭社区配置说明，方便直接填写 `https://api-inference.modelscope.cn/v1` 或完整 `/v1/images/generations` 地址。

## 5.7.5

- QQ 空间说说新增“说说风格提示词”配置，可单独约束自动生活说说、心情说说和说说草稿测试的口吻；默认更像轻量生活碎片，减少哲理总结、人生感悟、诗化独白和文案腔。
- QQ 空间自动配图新增“空间配图提示词”配置，可单独约束构图和画面选择；默认不再固定优先镜前自拍/手机挡脸自拍，会按说说正文和当前日程在生活物件、食物饮品、路上光影、桌面一角、窗边侧脸、背影、第一视角手部等构图间轮换。
- QQ 空间配图提示词后处理同步收紧：即使选择人物入镜，也会优先采用侧脸、背影、肩颈半身、窗边剪影、随身小物等自然入镜方式，只有正文或日程明确涉及穿搭、照镜子、整理仪容时才考虑镜前构图。

## 5.7.4

- Token 页面新增来源切换：可在陪伴插件、记忆插件和 LLM 主链之间切换查看，避免多套统计堆在同一页里难以分辨。
- LLM 主链会话排行改用更易读的私聊/群聊标签，不再直接展示 `default:FriendMessage:...` 这类原始 AstrBot 会话 ID。
- 生图提示词链路改为更标准的英文 `Positive prompt / Negative prompt` 形式，覆盖今日穿搭、自然语言生图/改图、主动带图和内置场景预设。
- 生图排障里的最近提示词改名为“最近提交给生图后端的提示词”，并优化长提示词、图片路径和结果内容的换行展示。
- 默认自然语言生图附加提示词、配置 schema 和 README 说明同步改成英文短语风格，减少中文说明文本被误传给生图后端的概率。
- 新增群级 LLM 回复熔断命令：在目标群发送 `陪伴群 关闭LLM` 可临时关闭该群所有 LLM 回复，`陪伴群 开启LLM` 恢复；排障页会固定示警当前熔断群，不依赖可清理的最近异常记录。
- 修复首次配置引导的主动消息测试可能长期停留在 pending 的问题：前端最多等待 120 秒并给出明确失败提示，后端会恢复超时的临时主动计划，陈旧 pending 记录不再让弹窗无限卡住。
- README 生图联动说明补充后端能力边界：明确区分纯文生图与参考图/改图能力，并记录 `astrbot_plugin_nai_image` 可作为 OpenAI Images 本地代理使用但当前不适合参考图链路。

## 5.7.3

- 长线主动新增“私聊主动触达”入口，主动文本生成、触达频率、主动类型偏好和人格/世界观复核集中在同一个功能详情页，避免用户找不到主动类型设置。
- 新增主动类型偏好权重：窥屏、带图、戳一戳、语音均可按 `0–300%` 调整；`100%` 为默认，`0%` 表示自动主动不再抽到该类型，但不会关闭底层能力或手动测试入口。
- 主动动作选择链路接入类型权重：候选提示、普通主动升级、日程故事事件、关系状态轻分享和带图+语音组合都会尊重该偏好，同时仍受功能开关、额度、冷却、后端可用性和发送复核限制。
- 配置页补齐四个权重项的中文名、释义、输入范围和进阶引导入口，并刷新前端缓存版本，避免浏览器继续加载旧功能开关面板。
- 修复 QQ 空间手动评论偶发失败：当 QQ 接口返回 JSON 后继续拼接 `document.domain`、`try/catch` 等脚本片段时，响应解析器不再把后续脚本误吞进 JSON。
- 优化主 LLM 缓存友好注入：私聊被动增强会把回复风格和身份锚点拆成稳定 `system_prompt` 前缀，把每轮变化的状态、图片、日程和记忆继续后置到当前请求尾部，减少动态上下文破坏 Provider prompt cache 的概率。
- 图片视觉缓存签名改为稳定语义版本，不再因为识图提示词小幅优化就击穿同图缓存；角色外观线索和上下文看图请求仍会进入签名，避免错误复用。
- 旧图片缓存命中时会轻量修正“当前角色表情包”摘要：归属只作为辅助，回复目标优先接住表情包的表情、动作、文字梗和情绪，避免 Bot 过度关注“这是我”。
- 与“我会牢牢记住你”联动时，记忆检索侧支持短 TTL 结果缓存；同一轮重复模拟召回、注入或工具查询会减少重复检索、Embedding query 和 Rerank 调用，同时仍按会话、权限、配置和记忆库 revision 隔离。

## 5.7.2

- 重新整理配置引导里的群聊增强分类：按群聊启用范围、连续对话保持、群聊读空气、高压消息合并、群聊回复理解、群聊安全保护、群聊主动唤醒、群聊观察学习、复读处理、跨群转述和跨用户记忆展示，减少重复入口，让常用功能更容易第一时间找到。
- 群聊相关模型与性能参数跟随对应功能展示，不再混在大而全的总开关下；读空气、主动唤醒、观察学习、关系网、黑话、话题线和片段整理的注释同步补准。
- 修复部分新分组配置在旧扁平兼容键已存在时可能只同步旧位置的问题；现在保存会同步旧键并确保写入新 schema 分组，降低更新后配置看似保存但下次加载位置不一致的风险。
- 配置页补强持久化失败提示：字体、主题以及常规设置保存后如果只写入运行态但未能落盘，会明确提示重启后可能恢复旧值。
- 统一进阶配置引导里的百分比输入：概率类字段统一按百分比显示和填写，提交时按后端需要自动转换，避免 `0.15` 与 `15%` 混用导致实际概率偏差。
- 修复智能沉默置信度在引导和功能详情中的单位不一致问题，统一显示为百分比并正确提交为小数概率。

## 5.7.1

- 智能沉默新增判断模式：默认 `boundary_only` 维持原有保守行为，只在用户明确说别聊、别问、换话题、不用回复等边界语义时调用模型；可切到 `contextual`，让短句收尾、忙了/睡了、敷衍回应、群聊普通反应等也进入模型判断，减少 Bot 明显该收住时还硬接话。
- 优化休息/睡眠中的被动回复：短句叫醒、查岗、例行检查等场景会额外提示主模型短答收住，并约束记忆插件把旧“例行检查/查岗/梦境/穿着”等召回只当语气底色，不再因为相似旧话题展开长段梦境或回忆。
- 收紧自拍/改图参考图链路：带参考图的生图不再静默回退到 SDGen、ComfyUI images=0 工作流或在线纯文生图；如果后端没有真正接收参考图，会明确返回“工作流不接收图片/参考图接口失败/后端不支持参考图”，避免生成一张完全不像参考图的图片却被当作成功。
- 修复生图排障页“测试自拍”没有显式传入解析后的参考图、结果展示容易误判“已带参考图”的问题；现在会先解析本地/URL/今日穿搭参考图，并只在后端确认使用参考图后标记通过。
- 显式 `陪伴 生图/自拍/改图` 指令从自然语言生图开关和主要用户限制中拆出：自然语言自动接管仍保持保守，显式指令只要求用户是已启用陪伴对象，并使用独立的每日指令额度，避免关闭自然语言入口后指令也表现为失效。
- 新增 `pc_generate_photo` 生图工具：主链模型在用户明确要求画图、自拍、头像、表情包或改图时可直接调用插件生图能力；工具支持 `text2img/selfie/sticker/edit`、参考图、尺寸、是否直接发送和 `scene_preset` 场景预设，并会把自拍、头像、穿搭、COS 等角色出镜请求纠偏到自拍链路；角色表情包默认走 `sticker`，使用自拍/人像参考图链路和“表情包场景”预设。
- `pc_generate_photo` 补齐回写链路：工具生成后会标注最近生图排障记录的触发来源、意图类型、是否发送、场景预设和附言，并在可用时同步到记忆插件的 Bot 行为记忆；用户问“刚才画了什么/发了什么图/表情包是什么”时自我时间线也能命中这条记录。
- 非指令生图新增三档处理方式：默认 `tool_first` 由主链模型调用 `pc_generate_photo`，`rule_fast` 才启用插件前置规则快判并受每日上限控制，`off` 会停止工具提示注入并拦截误调用；配置页、引导、命令排障和 schema 文案同步区分工具调用与规则快判。
- 参考图命令支持直接预览：`陪伴 参考图 查看/预览/检查/发出来` 会返回当前配置状态，并在本地参考图可用时把 Bot 实际用于自拍/人像链路的参考图一并发出；配置为 URL 时会先尝试下载并回写为稳定本地文件。
- 每日穿搭图支持更自然的重生命令：`陪伴 重新生成穿搭图 / 生成穿搭图 / 刷新穿搭图 / 重置穿搭图` 都会强制生成新的今日穿搭图；底层 `force=True` 也不再复用当天已有穿搭记录。
- 优化首次配置引导的日程步骤：默认改为“快速生成今日日程”，不再强制等待当前时段完整细化；“补充当前细化”拆成可选慢操作，并在生成今日日程成功前禁用，避免新用户误点后长时间等待。
- 配置引导的日程结果展示改为简要状态卡，优先显示是否生成成功、日程点数量和当前细化状态；详细粗日程和细化内容改为可展开查看，减少首次配置时的信息噪音。
- 修正首次配置阻塞提示和加载态文案，避免继续提示“必须生成并细化当前时段”；现在只要求快速日程链路通过即可继续后续配置。
- 进阶配置引导继续收敛为板块内问卷式体验：按通用能力、私聊增强、群聊增强、主动增强逐项确认是否需要，并在弹窗内直接保存对应功能开关和关键参数。
- 进阶配置引导补充联动前置说明：B 站、屏幕陪伴、QQ 空间、夹层阅读、主动搜索、TTS 和识图等能力会直接说明需要的插件、服务或凭据。
- 主动带图/生图引导改为可直接填写后端关键信息：在线图片 API、ComfyUI 工作流、SDGen 前置、参考图、风格、固定提示词和非指令生图处理方式都能在弹窗内配置。
- 优化配置引导的选择页和问卷选项样式：引导类型卡片改为清晰的摘要 + 条目列表，进阶问卷顶部改为轻量进度栏，二选一和开关项改为更轻的分段/开关交互，减少顶部信息块和右侧列表挤压。
- 配置引导里的说明性文案统一改成轻量状态提示，不再使用像输入框的边框卡片承载“已连接、已预填、后续可配置”等纯说明内容，避免用户误以为需要填写。
- 首次配置的“快速生成今日日程”改为 setup 专用快速链路：优先复用今日已有日程，不再同步等待穿搭图、新闻阅读、当前细化等后续动作；模型生成超时会先写入兜底粗日程并放行，后台继续生成正式日程，避免引导长时间卡在日程步骤。

## 5.7.0

- 总览页新增“配置引导”入口：点击后在大弹窗内完成问卷式首次配置，覆盖基础连通、目标用户 ID 与平台、运行策略与权限、快捷模型配置、人格与世界知识、主动能力、日程初始化、私聊主动强度、群聊唤醒/插话、关系网和主动消息测试。
- 首次配置引导已落地保存：完成前会把当前草稿写入插件配置与运行态，主动消息测试前也会先保存目标用户、平台和模型等必要配置，避免刚填完用户 ID 就测试不到目标用户。
- 快捷模型配置改为在弹窗内直接选择并逐项测试四类模型；所有必需模型测试通过后才能进入下一步，避免用户只选模型但实际不可用。
- 人格与世界知识步骤改为先选择人格，再手动点击推导世界知识草稿；推导完成后可在弹窗内修改确认，并明确提示世界知识只服务插件，实际对话人格仍由 AstrBot 配置决定。
- 日程与观察步骤新增“生成今日日程和当前细化”按钮，并在弹窗内展示生成结果；私聊主动强度预设会实时展示并允许手动调整对应参数。
- 群聊与关系网拆分为独立步骤；唤醒增强默认按现有配置关闭，关闭时不展示其下配置；关系网首个用户词条配置去掉不适合首次引导的绑定 QQ、重要记忆草稿和优先级。
- 主动消息测试复用排障链路：点击后等待约 15 秒进行完整主动消息测试，实时展示结果；通过后可保存完成并选择自行探索或继续后续进阶引导。
- 配置引导补齐步骤阻塞校验和进度条状态，已完成步骤显示绿色；移除页面切换过渡动画，减少弹窗内操作时的跳动感。
- 配置引导新增“进阶配置”：点击后先选择通用能力、私聊增强、群聊增强或主动增强，再按板块用问卷形式确认功能是否需要，并用大白话说明功能作用和开启注意事项；保存时会落到对应功能开关和关键参数。
- 修正 OpenAI 兼容 Provider 在插件下拉框里统一显示为 `openai` 的问题：现在优先使用 AstrBot 供应商显示名/Provider ID，必要时再按 API 地址或模型名推断火山引擎、DeepSeek、百炼等供应商。
- 修正主动候选总览把已拦截候选也算进“未发送候选”、以及重复合并次数无限增长导致来源分布显示上万次的问题；候选合并计数现在会自动封顶，旧数据会在启动/保存时压缩，候选总数改为展示记录数，触发次数只作为说明。
- TTS 生成前的空内容判断进一步收紧：只有情绪标签、动作括号或纯标点时不再调用语音合成；带正文的情绪标签仍会保留给支持的 TTS 供应商。
- 移除“主链会话锁兼容模式”功能和配置项；新版本不再提供该开关，也不再额外创建同会话主链锁。
- 修复群聊主动插话可能把 `（空字符串）`、`【不回复】`、`null` 等“不发言占位”当作正文发送并写入 `last_bot_interjection` 的问题；源头已从“正文/空文本”改为 JSON 决策 `should_reply + text`，插话清洗和群聊发送前静默拦截也会兜底归一化括号、引号和结尾标点。
- 修正私聊图片链路的上下文优先级：单图回复仍避免把旧邀约/旧日程当当前图片内容，但最近对话里用户明确规定“这张/下一张图片只回某句、不要回复其他内容”时会优先遵守，不再被图片边界提示降权成普通语气背景。
- 修复休息静默闸门把“你不用回复图片内容，夸夸我就行”误判成“不要回复我”的问题；带有图片/内容等明确作用域和替代回复方式的句子不再触发勿扰，已有误判静默也会在读取时自动清理。
- 吸收 PR #48 的安全部分：JSON 提取增加智能引号、注释、尾逗号和 Python 风格单引号字典/列表的容错；TTS 生成兼容当前会话 Provider 暴露 `synthesize_text` 的形态，但不绕过 AstrBot 配置主动扫描其他 TTS 插件。
- 群聊引用触发消息新增“连续同对象只首条引用”策略：同一群内连续回复同一个群友时只首次引用，换人或超过连续对话窗口后再重新引用，减少群聊里连续消息每条都挂引用的打扰感。

## 5.6.15

- 移除“私聊工具安全裁剪”功能和相关配置项，不再在普通私聊被动主链前改写 AstrBot 工具列表。
- 修复配置备用生图 API 后，`/陪伴 生成穿搭` 在部分适配器上可能把同一张穿搭图发送两次的问题；穿搭命令现在走单一媒体回复链路，并对同一会话同图同文增加短时间去重。

## 5.6.14

- 修复私聊单图/GIF 走手动分段发送时，`[[PCTTS:...]]` 私有 TTS 保护占位可能出现在后续分段或日志预览里的问题；现在发送前会先尝试按当前事件还原为 `<tts>` 语音块，无法还原的孤儿占位会清理掉，分段链路也有兜底过滤。

## 5.6.13

- 修复自然语言生图/改图可能把“我会牢牢记住你”的上下文注入块、内部 instruction 或记忆分区说明当作图片 prompt 传给生图后端，导致生成图片里出现大段提示词/记忆文本的问题；现在图片模型只接收清洗后的视觉连续性参考（穿搭、地点、外观、颜色、背景等），并明确禁止生成文字说明栏。
- 每日穿搭图的记忆连续性参考也改用同一套视觉清洗，避免 MemoryCompanion 标签、内部分工说明或非视觉记忆进入图片 prompt。

## 5.6.12

- 修复私聊单图识图链路过度依赖配置顺序的问题：会话配置里的空图片转文字字段不再遮住 AstrBot 全局“默认图片转文字模型”；成功识图的视觉模型会被记录为近期恢复候选，但不会覆盖 AstrBot 明确配置的首位。首选不可用或失败降权后，才会用最近成功模型加速恢复。识图模型返回空摘要或“看不到图”类不可用摘要时不再提前结束，而是临时降权并继续尝试下一个候选。排障页也会显示私聊图片识别的最近成功模型、当前候选可用性和临时降权原因。
- 收紧私聊图片识别的快速配置边界：视觉转述候选只允许 AstrBot 默认图片转文字模型、插件识图模型和已验证的视觉恢复候选，不再把快速配置里的复杂推理模型/插件主模型或 AstrBot 默认聊天模型拿来跑识图；历史运行态中记录过的 `plugin_main` 视觉成功也会被过滤，避免旧状态绕回。
- 调整拓展页模型配置页：模型分组不再默认折叠，也不再通过鼠标移入/点击标题展开收起；快速配置/精准配置仍只负责显示当前模式需要填写的模型项。
- 私聊被动主链的高风险工具裁剪改为可选功能“私聊工具安全裁剪”，默认关闭；需要更强安全边界时可在拓展页“功能开关 -> 通用能力”或官方配置页开启，开启后才会裁掉 Shell、文件读写、启动程序和跨会话发消息等工具。
- 强化私聊单图无视觉摘要保护：识图失败且图片没有直挂给主模型时，不再进入主链生成“看图回复”，改用更自然的短句说明没读出图片、请用户点一下重点，避免主动状态/历史话题里的“路上、天色、冰激凌”等旧上下文被模型当成图片内容并分段漏出。
- 修复私聊单图识图回复后“下一句不记得刚才看过图”的问题：手动发送链路现在会把“用户发图/图片摘要 + 已发出的 Bot 回复首段或单段回复”写成一轮标准 AstrBot 会话历史，并同步给 MemoryCompanion 的可见对话窗口；保留轮数继续按 AstrBot/记忆插件自身上下文配置裁剪，不再用固定一次的临时缓存。
- 修复私聊单图识图失败后仍进入主链导致模型凭历史猜图的问题：没有可靠视觉摘要且图片未直接交给支持图片的主模型时，会丢弃看图式主链回复，改为短句请用户补充要看的位置。
- 收窄群聊关系网“近期参与者”注入：主要用户/目标陪伴用户不会再仅因最近发过言就被注入到其他群友当前发言里，避免普通群友被误套主要用户身份。
- 吸收 PR #45 的运行时保护：通用文本清理会移除泄漏的 `<think>` / `<reasoning>` 块；TTS 生成前会跳过纯标点朗读文本；群黑话释义遇到模型返回非 JSON 时只跳过本轮刷新，不再进入 30 分钟短冷却。
- 优化主动强度预设梯度：`标准偏主动 / 私聊高频 / 群聊活跃 / 在线陪伴` 的有效差异显著拉开；最高档“在线陪伴”不按每日主动次数或每日群聊插话次数封顶，空闲 0 分钟、最小间隔 5 分钟、群唤醒 3 秒冷却，并忽略 Token 软限额降载，但仍保留免打扰、休息、用户拒绝、隐私和每日 Token 硬限额。
- 修复主动消息发送失败后复用待重发内容时，可能把“直接在当前对话中输出这条主动消息”这类内部发送指令当作正文发给用户的问题；现在新生成、复核改写、发送前硬拦截和待重发缓存都会识别并丢弃这类提示词泄漏，旧污染缓存也会自动清空并重新排程。
- 收束主动消息发送闸：新增统一本地校验入口，主动复核、真实发送前、待重发读取和待重发写入都复用同一套“回执/内部指令/工具摘要/空内容”判定；待重发内容如果只能清出安全正文会改写后继续，否则直接丢弃并重新排程。
- 优化 QQ 空间 Cookie 自动获取：参考 `astrbot_plugin_qzone_ultra` 的绑定思路，自动模式会尝试 `get_cookies/get_credentials`、多种 OneBot action 调用签名、多域名候选和嵌套/JSON 返回结构；缺少 `uin` 时会用 `get_login_info` 补齐，并在扩展页状态中记录上次 Cookie 获取是否拿到 `uin/skey/p_skey`。

## 5.6.11

- 修复主动搜索使用自定义 GET 接口时，配置为 API 尾插或尾部 `?`/`&` 的地址会拼接失败的问题；现在支持 `{query}`、`{{query}}`、`${query}` 占位、已有查询参数和尾插写法，超时提示也改成更直观的“请求超时”。
- 修复外部图片结果下载遇到鉴权/防盗链时容易 403 的问题：主动图片结果拉取会使用浏览器式请求头，并在同源结果地址上安全携带自定义鉴权头；拓展页也补齐主/备用图片 API 自定义头配置。
- 修复用户引用的消息本身又引用更早消息时，回复前只能看到一层引用的问题；现在会最多读取 3 层引用链，并把原始被引用内容注入 `reply.chain` 上下文，同时引用图片识别也会沿链查找。
- 优化次要用户长时间未回应后的主动念头降级：不再只是拉长发送间隔，而会按未回应程度逐级改成低压文字、留出空间，并清理旧念头语义缓存，避免旧高压念头隔很久后原样回来。
- 修复“习惯关心”把用户常说的“早”当作普通主动话题，导致早上已经自然聊过后仍发早安类主动的问题；带称呼的“某某，早……”也会按早间问候去重和拦截。

## 5.6.10

- 修复 `<bubble/>` 伪分段标记泄漏到聊天的问题：内部历史、表达学习、主动/工具直发和发送前兜底都会清理未知自闭合控制标签；同时清理本地 `companions.json` 中已污染的 `<bubble/>` 样本并保留备份。
- 修复 QQ 空间发布成功后 Bot 自己“不记得刚才发了什么说说”的问题：说说发布成功会写入本插件自我时间线，并在可用时同步到 MemoryCompanion 的人格生活记忆；用户问“刚才发了什么说说/QQ 空间发了什么”时会按真实发布记录回答。
- 修复拓展页在部分 AstrBot Page 桥实现下只显示“加载失败：未找到该路由”的问题：前端请求会自动尝试 `page/...`、裸路由和完整插件名前缀等多种端点格式，命中后缓存可用格式。
- 修复撤回图片缓存持续膨胀的问题：`recall_message_images` 现在会跟随撤回缓存秒数自动清理过期图片，并新增 `recall_message_image_cache_max_mb` 磁盘体积上限（默认 256MB）；启动维护也会执行一次清理，避免群聊图片/GIF 长期堆到数 GB。
- 修复 QQ 状态同步在 NapCat 等协议端上的兼容风险：基础在线/忙碌状态仍默认可用，但“QQ 自定义短状态”拆成独立开关并默认关闭；执行层不再调用已知会让部分 NapCat 版本断连的 `set_custom_online_status`，睡觉/忙碌等状态在未开启自定义短状态时会回退为标准在线/忙碌。
- 主动搜索新增可选自定义搜索接口：可在拓展页“功能开关 -> 长线主动 -> 主动搜索详情 -> 自定义搜索接口”或官方配置页“主动搜索”分组填写接口地址、API Key 和模型；留空仍使用 AstrBot 全局网页搜索，且只接管主动搜索空档探索，不影响新闻阅读和群黑话联网参考。
- 修复缺少 `torch` 等可选模型依赖时，私聊图片存在性检测可能把 `on_private_message` 整个处理器炸掉的问题：私聊空消息判断、图文分支和自然语言生图参考图检测统一改用安全图片检测，缺依赖时按无图片继续并写入降级日志。
- 增加 AstrBot 指令注册诊断日志：插件初始化时会输出 `/陪伴`、`/陪伴群` 及其别名是否已进入 AstrBot handler registry，方便区分“指令未注册”和“指令查询/LLM 执行器缓存未刷新”。
- 补充 QQ 空间评论回复排障确认：评论收件箱自动回复成功时日志会记录说说 ID、评论 ID、评论者和实际回复文本；当前不会默认打印对方原评论全文，避免公开评论内容在日志中过度展开。
- 修复主动私聊发送的核心兜底平台寻址：当历史 UMO 为 `aiocqhttp:FriendMessage:...` 但当前平台实例 ID 是 `default` 等其他名称时，兜底发送会使用实际平台实例 ID 重建 `MessageSession`，避免 AstrBot 核心日志继续报 `cannot find platform for session aiocqhttp:...`；平台实例本身就是 `aiocqhttp` 的环境会保持原 ID，不写死为 `default`。
- 修复群聊消息误影响私聊主动的问题：私聊主动的空闲判定、问候候选、发送前 recent-chat guard，以及发送期间“用户有新消息则取消”保护，都改为只看私聊专用活动时间和私聊入站计数，群聊发言不再打断私聊主动消息或排障页主动消息测试。
- 新增低概率“群里冒泡但私聊未回”的私聊候选：当用户长时间未回复上一条私聊主动、但本人又在群聊里发言时，会在冷却和人格倾向都合适的情况下低概率排入一条低压抱怨/撒娇式私聊候选；不会直接硬发，也不会因为普通群聊消息打断既有私聊主动。
- 强化 QQ 空间自拍配图参考图链路：配图提示词设计前会预检自拍参考图状态，选择 `selfie` 时会显式把今日穿搭/人设参考图传给生图入口，并在日志和扩展页摘要状态中记录参考图路径与是否存在，便于排查“自拍不像参考图”的问题。
- 优化 QQ 空间配图提示词逻辑：配图模型现在先输出唯一视觉锚点和构图，再决定 `selfie` 或 `text2img`，减少泛天气图、多主体拼贴和无依据自拍；链路测试、日志和扩展页摘要会显示视觉锚点/构图，方便区分“提示词设计跑偏”和“生图后端执行跑偏”。
- QQ 空间说说配图默认开启：`enable_qzone_generated_image_publish` 的代码默认值和配置页 schema 默认值改为 `true`，配置页说明补充“手动发说说未附图时会直接尝试生成配图”。
- 手动发说说接入自动配图：扩展页发布、聊天命令“陪伴 发说说”和 LLM 工具发布在未附图且有正文时，会调用 `_maybe_generate_qzone_publish_image(reason="manual_publish", force=True)` 生成配图；自动生活说说/情绪说说仍按原概率逻辑，不会被强制每次配图。
- QQ 空间配图策略调整为人物优先：配图提示词优先选择 `selfie`，让人物成为主角；即使没有明确自拍动机，也鼓励使用第一视角手部、侧脸、背影、镜中局部、肩颈半身等自然入镜方式，避免纯景物照片。没有参考图时外貌参考人格描述和公开状态，不再强制降级为纯生活碎片。
- 手动发布配图状态独立记录为 `manual_publish` 前缀，避免覆盖自动生活说说的配图排障状态。


## 历史版本

- `5.6.9` 及更早版本已移动到 [CHANGELOG_ARCHIVE.md](CHANGELOG_ARCHIVE.md)，主文件只保留当前开发版和最近稳定版本，方便日常维护。
## 6.2.3

- 修正内容扩展联动入口显示逻辑。
- 未安装或未启用内容扩展时隐藏 QQ 空间与书柜入口。
- 内容扩展启用后显示对应联动入口，保留本体兼容回退。
